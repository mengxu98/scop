% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IntegrationBenchmarkPlot.R
\name{IntegrationBenchmarkPlot}
\alias{IntegrationBenchmarkPlot}
\title{Plot integration benchmark results}
\usage{
IntegrationBenchmarkPlot(
  object,
  plot_type = c("auto", "box", "heatmap", "scatter", "umap"),
  tool_name = "IntegrationBenchmark",
  metrics = NULL,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  pt.size = NULL,
  pt.alpha = 1,
  boxplot_jitter = FALSE,
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  verbose = TRUE,
  ...,
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object from \code{\link[=RunIntegrationBenchmark]{RunIntegrationBenchmark()}}. \code{"box"} can
also plot any metadata columns ending in \code{"_LISI"}.}

\item{plot_type}{Plot type, or \code{"auto"} for a named list of all views.}

\item{tool_name}{\code{srt@tools} entry created by \code{\link[=RunIntegrationBenchmark]{RunIntegrationBenchmark()}}.}

\item{metrics}{Optional metric names used in the heatmap.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{theme_use, theme_args}{Theme name or function, plus extra theme arguments.}

\item{pt.size, pt.alpha}{Point size and transparency. \code{pt.size = NULL} scales
with \code{sqrt(n)} (minimum \code{0.3}). Rasterized points keep at least a two-pixel
radius at \code{raster.dpi = c(512, 512)} and scale with \code{raster.dpi}.}

\item{boxplot_jitter}{Whether to overlay jittered points on the LISI boxes.}

\item{combine}{Combine plots with \link[patchwork:patchwork]{patchwork}. \code{combine = FALSE} returns a list of ggplots.}

\item{nrow}{Combine plots with \link[patchwork:patchwork]{patchwork}. \code{combine = FALSE} returns a list of ggplots.}

\item{ncol}{Number of columns of the combined plot.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{The message to print.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\value{
A ggplot, patchwork, or a named list of plots when
\code{plot_type = "auto"}.
}
\description{
Visualize a \code{Seurat} object returned by \code{\link[=RunIntegrationBenchmark]{RunIntegrationBenchmark()}}.
\code{"box"} compares per-cell LISI scores across methods, \code{"heatmap"} shows
scaled summary metrics, \code{"scatter"} plots the bio-vs-batch trade-off, and
\code{"umap"} draws batch and cell-type embeddings. \code{"auto"} returns all of these
plots.
}
\examples{
\dontrun{
data(panc8_sub)
panc8_sub <- RunIntegrationBenchmark(
  panc8_sub,
  batch = "tech",
  celltype = "celltype",
  methods = c("Uncorrected", "Harmony"),
  nHVF = 500,
  linear_reduction_dims = 20,
  linear_reduction_dims_use = 1:10,
  perplexity = 10
)
IntegrationBenchmarkPlot(panc8_sub)
IntegrationBenchmarkPlot(panc8_sub, plot_type = "box")
IntegrationBenchmarkPlot(panc8_sub, plot_type = "heatmap")
IntegrationBenchmarkPlot(panc8_sub, plot_type = "scatter")
}
}
\seealso{
\link{RunIntegrationBenchmark}, \link{RunLISI}
}
