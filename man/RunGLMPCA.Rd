% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunGLMPCA.R
\name{RunGLMPCA}
\alias{RunGLMPCA}
\alias{RunGLMPCA.Seurat}
\alias{RunGLMPCA.Assay}
\alias{RunGLMPCA.Assay5}
\alias{RunGLMPCA.default}
\title{Run generalized principal components analysis (GLMPCA)}
\usage{
RunGLMPCA(object, ...)

\method{RunGLMPCA}{Seurat}(
  object,
  assay = NULL,
  layer = "counts",
  features = NULL,
  L = 5,
  fam = c("poi", "nb", "nb2", "binom", "mult", "bern"),
  rev.gmlpca = FALSE,
  ndims.print = 1:5,
  nfeatures.print = 30,
  reduction.name = "glmpca",
  reduction.key = "GLMPC_",
  verbose = TRUE,
  seed.use = 11,
  ...
)

\method{RunGLMPCA}{Assay}(
  object,
  assay = NULL,
  layer = "counts",
  features = NULL,
  L = 5,
  fam = c("poi", "nb", "nb2", "binom", "mult", "bern"),
  rev.gmlpca = FALSE,
  ndims.print = 1:5,
  nfeatures.print = 30,
  reduction.key = "GLMPC_",
  verbose = TRUE,
  seed.use = 11,
  ...
)

\method{RunGLMPCA}{Assay5}(
  object,
  assay = NULL,
  layer = "counts",
  features = NULL,
  L = 5,
  fam = c("poi", "nb", "nb2", "binom", "mult", "bern"),
  rev.gmlpca = FALSE,
  ndims.print = 1:5,
  nfeatures.print = 30,
  reduction.key = "GLMPC_",
  verbose = TRUE,
  seed.use = 11,
  ...
)

\method{RunGLMPCA}{default}(
  object,
  assay = NULL,
  layer = "counts",
  features = NULL,
  L = 5,
  fam = c("poi", "nb", "nb2", "binom", "mult", "bern"),
  rev.gmlpca = FALSE,
  ndims.print = 1:5,
  nfeatures.print = 30,
  reduction.key = "GLMPC_",
  verbose = TRUE,
  seed.use = 11,
  ...
)
}
\arguments{
\item{object}{An object.
Can be a Seurat object, an assay object, or a matrix-like object.}

\item{...}{Additional arguments to be passed to the \link[glmpca:glmpca]{glmpca::glmpca} function.}

\item{assay}{The assay to be used for the analysis.
Default is \code{NULL}.}

\item{layer}{The layer to be used for the analysis.
Default is \code{"counts"}.}

\item{features}{The features to be used for the analysis.
Default is \code{NULL}, which uses all variable features.}

\item{L}{The number of components to be computed.
Default is \code{5}.}

\item{fam}{The family of the generalized linear model to be used.
Currently supported values are \code{"poi"}, \code{"nb"}, \code{"nb2"}, \code{"binom"}, \code{"mult"}, and \code{"bern"}.
Default is \code{"poi"}.}

\item{rev.gmlpca}{Whether to perform reverse GLMPCA (i.e., transpose the input matrix) before running the analysis.
Default is \code{FALSE}.}

\item{ndims.print}{The dimensions (number of components) to print in the output.
Default is \code{1:5}.}

\item{nfeatures.print}{The number of features to print in the output.
Default is \code{30}.}

\item{reduction.name}{The name of the reduction to be stored in the Seurat object.
Default is \code{"glmpca"}.}

\item{reduction.key}{The prefix for the column names of the basis vectors.
Default is \code{"GLMPC_"}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed.use}{The random seed to be used.
Default is \code{11}.}
}
\description{
Run generalized principal components analysis (GLMPCA)
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)
pancreas_sub <- RunGLMPCA(pancreas_sub)
CellDimPlot(
  pancreas_sub,
  group.by = "CellType",
  reduction = "glmpca"
)
}
