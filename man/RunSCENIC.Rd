% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSCENIC.R
\name{RunSCENIC}
\alias{RunSCENIC}
\title{Run SCENIC gene regulatory network analysis}
\usage{
RunSCENIC(
  srt,
  assay = NULL,
  layer = "counts",
  ranking_dbs = NULL,
  motif_annotations = NULL,
  regulators = NULL,
  targets = NULL,
  work_dir = "scenic_output/",
  species = c("Homo_sapiens", "Mus_musculus", "Drosophila_melanogaster"),
  genome = NULL,
  data_dir = NULL,
  prefix = "scenic",
  min_expr_cells = 3,
  min_regulon_size = 10,
  backend = c("cpp", "python"),
  n_rounds = 5000,
  learning_rate = 0.01,
  max_depth = 3,
  max_features = 0.1,
  subsample = 0.9,
  early_stop_window_length = 25,
  cores = 1,
  seed = 1234,
  force = FALSE,
  assay_name = "scenic",
  tool_name = "SCENIC",
  return_seurat = TRUE,
  envname = NULL,
  conda = "auto",
  prepare_env = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{layer}{Assay layer used as the count matrix.}

\item{ranking_dbs}{Character vector of cisTarget ranking feather files. If
\code{NULL}, the gene-based v10 cisTarget ranking databases are prepared from
\code{species}.}

\item{motif_annotations}{Motif annotation table used by \verb{scenic ctx}. If
\code{NULL}, the v10 motif2tf table is prepared from \code{species}.}

\item{regulators}{Transcription factors used as candidate regulators in
GRNBoost2. This can be a character vector of gene names or one text file. If
\code{NULL}, the cisTarget TF list is prepared from \code{species}.}

\item{targets}{Optional target genes used to restrict the GRN output. This
can be a character vector of gene names or one text file. Regulator
expression is still kept as predictor input for GRNBoost2, and the adjacency
table passed to \verb{scenic ctx} is filtered to these targets.}

\item{work_dir}{Directory used for SCENIC input and output files.}

\item{species}{Species used to select cisTarget reference files when
\code{ranking_dbs}, \code{motif_annotations}, or \code{regulators} is \code{NULL}. Supported
values are \code{"Homo_sapiens"}, \code{"Mus_musculus"}, and
\code{"Drosophila_melanogaster"}.}

\item{genome}{Genome build used to select cisTarget reference files when
automatic references are prepared. Human supports \code{"hg38"} (default) and
\code{"hg19"}. Mouse and fly currently use \code{"mm10"} and \code{"dm6"}, respectively.}

\item{data_dir}{Directory used to cache automatically prepared SCENIC
reference files. If \code{NULL}, files are stored under
\verb{tools::R_user_dir("scop", "data")/SCENIC/<species>}.}

\item{prefix}{Prefix for SCENIC output files.}

\item{min_expr_cells}{Minimum number of cells where a gene must be detected
before GRNBoost2. To run SCENIC on metacells, first create a metacell-level
object with \code{\link[=RunMetaCell]{RunMetaCell()}} and pass that object to \code{RunSCENIC()}.}

\item{min_regulon_size}{Minimum regulon size kept after \verb{scenic ctx}.}

\item{backend}{SCENIC backend. \code{"cpp"} uses the R/C++ path and
\code{"python"} uses the Python pySCENIC path. The selected backend controls GRN,
cisTarget pruning, and AUCell scoring together.}

\item{n_rounds}{Number of boosting rounds used by GRNBoost2.}

\item{learning_rate}{Learning rate used by GRNBoost2.}

\item{max_depth}{Maximum tree depth used by GRNBoost2.}

\item{max_features}{Fraction of features sampled by GRNBoost2.}

\item{subsample}{Row subsampling fraction used by GRNBoost2.}

\item{early_stop_window_length}{Early-stopping window used by GRNBoost2.}

\item{cores}{Number of workers used by GRNBoost2, \verb{scenic ctx}, and
AUCell scoring. If multicore execution is not supported, this is
automatically reduced to one core.}

\item{seed}{Random seed used by GRNBoost2 and Seurat overclustering.}

\item{force}{Whether to rebuild existing SCENIC outputs.}

\item{assay_name}{Name of the assay used to store regulon activity scores.}

\item{tool_name}{Name of the \code{srt@tools} entry.}

\item{return_seurat}{Whether to return the modified Seurat object. If
\code{FALSE}, a result list is returned.}

\item{envname}{Python environment used for SCENIC. If \code{NULL}, the isolated
\code{"scenic_env"} environment is used.}

\item{conda}{The path or command name of a conda-compatible executable.}

\item{prepare_env}{Whether to prepare and configure the SCENIC Python
environment before running.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat object with SCENIC results, or a result list when
\code{return_seurat = FALSE}.
}
\description{
Run SCENIC gene regulatory network analysis
}
\examples{
\dontrun{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)
pancreas_sub <- RunSCENIC(
  pancreas_sub,
  species = "Mus_musculus"
)
}
}
