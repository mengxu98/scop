% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialDM.R
\name{RunSpatialDM}
\alias{RunSpatialDM}
\title{Run official SpatialDM spatial ligand-receptor association analysis}
\usage{
RunSpatialDM(
  srt,
  species = c("human", "mouse"),
  lr.database = NULL,
  assay = NULL,
  layer = "data",
  counts.layer = "counts",
  image = NULL,
  coord.cols = c("col", "row"),
  coordinate.unit = NULL,
  method = c("z-score", "permutation"),
  run.local = TRUE,
  l = NULL,
  eff_dist = NULL,
  cutoff = 0.1,
  n_neighbors = NULL,
  n_neighbor_layers = 6,
  single_cell = FALSE,
  complex.mean = c("algebra", "geometric"),
  min_cell = 3,
  n_perm = 1000,
  nproc = 1,
  seed = 1,
  global.fdr = TRUE,
  global.threshold = 0.1,
  local.fdr = FALSE,
  local.threshold = 0.1,
  result.name = "default",
  envname = "spatialdm_env",
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} spatial object.}

\item{species}{Built-in SpatialDM species (\code{"human"} or \code{"mouse"}).}

\item{lr.database}{Optional custom LR table with \code{ligand}, \code{receptor}, and
\code{annotation} columns.}

\item{assay, layer, counts.layer}{Assay and normalized/count expression layers.}

\item{image, coord.cols}{Spatial image or metadata coordinate columns.}

\item{coordinate.unit}{Unit of the raw input coordinates. Automatic unit selection uses technology-specific rules.}

\item{method}{Official global/local test mode.}

\item{run.local}{Whether to run locally.}

\item{l, eff_dist}{SpatialDM RBF scale; one must be supplied.}

\item{cutoff}{RBF weight cutoff used by SpatialDM to retain local spatial
weights. This is a fraction between zero and one; it is not a Scissor
alpha-search parameter.}

\item{n_neighbors}{Number of neighbors to use for constructing the KNN graph.}

\item{n_neighbor_layers}{Number of neighbor layers to use.}

\item{single_cell}{Whether to use the single-cell spatial mode supported by
SpatialDM. This is not a generic "single-cell rather than spatial" switch.}

\item{complex.mean}{Mean used for complex ligand-receptor expression:
\code{"algebra"} or \code{"geometric"}.}

\item{min_cell}{Minimum number of cells required.}

\item{n_perm}{Number of permutations.}

\item{nproc}{Number of parallel processes.}

\item{seed}{Random seed.}

\item{global.fdr, local.fdr}{Whether the corresponding p-value table uses FDR
values for selection. These are logical switches, not numeric thresholds.}

\item{global.threshold, local.threshold}{Selection thresholds for global and
local results.}

\item{result.name}{Stored result name.}

\item{envname}{Conda-compatible Python environment. If \code{NULL}, the environment name will be set to \code{"scop_env"}.}

\item{overwrite}{Whether to overwrite existing metadata.}

\item{verbose}{Whether to print messages.}
}
\value{
A Seurat object with a schema-v1 \code{SpatialDM} result bundle.
}
\description{
Run official SpatialDM spatial ligand-receptor association analysis
}
