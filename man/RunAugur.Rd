% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunAugur.R
\name{RunAugur}
\alias{RunAugur}
\title{Prioritize perturbed cell types using Augur}
\usage{
RunAugur(
  srt,
  celltype.by,
  label.by,
  assay = NULL,
  layer = "counts",
  backend = c("cpp", "r"),
  features = NULL,
  n_subsamples = 50,
  subsample_size = 20,
  folds = 3,
  min_cells = NULL,
  var_quantile = 0.5,
  feature_perc = 0.5,
  cores = 1,
  select_var = TRUE,
  augur_mode = c("default", "velocity", "permute"),
  classifier = c("rf", "lr"),
  rf_params = list(trees = 100, mtry = 2, min_n = NULL, importance = "accuracy"),
  lr_params = list(mixture = 1, penalty = "auto"),
  prefix = "augur",
  tool_name = "Augur",
  add_meta = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{celltype.by}{Metadata column or vector defining the cell types used by
Augur.}

\item{label.by}{Metadata column or vector defining the labels to predict,
such as condition, stimulation, sample type, or technology.}

\item{assay}{Assay used to extract the feature matrix. If \code{NULL}, the
default assay is used.}

\item{layer}{Assay layer used as the feature matrix.}

\item{backend}{Backend used to run Augur. \code{"cpp"} uses a parity-preserving
\code{scop} backend that keeps Augur's sampling, random forest, and metric
semantics but removes the heavier recipe/bake loop and uses C++ sparse
matrix preparation where possible. \code{"r"} calls the native
\code{Augur::calculate_auc} implementation.}

\item{features}{Features used by Augur. If \code{NULL}, all features in \code{assay}
are used.}

\item{n_subsamples, subsample_size, folds, min_cells, var_quantile, feature_perc, select_var, augur_mode, classifier, rf_params, lr_params}{Arguments passed to \code{Augur::calculate_auc}.}

\item{cores}{Number of cores used by Augur.}

\item{prefix}{Prefix for metadata columns written to \code{srt@meta.data}.}

\item{tool_name}{Name of the \code{srt@tools} entry used to store Augur results.}

\item{add_meta}{Whether to write \code{prefix_auc} and \code{prefix_rank} metadata
columns back to each cell by matching \code{celltype.by}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{Augur::calculate_auc}.}
}
\value{
A \code{Seurat} object with native Augur results stored in
\code{srt@tools[[tool_name]]}. When \code{add_meta = TRUE}, cell-level metadata columns
\code{prefix_auc} and \code{prefix_rank} are added for use with existing \code{scop}
plotting functions such as \code{\link[=FeatureDimPlot]{FeatureDimPlot()}}.
}
\description{
Prioritize perturbed cell types using Augur
}
\examples{
data(panc8_sub)
panc8_sub <- subset(panc8_sub, subset = tech \%in\% c("celseq", "celseq2"))
panc8_sub <- standard_scop(panc8_sub, verbose = FALSE)
panc8_sub <- RunAugur(
  panc8_sub,
  celltype.by = "celltype",
  label.by = "tech",
  n_subsamples = 5,
  subsample_size = 20,
  min_cells = 20,
  cores = 1,
  verbose = FALSE,
  rf_params = list(
    trees = 20,
    mtry = 2,
    min_n = NULL,
    importance = "accuracy"
  )
)

panc8_sub@tools$Augur$AUC
FeatureDimPlot(
  panc8_sub,
  features = "augur_auc",
  reduction = "StandardUMAP2D",
  bg_cutoff = -Inf
)
}
\references{
Skinnider, M.A., Squair, J.W., Kathe, C., et al. (2021). Cell type
prioritization in single-cell data. \emph{Nature Biotechnology}, 39,
30-34. \doi{10.1038/s41587-020-0605-1}
}
