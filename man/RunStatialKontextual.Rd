% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunStatialKontextual.R
\name{RunStatialKontextual}
\alias{RunStatialKontextual}
\title{Run Statial Kontextual spatial relationships}
\usage{
RunStatialKontextual(
  srt,
  group.by,
  r,
  from = NULL,
  to = NULL,
  parent = NULL,
  parent_df = NULL,
  image = NULL,
  sample.by = NULL,
  images = NULL,
  coord.cols = NULL,
  inhom = FALSE,
  edge_correct = TRUE,
  window = c("convex", "square", "concave"),
  window.length = NA_real_,
  include_original = TRUE,
  cores = 1,
  tool_name = "StatialKontextual",
  store_results = TRUE,
  store_input = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{group.by}{Metadata column containing cell or spot labels.}

\item{r}{Numeric radius or radii used by \code{Statial::Kontextual()}.}

\item{from, to, parent}{Cell or spot labels passed to \code{Statial::Kontextual()}.
Ignored when \code{parent_df} is supplied.}

\item{parent_df}{Optional data frame from \code{Statial::parentCombinations()}.}

\item{image}{Name of the Seurat spatial image. If \code{NULL}, the first image is
used when present.}

\item{sample.by}{Optional metadata column used as Statial \code{imageID}. If
\code{NULL}, all cells or spots are treated as one image.}

\item{images}{Optional Statial image filter passed to \code{Kontextual(image = )}.}

\item{coord.cols}{Metadata coordinate columns to use explicitly. If \code{NULL},
Seurat image coordinates are used first when available, then metadata \code{x/y}
or \code{col/row}.}

\item{inhom}{Whether Statial should account for inhomogeneity.}

\item{edge_correct}{Whether Statial should perform edge correction.}

\item{window, window.length}{Window arguments passed to
\code{Statial::Kontextual()}.}

\item{include_original}{Whether to include original L-function values.}

\item{cores}{Number of cores passed to \code{Statial::Kontextual()}.}

\item{tool_name}{Name used to store results in \code{srt@tools}.}

\item{store_results}{Whether to store results in \code{srt@tools}.}

\item{store_input}{Whether to store the backend input cell table in
\code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional named arguments passed to \code{Statial::Kontextual()}.}
}
\value{
A \code{Seurat} object with Statial results stored in
\code{srt@tools[[tool_name]]} when \code{store_results = TRUE}.
}
\description{
Run \code{Statial::Kontextual()} on a spatial \code{Seurat} object to quantify
pairwise cell or spot label relationships relative to a parent context.
Results are stored as a compact SCOP bundle with raw Statial output,
standardized summary, and parameters. \code{Statial} is an optional Bioconductor
dependency installable with \code{BiocManager::install("Statial")}.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub

if (
  requireNamespace("Statial", quietly = TRUE) &&
    identical(Sys.getenv("SCOP_RUN_SPATIAL_BACKEND_EXAMPLES"), "true")
) {
  labels <- unique(as.character(spatial$coda_label))
  if (length(labels) >= 2) {
    spatial <- RunStatialKontextual(
      spatial,
      group.by = "coda_label",
      r = 50,
      from = labels[1],
      to = labels[2],
      parent = labels[1:2],
      coord.cols = c("x", "y"),
      verbose = FALSE
    )
    spatial@tools$StatialKontextual$summary
  }
}
}
