% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCytoSPACE.R
\name{RunCytoSPACE}
\alias{RunCytoSPACE}
\title{Run CytoSPACE spatial assignment}
\usage{
RunCytoSPACE(
  srt,
  reference,
  reference_label,
  assay = NULL,
  reference_assay = NULL,
  layer = "counts",
  reference_layer = "counts",
  features = NULL,
  cell_fractions = NULL,
  n_cells_per_spot = NULL,
  mean_cell_numbers = 5,
  scRNA_max_transcripts_per_cell = 1500,
  sampling_method = "duplicates",
  seed = 1,
  prefix = "CytoSPACE",
  store_results = TRUE,
  verbose = TRUE,
  image = NULL,
  coord.cols = c("col", "row"),
  coordinate_space = c("legacy_display", "raw")
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{reference}{Reference \code{Seurat} object containing annotated single cells.}

\item{reference_label}{Metadata column in \code{reference} with cell type labels.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{reference_assay}{Assay used in \code{reference}.}

\item{layer, reference_layer}{Assay layers used for spatial and reference
expression.}

\item{features}{Features used for assignment. If \code{NULL}, shared features are
used.}

\item{cell_fractions}{Optional cell-type fractions. Provide a named numeric
vector, one-row matrix/data.frame, or a spot-by-cell-type matrix/data.frame.
Spot-level rows are aggregated to the global composition used by the default
CytoSPACE assignment workflow.}

\item{n_cells_per_spot}{Optional number of cells assigned to each spatial
spot. If \code{NULL}, counts are estimated from spatial RNA reads with
\code{mean_cell_numbers}.}

\item{mean_cell_numbers}{Mean number of cells per spot. Default \code{5}, matching
the CytoSPACE Visium default.}

\item{scRNA_max_transcripts_per_cell}{Maximum reference transcripts per cell
before assignment. Default \code{1500}, matching CytoSPACE.}

\item{sampling_method}{Sampling method. Only \code{"duplicates"} is supported in
the package runtime.}

\item{seed}{Random seed used for deterministic reference downsampling and
duplicate sampling.}

\item{prefix}{Prefix for metadata columns.}

\item{store_results}{Whether to store detailed assignment results in
\code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{image}{Optional Seurat image used for spatial coordinates.}

\item{coord.cols}{Metadata coordinate columns used when no image is selected.}

\item{coordinate_space}{Coordinate space used for assignment locations. The
default preserves the historical coordinate behavior.}
}
\value{
A \code{Seurat} object with CytoSPACE metadata columns and detailed
results stored in \code{srt@tools[["CytoSPACE"]]}.
}
\description{
Run CytoSPACE spatial assignment
}
\examples{
data(visium_human_pancreas_sub)
data(pancreas_sub)
features_use <- intersect(
  rownames(visium_human_pancreas_sub),
  rownames(pancreas_sub)
)
spatial <- RunCytoSPACE(
  visium_human_pancreas_sub,
  reference = pancreas_sub,
  reference_label = "CellType",
  features = features_use,
  mean_cell_numbers = 1
)

SpatialSpotPlot(
  visium_human_pancreas_sub,
  group.by = "coda_label",
  theme_use = "theme_scop"
)

SpatialSpotPlot(
  spatial,
  group.by = "CytoSPACE_dominant_type",
  theme_use = "theme_scop"
)
}
