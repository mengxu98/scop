% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCNV.R
\name{RunCNV}
\alias{RunCNV}
\title{Run copy-number alteration inference}
\usage{
RunCNV(
  srt,
  method = c("copykat", "fastCNV", "scevan", "infercnv", "numbat", "casper"),
  assay = NULL,
  layer = "counts",
  group.by = NULL,
  reference.by = NULL,
  reference = NULL,
  genome = c("hg38", "hg19", "mm10"),
  gene_order = NULL,
  sample.by = NULL,
  allele_counts = NULL,
  reference_counts = NULL,
  loh = NULL,
  loh_name_mapping = NULL,
  cytoband = NULL,
  output_dir = NULL,
  prefix = "CNV",
  tool_name = "CNV",
  store_matrix = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{method}{CNA/CNV backend. Supported backends are \code{"copykat"},
\code{"fastCNV"}, \code{"scevan"}, \code{"infercnv"}, \code{"numbat"}, and \code{"casper"}.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{layer}{Assay layer used as the expression matrix.}

\item{group.by}{Optional metadata column forwarded to supported backends and
stored as cell annotation.}

\item{reference.by}{Metadata column identifying reference/normal cells.
Required for \code{"infercnv"} and \code{"fastCNV"}.}

\item{reference}{Reference labels in \code{reference.by}.}

\item{genome}{Reference genome label.}

\item{gene_order}{Gene coordinate table or a path to one. The table should
contain gene, chromosome, start, and end columns. If \code{NULL}, SCOP tries to
resolve these columns from assay feature metadata.}

\item{sample.by}{Optional sample metadata column.}

\item{allele_counts}{Allele count table for \code{"numbat"}. This is forwarded
to \code{numbat::run_numbat()} as \code{df_allele}.}

\item{reference_counts}{Reference expression profile for \code{"numbat"}. This
is forwarded to \code{numbat::run_numbat()} as \code{lambdas_ref}.}

\item{loh}{B-allele frequency/LOH signal for \code{"casper"}.}

\item{loh_name_mapping}{Optional CaSpER LOH-to-cell mapping table.}

\item{cytoband}{Cytoband table for \code{"casper"}.}

\item{output_dir}{Optional backend output directory.}

\item{prefix}{Prefix for metadata columns.}

\item{tool_name}{Name used for \code{srt@tools}.}

\item{store_matrix}{Whether to store the normalized CNV matrix in
\code{srt@tools[[tool_name]]}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters forwarded to the selected backend.}
}
\value{
A \code{Seurat} object with CNV metadata columns and a result bundle in
\code{srt@tools[[tool_name]]}.
}
\description{
Run expression-based single-cell or spatial copy-number alteration backends
and store the results in a unified SCOP schema.
}
\examples{
\dontrun{
# copykat uses raw counts and can infer diploid/aneuploid cells directly.
srt <- RunCNV(
  srt,
  method = "copykat",
  genome = "hg38"
)

# fastCNV and inferCNV require a normal/reference cell annotation.
srt <- RunCNV(
  srt,
  method = "fastCNV",
  reference.by = "celltype",
  reference = "Normal",
  genome = "hg38"
)

gene_order <- data.frame(
  gene = rownames(srt),
  chr = "chr1",
  start = seq_len(nrow(srt)) * 1000,
  end = seq_len(nrow(srt)) * 1000 + 999
)
srt <- RunCNV(
  srt,
  method = "infercnv",
  reference.by = "celltype",
  reference = "Normal",
  gene_order = gene_order
)

# Numbat and CaSpER require allele-aware inputs from matched DNA/allele
# preprocessing workflows.
srt <- RunCNV(
  srt,
  method = "numbat",
  allele_counts = df_allele,
  reference_counts = lambdas_ref,
  genome = "hg38"
)
srt <- RunCNV(
  srt,
  method = "casper",
  reference.by = "celltype",
  reference = "Normal",
  gene_order = gene_order,
  loh = baf_signal,
  cytoband = cytoband_hg38
)

CNVPlot(srt, plot_type = "heatmap", group.by = "CNV_prediction")
CNVPlot(srt, plot_type = "dim", value = "CNV_prediction")
}
}
\seealso{
\link{CNVPlot}
}
