% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DeconvolutionPlot.R
\name{DeconvolutionPlot}
\alias{DeconvolutionPlot}
\title{Plot deconvolution results}
\usage{
DeconvolutionPlot(
  object = NULL,
  res = NULL,
  plot_type = c("bar", "heatmap", "box"),
  sample_order = NULL,
  cell_type_order = NULL,
  palette = "Chinese",
  palcolor = NULL,
  heatmap_palette = "YlGnBu",
  heatmap_palcolor = NULL,
  sample_annotation = NULL,
  sample_annotation_palette = "Chinese",
  sample_annotation_palcolor = NULL,
  sample_split = NULL,
  cluster_rows = FALSE,
  cluster_columns = FALSE,
  show_row_names = TRUE,
  show_column_names = FALSE,
  theme_use = "theme_scop",
  theme_args = list(),
  legend.position = "right",
  legend.direction = "vertical",
  grid_major = FALSE,
  ...,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object containing deconvolution
results in \code{metadata(object)[["Deconvolution"]]}.}

\item{res}{A deconvolution result data frame. When provided, \code{object} is
ignored.}

\item{plot_type}{Plot type. One of \code{"bar"}, \code{"heatmap"}, or \code{"box"}.}

\item{sample_order}{Optional sample order.}

\item{cell_type_order}{Optional cell-type order.}

\item{palette}{Palette used for discrete cell-type colors in \code{"bar"} and
\code{"box"} modes.}

\item{palcolor}{Optional custom colors for \code{palette}.}

\item{heatmap_palette}{Palette used for continuous proportions in
\code{"heatmap"} mode.}

\item{heatmap_palcolor}{Optional custom colors for \code{heatmap_palette}.}

\item{sample_annotation}{Character vector of \code{colData(object)} columns used
as top annotations in \code{"heatmap"} mode.}

\item{sample_annotation_palette}{Palette(s) used for \code{sample_annotation}.}

\item{sample_annotation_palcolor}{Optional custom colors for
\code{sample_annotation_palette}. Use a list when multiple annotations are
provided.}

\item{sample_split}{Optional \code{colData(object)} column used to split heatmap
columns for grouped comparison.}

\item{cluster_rows, cluster_columns}{Whether to cluster rows or columns in
\code{"heatmap"} mode.}

\item{show_row_names, show_column_names}{Whether to show row or column names in
\code{"heatmap"} mode.}

\item{theme_use}{Theme function name. Default is \code{"theme_scop"}.}

\item{theme_args}{Additional theme arguments passed to \code{theme_use}.}

\item{legend.position}{Legend position. Default is \code{"right"}.}

\item{legend.direction}{Legend direction. Default is \code{"vertical"}.}

\item{grid_major}{Whether to show major grid lines for \code{"bar"} and \code{"box"}
plots. Default is \code{FALSE}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{ggplot} object.
For \code{plot_type = "heatmap"}, returns a \code{ComplexHeatmap::Heatmap} object.

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\description{
Visualize bulk or pseudobulk deconvolution results from a
\code{SummarizedExperiment} object or a supplied result table.
}
\examples{
data(islet_bulk)
data(panc8_sub)
islet_bulk <- RunDeconvolution(
  islet_bulk,
  reference = panc8_sub,
  method = "MuSiC",
  group.by = "celltype"
)
DeconvolutionPlot(islet_bulk, plot_type = "bar")

DeconvolutionPlot(islet_bulk, plot_type = "box")

ht <- DeconvolutionPlot(
  islet_bulk,
  plot_type = "heatmap",
  sample_annotation = "condition",
  sample_split = "condition"
)
ComplexHeatmap::draw(ht)
}
\seealso{
\link{RunDeconvolution}
}
