% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunscMalignantFinder.R
\name{RunscMalignantRegion}
\alias{RunscMalignantRegion}
\title{Run scMalignantFinder malignant spatial region identification}
\usage{
RunscMalignantRegion(
  srt = NULL,
  adata = NULL,
  h5ad = NULL,
  assay = "RNA",
  layer = "counts",
  cells = NULL,
  signature_gmt,
  features = NULL,
  nclus = 3,
  define_feature = "Malignant_up",
  spatial_nn = TRUE,
  spatial.cols = NULL,
  spatial_key = "spatial",
  image = FALSE,
  norm_type = NULL,
  prefix = "scMalignantFinder_",
  return_seurat = !is.null(srt),
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{adata}{Optional Python AnnData object.}

\item{h5ad}{Optional path to an \code{.h5ad} file.}

\item{assay}{Assay used when \code{srt} is supplied. Default is \code{"RNA"}.}

\item{layer}{Layer used when \code{srt} is supplied. Default is \code{"counts"}.}

\item{cells}{Optional cells to run. If supplied with \code{srt}, results are
appended to these cells and other cells receive \code{NA}.}

\item{signature_gmt}{Path to the malignant signature \code{.gmt} file, such as
\code{sc_malignant_deg.gmt} from the scMalignantFinder resources.}

\item{features}{Features in \code{adata.obs} used for region clustering.
If \code{NULL}, uses \code{malignancy_probability} and \code{Malignant_up}, plus
\code{image_score} when \code{image = TRUE}.}

\item{nclus}{Number of clusters used by the spatial region model.}

\item{define_feature}{Feature used to select the malignant region cluster.}

\item{spatial_nn}{Whether to refine labels by spatial neighbors.}

\item{spatial.cols}{Optional two metadata columns used as spatial
coordinates for Seurat input.}

\item{spatial_key}{Key written to \code{adata.obsm} for spatial coordinates.}

\item{image}{Whether to call \code{scMalignantFinder.spatial.image_cal}.
Default is \code{FALSE} because Seurat-to-AnnData conversion does not generally
carry histology images.}

\item{norm_type}{Passed to \code{scMalignantFinder}. Use \code{TRUE} for raw counts
that should be library-size normalized; use \code{FALSE} for already normalized
input. If \code{NULL}, defaults to \code{TRUE} only for Seurat counts input.}

\item{prefix}{Optional prefix for output metadata columns. Default preserves
the original \code{scMalignantFinder} column names.}

\item{return_seurat}{Whether to return a Seurat object when \code{srt} is
supplied. If \code{FALSE}, returns a data frame of predictions.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat object with malignant region metadata, or a data frame when
\code{return_seurat = FALSE}.
}
\description{
Use \code{scMalignantFinder} spatial utilities to score malignant signatures and
infer malignant spatial regions. For Seurat input, provide \code{spatial.cols}
when spatial-neighborhood refinement is requested and the converted AnnData
object does not already contain spatial coordinates.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
srt <- RunscMalignantRegion(
  srt,
  signature_gmt = "path/to/sc_malignant_deg.gmt",
  spatial.cols = c("x", "y")
)
\dontshow{\}) # examplesIf}
}
