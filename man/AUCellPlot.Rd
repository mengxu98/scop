% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AUCellPlot.R
\name{AUCellPlot}
\alias{AUCellPlot}
\title{Plot a HAR-style AUCell dashboard from a Seurat object}
\usage{
AUCellPlot(
  srt,
  features = NULL,
  auc = NULL,
  group.by = NULL,
  reduction = NULL,
  dims = c(1, 2),
  cells = NULL,
  thresholds = NULL,
  add_box = TRUE,
  add_bar = !is.null(thresholds),
  ncol = 5,
  nrow = NULL,
  point.size = 0.5,
  raster.dpi = 300,
  point.fraction = 0.1,
  point.alpha = 0.2,
  boxplot.y.range = TRUE,
  group.palette = "Chinese",
  group.palcolor = NULL,
  feature.palette = "Chinese",
  feature.palcolor = NULL,
  auc_palette = "Spectral",
  auc_palcolor = NULL,
  highlight.groups = NULL,
  highlight.label = FALSE,
  highlight.mark.color = NULL,
  highlight.label.color = "grey40",
  highlight.mean.color = NULL,
  highlight.color = NULL,
  ellipse.level = 0.98,
  show.auc.legend = TRUE,
  group.title = NULL,
  legend.labels = NULL,
  threshold.colors = c(`Above AUC threshold` = "black", `Below AUC threshold` = "grey50"),
  theme.base.size = 11,
  cell_dim_args = list(),
  combine = TRUE,
  seed = 2025,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A `Seurat` object.}

\item{features}{Score names to plot. For metadata input these are metadata
columns; for `auc` input these are matrix row or column names. A named
character vector uses its names as display labels.}

\item{auc}{Optional numeric matrix or data frame, with features by cells or
cells by features. When `NULL`, scores are read from `srt` metadata.}

\item{group.by}{Metadata column used for cell groups. `NULL` uses active
Seurat identities.}

\item{reduction}{Dimensional reduction. `NULL` uses [DefaultReduction()].}

\item{dims}{Two dimensions to plot.}

\item{cells}{Cells to include. `NULL` uses all cells.}

\item{thresholds}{Optional AUCell thresholds. Supply one value per feature,
a named vector keyed by display or source feature name, or `"auto"` to
infer thresholds from the supplied scores with AUCell. When `NULL` and
`add_bar = TRUE`, automatic thresholds are used.}

\item{add_box}{Add the HAR-style AUCell boxplot.}

\item{add_bar}{Add the threshold-proportion bar chart. If `TRUE` without
explicit `thresholds`, thresholds are inferred automatically with AUCell.
By default the bar is added whenever `thresholds` is supplied.}

\item{ncol}{Number of physical columns. The HAR layout uses `5`.}

\item{nrow}{Number of physical rows. Rows occur in UMAP/statistic pairs and
must therefore be even. `NULL` chooses the minimum required value. The HAR
layout for nine features uses `4`.}

\item{point.size}{Point size in rasterized UMAPs.}

\item{raster.dpi}{Raster resolution for UMAP points.}

\item{point.fraction}{Fraction of cells shown as jittered points in each
boxplot. Boxplot statistics always use all cells.}

\item{point.alpha}{Alpha for jittered points.}

\item{boxplot.y.range}{If `TRUE`, use the original HAR boxplot y-axis range
of 0--0.5 when it contains all scores; otherwise use the score range.}

\item{group.palette, group.palcolor}{Group color palette arguments passed to
[thisplot::palette_colors()].}

\item{feature.palette, feature.palcolor}{Feature-title color palette
arguments passed to [thisplot::palette_colors()].}

\item{auc_palette, auc_palcolor}{Continuous AUCell palette name and optional
custom colors, resolved with [thisplot::palette_colors()]. The default
`"Spectral"` matches other continuous feature plots in scop.}

\item{highlight.groups}{Optional groups to highlight with the HAR-style 98%
t ellipse and optional label. An unnamed vector applies to every feature;
a named vector or list maps display/source feature names to groups.}

\item{highlight.label}{Whether to label highlighted groups on AUCell UMAPs.
Defaults to `FALSE`; highlight ellipses are still drawn.}

\item{highlight.mark.color}{Color of highlighted group outlines. `NULL`
inherits the matching group colors, consistent with [CellDimPlot()].}

\item{highlight.label.color}{Highlight label color; defaults to grey.}

\item{highlight.mean.color}{Mean AUC line and text color. `NULL` inherits
`highlight.label.color`.}

\item{highlight.color}{Compatibility shortcut that sets all highlight
colors when their newer arguments are not supplied.}

\item{ellipse.level}{Confidence level for highlighted group ellipses.}

\item{show.auc.legend}{Which AUCell UMAPs show a continuous legend: `TRUE`
shows the first, `FALSE` shows none, or supply feature names. Its position
is fixed at the bottom of the assembled figure.}

\item{group.title}{Optional group legend title.}

\item{legend.labels}{Optional named replacement labels for groups.}

\item{threshold.colors}{Colors for cells above and below each threshold.}

\item{theme.base.size}{Base font size.}

\item{cell_dim_args}{Named list forwarded to [CellDimPlot()] for the group
dimensional plot. This exposes labels, selected-cell outlines, marks,
density, grid, graph, PAGA, velocity, and related functionality.
Structural arguments and legend placement remain controlled here.}

\item{combine}{Return a patchwork when `TRUE`; otherwise return named plot
components before layout spacers are inserted.}

\item{seed}{Seed used for automatic threshold inference and jitter-point
sampling.}

\item{verbose}{Whether to report plotting progress.}
}
\value{
A `patchwork` object, or a named list of `ggplot`/`patchwork`
  components when `combine = FALSE`.
}
\description{
A parameterized port of the AUCell plotting workflow used by HARNexus. Each
feature occupies a paired slot: an AUCell UMAP above a boxplot (and, when a
threshold is available, a stacked threshold bar chart). The first
slot contains a [CellDimPlot()] group map and its legend. Scores are plotted
as supplied; this function does not recalculate AUC scores. When requested,
automatic thresholds are inferred from those scores with AUCell.
}
\examples{
data(pancreas_sub)
pancreas_sub <- RunStandardWorkflow(pancreas_sub)
pancreas_sub$AUCell_A <- seq_len(ncol(pancreas_sub)) / ncol(pancreas_sub)
AUCellPlot(
  pancreas_sub,
  features = c("Program A" = "AUCell_A"),
  group.by = "SubCellType"
)
}
