% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunGiottoSpatialMethods.R
\name{RunGiottoSpatialGenes}
\alias{RunGiottoSpatialGenes}
\title{Run Giotto spatial gene detection}
\usage{
RunGiottoSpatialGenes(
  srt,
  assay = NULL,
  layer = "data",
  features = NULL,
  image = NULL,
  coord.cols = c("x", "y"),
  network_method = "Delaunay",
  bin_method = c("kmeans", "rank"),
  set_variable_features = TRUE,
  top_n = 100,
  tool_name = "GiottoSpatialGenes",
  store_giotto = FALSE,
  conversion_params = list(),
  network_params = list(),
  binSpect_params = list(),
  verbose = TRUE,
  seed = 11
)
}
\arguments{
\item{srt}{A Seurat object.}
\item{assay}{Which assay to use. If \code{NULL}, the default assay is used.}
\item{layer}{Assay layer used as the expression matrix.}
\item{features}{Features to test. If \code{NULL}, variable features are used, falling back to all assay features.}
\item{image}{Name of the Seurat spatial image.}
\item{coord.cols}{Metadata coordinate columns used when no image is available.}
\item{network_method}{Spatial network method passed to \code{Giotto::createSpatialNetwork()}.}
\item{bin_method}{Binning method passed to \code{Giotto::binSpect()}.}
\item{set_variable_features}{Whether to update \code{VariableFeatures()}.}
\item{top_n}{Number of top spatial genes to store in \code{srt@misc}.}
\item{tool_name}{Name of the \code{srt@tools} entry used to store results.}
\item{store_giotto}{Whether to store the full Giotto object in \code{srt@tools}.}
\item{conversion_params}{Additional parameters passed to \code{Giotto::createGiottoObject()}.}
\item{network_params}{Additional parameters passed to \code{Giotto::createSpatialNetwork()}.}
\item{binSpect_params}{Additional parameters passed to \code{Giotto::binSpect()}.}
\item{verbose}{Whether to print messages.}
\item{seed}{Random seed for reproducibility.}
}
\value{A \code{Seurat} object with spatial gene results in \code{srt@tools} and top genes in \code{srt@misc[[tool_name]]}.}
\description{
Run Giotto \code{binSpect()} spatial gene detection and store the ranked result
table and top features in a \code{Seurat} object.
}
