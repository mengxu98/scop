% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunDorothea.R
\name{DorotheaPlot}
\alias{DorotheaPlot}
\title{Plot differential DoRothEA TF activity}
\usage{
DorotheaPlot(
  srt,
  group.by,
  group1,
  group2,
  tool_name = "Dorothea",
  features = NULL,
  top_n = 30,
  test.use = c("wilcox.test", "t.test"),
  p.adjust.method = "BH",
  color.by = c("p_val", "p_val_adj"),
  rank.by = c("abs_logFC", "p_val", "p_val_adj", "logFC"),
  sort.by = c("logFC", "abs_logFC", "p_val", "p_val_adj"),
  p_floor = .Machine$double.xmin,
  bar_width = 0.85,
  cols = c("#2166AC", "white", "#B2182B"),
  title = NULL,
  xlab = NULL,
  ylab = "logFC",
  fill.title = NULL,
  angle = 90,
  hjust = 1,
  vjust = 0.5,
  theme_use = "theme_scop",
  theme_args = list(),
  return_data = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} object containing results from \code{\link[=RunDorothea]{RunDorothea()}}.}

\item{group.by}{Metadata column used to define groups.}

\item{group1, group2}{Two group labels to compare. Positive logFC means
higher TF activity in \code{group1}.}

\item{tool_name}{Name of the \code{srt@tools} entry created by \code{\link[=RunDorothea]{RunDorothea()}}.}

\item{features}{TFs to plot. If \code{NULL}, all TFs are tested and the top
\code{top_n} TFs are shown.}

\item{top_n}{Number of TFs to show when \code{features = NULL}. Set \code{NULL} to
show all tested TFs.}

\item{test.use}{Statistical test used for each TF.}

\item{p.adjust.method}{Method passed to \link[stats:p.adjust]{stats::p.adjust}.}

\item{color.by}{P-value column used for bar fill.}

\item{rank.by}{Metric used to select top TFs when \code{features = NULL}.}

\item{sort.by}{Metric used to order TFs on the x-axis.}

\item{p_floor}{Lower bound used before \code{-log10()} transformation.}

\item{bar_width}{Width of bars.}

\item{cols}{Color vector of length 3 for low, midpoint, and high values.}

\item{title, xlab, ylab, fill.title}{Axis, plot, and legend titles.}

\item{angle}{X-axis text angle.}

\item{hjust, vjust}{X-axis text justification.}

\item{theme_use}{Theme function used to style the plot.}

\item{theme_args}{Other arguments passed to \code{theme_use}.}

\item{return_data}{Whether to return a list with the plot and statistics.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object, or a list with \code{plot} and \code{data} when
\code{return_data = TRUE}.
}
\description{
Compare DoRothEA transcription factor activity between two groups and draw
a signed bar plot. Bar height is the mean activity difference
\code{group1 - group2}; fill color is the signed \code{-log10(p)} or
\verb{-log10(adjusted p)}, where the sign follows the activity difference.
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub, verbose = FALSE)
pancreas_sub <- RunDorothea(
  pancreas_sub,
  layer = "counts",
  species = "Mus_musculus",
  method = "ulm",
  minsize = 5,
  new_assay = FALSE
)
groups <- unique(as.character(pancreas_sub$CellType))
DorotheaPlot(
  pancreas_sub,
  group.by = "CellType",
  group1 = groups[1],
  group2 = groups[2]
)
}
