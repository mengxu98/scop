% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BenchmarkPlot.R
\name{BenchmarkPlot}
\alias{BenchmarkPlot}
\title{Plot benchmark metrics}
\usage{
BenchmarkPlot(
  srt = NULL,
  data = NULL,
  features = NULL,
  metrics = NULL,
  tool_name = NULL,
  reduction = NULL,
  plot_type = c("auto", "feature", "boxplot", "bar", "funkyheatmap"),
  plot_boxplot = TRUE,
  boxplot_jitter = FALSE,
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{data}{Optional summary benchmark \code{data.frame} containing at least
\code{metric} and \code{value}, and optionally \code{method} and \code{workflow}.}

\item{features}{Metadata columns containing per-cell benchmark scores.
Default is \code{NULL}.}

\item{metrics}{One or more summary metric names to visualize. Default is
\code{NULL}, which uses all available summary metrics.}

\item{tool_name}{Tool entries created by benchmark-related workflows.
This can be a character vector. For per-cell metrics, benchmark columns are
resolved from tool entries that contain \code{colnames}; for summary metrics,
entries containing \code{summary} or \code{metrics$summary} are used.}

\item{reduction}{Dimensional reduction used for per-cell feature plots.
Default is \code{NULL}, which uses the reduction stored in \code{tool_name} when
available, otherwise \code{\link[=DefaultReduction]{DefaultReduction()}}.}

\item{plot_type}{Plot type. One of \code{"auto"}, \code{"feature"}, \code{"boxplot"},
\code{"bar"}, or \code{"funkyheatmap"}.}

\item{plot_boxplot}{Whether to add the summary boxplot when per-cell metrics
are shown. Default is \code{TRUE}.}

\item{boxplot_jitter}{Whether to overlay jittered points on the boxplot.
Default is \code{FALSE}.}

\item{combine}{Combine plots into a single \code{patchwork} object.
If \code{FALSE}, return a list of ggplot objects.}

\item{nrow}{Number of rows in the combined plot.
Default is \code{NULL}, which means determined automatically based on the number of plots.}

\item{ncol}{Number of columns in the combined plot.
Default is \code{NULL}, which means determined automatically based on the number of plots.}

\item{byrow}{Whether to arrange the plots by row in the combined plot.
Default is \code{TRUE}.}

\item{pt.size}{The size of the points in the plot.}

\item{pt.alpha}{The transparency of the data points.
Default is \code{1}.}

\item{palette}{Color palette name.
Available palettes can be found in \link[thisplot:show_palettes]{thisplot::show_palettes}.
Default is \code{"Chinese"}.}

\item{palcolor}{Custom colors used to create a color palette.
Default is \code{NULL}.}

\item{theme_use}{Theme used. Can be a character string or a theme function.
Default is \code{"theme_scop"}.}

\item{theme_args}{Other arguments passed to the \code{theme_use}.
Default is \code{list()}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{The message to print.}
}
\value{
A ggplot, patchwork plot, or \code{funkyheatmap} object depending on the selected
mode. If \code{combine = FALSE} in per-cell mode, a named list of plots is
returned.
}
\description{
Visualize benchmark results stored in a \code{Seurat} object or a summary
\code{data.frame}. Per-cell metrics such as LISI are shown as feature plots and/or
boxplots. Summary metrics such as integration or mapping benchmark scores are
shown as barplots or a \code{funkyheatmap}.
}
\examples{
metrics_df <- data.frame(
  method = c("Raw", "Raw", "Harmony", "Harmony"),
  metric = c("batch_ASW_mixing", "celltype_ASW", "batch_ASW_mixing", "celltype_ASW"),
  value = c(0.42, 0.71, 0.68, 0.66)
)
BenchmarkPlot(
  data = metrics_df,
  plot_type = "bar"
)

data("pbmcmultiome_sub", package = "scop")
pbmcmultiome_sub[["MethodA_batch_LISI"]] <-
  seq_len(ncol(pbmcmultiome_sub)) / ncol(pbmcmultiome_sub)
pbmcmultiome_sub[["MethodB_batch_LISI"]] <-
  rev(pbmcmultiome_sub[["MethodA_batch_LISI", drop = TRUE]])
BenchmarkPlot(
  pbmcmultiome_sub,
  features = c("MethodA_batch_LISI", "MethodB_batch_LISI"),
  plot_type = "boxplot"
)

}
