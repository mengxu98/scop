% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunESTIMATE.R
\name{RunESTIMATE}
\alias{RunESTIMATE}
\title{Run ESTIMATE tumor microenvironment scoring}
\usage{
RunESTIMATE(
  object = NULL,
  count_matrix = NULL,
  assay = NULL,
  layer = "data",
  bulk_assay = "counts",
  sample.by = NULL,
  group.by = NULL,
  aggregate_fun = c("mean", "sum"),
  platform = c("rnaseq", "affymetrix", "agilent", "illumina"),
  filter_common_genes = TRUE,
  min_sig_genes = 10,
  purity = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{Optional expression matrix, \code{SummarizedExperiment}, or \code{Seurat}
object.}

\item{count_matrix}{Optional expression matrix with genes in rows and samples
in columns. Used when \code{object} is not provided as a matrix.}

\item{assay}{Assay used for \code{Seurat} input.}

\item{layer}{Assay layer used for \code{Seurat} input.}

\item{bulk_assay}{Assay name used for \code{SummarizedExperiment} input.}

\item{sample.by}{Metadata column used to aggregate \code{Seurat} cells into
pseudo-bulk samples.}

\item{group.by}{Optional metadata column used for grouping pseudo-bulk
samples. If \code{sample.by} is not supplied, \code{group.by} is used as the aggregation
variable.}

\item{aggregate_fun}{Pseudo-bulk aggregation function for \code{Seurat} input.}

\item{platform}{Platform label stored in the result metadata.}

\item{filter_common_genes}{Whether to restrict the expression matrix to the
ESTIMATE common-gene universe before scoring.}

\item{min_sig_genes}{Minimum number of observed stromal and immune signature
genes required after filtering.}

\item{purity}{Whether to calculate ESTIMATE tumor purity. The purity formula
was calibrated in the original ESTIMATE work primarily for Affymetrix data;
for RNA-seq it is best interpreted cautiously.}

\item{verbose}{Whether to print progress messages.}
}
\value{
A result bundle for matrix input, the modified \code{SummarizedExperiment} for
\code{SummarizedExperiment} input, or the modified \code{Seurat} object for \code{Seurat}
input.
}
\description{
Compute ESTIMATE stromal, immune, combined ESTIMATE, and tumor-purity scores
from bulk or pseudo-bulk expression data. The implementation uses the
original stromal and immune ESTIMATE signatures with an in-package scoring
routine, so it does not require the external \code{estimate} package.
}
\references{
Yoshihara et al. (2013) \url{doi:10.1038/ncomms3612}.
Barbie et al. (2009) \url{doi:10.1038/nature08460}.
}
