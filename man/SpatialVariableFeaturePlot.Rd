% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialVariableFeatures.R
\name{SpatialVariableFeaturePlot}
\alias{SpatialVariableFeaturePlot}
\title{Plot spatial variable feature results}
\usage{
SpatialVariableFeaturePlot(
  object = NULL,
  plot_type = c("summary", "surface", "combined"),
  features = NULL,
  nfeatures = 10,
  score_col = "score",
  assay = NULL,
  layer = NULL,
  image = NULL,
  overlay_image = TRUE,
  image.alpha = 1,
  coord.cols = c("col", "row"),
  flip.y = TRUE,
  pt.size = NULL,
  pt.alpha = 0.9,
  stroke = 0.1,
  palette = "Spectral",
  palcolor = NULL,
  legend.position = "right",
  theme_use = "theme_scop",
  theme_args = list(),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  srt = NULL,
  image.scale = c("lowres", "hires")
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{plot_type}{Plot type: \code{"summary"}, \code{"surface"}, or \code{"combined"}.}

\item{features}{Features to plot. If \code{NULL}, top features from the stored
spatial variable feature result are used.}

\item{nfeatures}{Number of top features used when \code{features = NULL}.}

\item{score_col}{Result column used for the summary x-axis.}

\item{assay, layer}{Expression assay and layer for surfaces. When \code{NULL}, use
the saved analysis settings; explicit values override them.}

\item{image}{Image for surfaces. When \code{NULL}, use the saved analysis image.}

\item{overlay_image, image.alpha}{Draw the spatial image beneath spots.}

\item{coord.cols}{Metadata coordinate columns for surfaces. When omitted,
use the saved analysis columns.}

\item{flip.y}{Reverse the y axis for metadata or FOV coordinates without a
raster image. The default \code{FALSE} agrees with spatial networks and cell
boundaries. Raster-backed display coordinates already include their flip.}

\item{pt.size, pt.alpha}{Point size and transparency. \code{pt.size = NULL} scales
with \code{sqrt(n)} (minimum \code{0.3}). Rasterized points keep at least a two-pixel
radius at \code{raster.dpi = c(512, 512)} and scale with \code{raster.dpi}.}

\item{stroke}{Point border width.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{legend.position}{Legend position for surface plots.}

\item{theme_use}{Theme name or function.}

\item{theme_args}{Theme name or function, plus extra theme arguments.}

\item{combine, nrow, ncol, byrow}{Combine plots with \link[patchwork:patchwork-package]{patchwork::patchwork}. \code{combine = FALSE}
returns a list of ggplots.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}

\item{image.scale}{Image scale factor matching the raster stored in the
selected image. Use \code{"hires"} for a hires raster; do not modify Seurat
scale-factor slots.}
}
\value{
A \code{ggplot} or \code{patchwork} object.
}
\description{
Visualize normalized results produced by \code{\link[=RunSpatialVariableFeatures]{RunSpatialVariableFeatures()}}. The
summary view shows feature ranks and, when finite p- or q-values are stored,
significance. Results without permutation statistics are shown without a
significance size mapping or legend. The surface view reuses
\code{\link[=SpatialSpotPlot]{SpatialSpotPlot()}} to draw spatial expression for selected features. Unless
explicitly overridden, surfaces inherit the saved assay, layer, image and
coordinate columns, even if the object's default assay has changed.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- Seurat::NormalizeData(
  visium_human_pancreas_sub,
  assay = "Spatial",
  verbose = FALSE
)
spatial <- RunSpatialVariableFeatures(
  object = spatial,
  assay = "Spatial",
  nfeatures = 10,
  verbose = FALSE
)
SpatialVariableFeaturePlot(object = spatial, plot_type = "summary")
}
