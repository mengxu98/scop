% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCoEmbedding.R
\name{RunCoEmbedding}
\alias{RunCoEmbedding}
\title{Co-embed reference and query cells}
\usage{
RunCoEmbedding(
  srt,
  reference,
  assay = NULL,
  reference_assay = NULL,
  reference_reduction = "pca",
  reference_dims = 1:30,
  gene_activity_assay = "ACTIVITY",
  weight_reduction = NULL,
  dims = 2:30,
  genes_use = NULL,
  imputed_assay = "RNA",
  modality_col = "modality",
  coembed_prefix = "CoEmbed",
  npcs = 30,
  umap_dims = 1:30,
  k.weight = 100,
  verbose = TRUE,
  seed = 11
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{reference}{RNA reference \code{Seurat} object used for label transfer.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{reference_assay}{Assay used in the reference object.}

\item{reference_reduction}{Reduction used in the reference object.}

\item{reference_dims}{Dimensions used from the reference reduction.}

\item{gene_activity_assay}{Name of the gene activity assay used for mapping.}

\item{weight_reduction}{Reduction in \code{srt} used to weight transferred labels.
If \code{NULL}, an ATAC linear reduction is resolved automatically from
\code{ATAC_default_linear_reduction}, \verb{\{prefix\}lsi}, \verb{\{prefix\}svd}, or the current
default reduction.}

\item{dims}{Query reduction dimensions used by \code{TransferData}.}

\item{genes_use}{Genes/features used for expression imputation and co-embedding.
If \code{NULL}, reference variable features are used.}

\item{imputed_assay}{Assay name for imputed RNA expression in ATAC cells.}

\item{modality_col}{Metadata column storing RNA/ATAC origin after merge.}

\item{coembed_prefix}{Prefix for co-embedding reductions.}

\item{npcs}{Number of PCs to compute.}

\item{umap_dims}{PC dimensions used for UMAP.}

\item{k.weight}{Number of neighbors used when weighting transfer anchors.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed}{Random seed used for UMAP.}
}
\value{
A merged \code{Seurat} object containing RNA reference and ATAC query cells.
}
\description{
Impute reference expression into query cells with \code{TransferData}, merge
reference and query cells, and compute PCA/UMAP on the shared expression
space. The current implementation mirrors the Seurat ATAC integration
vignette for ATAC query and RNA reference.
}
\examples{
\dontrun{
data("pbmcmultiome_sub", package = "scop")
pbmcmultiome_sub <- standard_scop(
  pbmcmultiome_sub,
  assay = "RNA",
  linear_reduction_dims = 20
)
reference <- subset(pbmcmultiome_sub, cells = colnames(pbmcmultiome_sub)[1:250])
query <- subset(pbmcmultiome_sub, cells = colnames(pbmcmultiome_sub)[251:350])
query <- standard_scop(
  query,
  assay = "peaks",
  normalization_method = "TFIDF",
  linear_reduction_dims = 20
)
coembed <- RunCoEmbedding(
  srt = query,
  reference = reference,
  assay = "peaks",
  reference_assay = "RNA",
  reference_reduction = "Standardpca",
  reference_dims = 1:10,
  dims = 2:10,
  umap_dims = 1:10
)
}
}
