% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataConvert.R
\name{h5ad_to_srt}
\alias{h5ad_to_srt}
\title{Read an \code{.h5ad} file and convert to a \code{Seurat}}
\usage{
h5ad_to_srt(path, verbose = TRUE, prepare_for_reticulate = TRUE)
}
\arguments{
\item{path}{Path to an \code{.h5ad} file (passed to \code{anndata.read_h5ad()}).}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{prepare_for_reticulate}{If \code{TRUE} (default), coerces \code{X} and each layer
matrix to CSR \code{float64} in Python (avoids invalid \code{dgRMatrix} conversion
via reticulate). Layers that still fail in \code{\link[=adata_to_srt]{adata_to_srt()}} are skipped and
reported. Set to \code{FALSE} for a plain \code{read_h5ad} then convert.}
}
\value{
A \code{Seurat} object.
}
\description{
Read an \code{.h5ad} file and convert to a \code{Seurat}
}
\examples{
\dontrun{
srt <- h5ad_to_srt("path/to/data.h5ad")
srt
}
}
\seealso{
\link{adata_to_srt}, \link{srt_to_adata}
}
