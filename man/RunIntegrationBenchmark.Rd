% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunIntegrationBenchmark.R
\name{RunIntegrationBenchmark}
\alias{RunIntegrationBenchmark}
\title{Benchmark single-cell integration methods}
\usage{
RunIntegrationBenchmark(
  srt,
  batch,
  celltype = NULL,
  methods = c("Uncorrected", "Harmony"),
  linear_reduction = "pca",
  nHVF = 2000,
  linear_reduction_dims = 50,
  linear_reduction_dims_use = NULL,
  nonlinear_reduction = "umap",
  perplexity = 30,
  k_graph = 15,
  bio_weight = 0.6,
  batch_weight = 0.4,
  skip_failed = TRUE,
  keep_objects = FALSE,
  print_table = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object containing a batch column.}

\item{batch}{Metadata column used as the technical batch.}

\item{celltype}{Optional metadata column used as biological labels.}

\item{methods}{Integration methods passed to \code{\link[=RunIntegration]{RunIntegration()}}.}

\item{perplexity}{Neighborhood size for \code{\link[=RunLISI]{RunLISI()}}.}

\item{k_graph}{Neighbor size for graph connectivity.}

\item{bio_weight, batch_weight}{Weights used for the overall score.
Defaults follow scIB (\code{0.6} bio conservation, \code{0.4} batch correction).}

\item{skip_failed}{Whether a failed method is recorded and skipped.}

\item{keep_objects}{Whether to keep a per-method \code{Seurat} object in the
result. The combined object used for plotting is always stored in \verb{$srt}.}

\item{print_table}{Whether to print the metric tables with
\code{\link[thisplot:print_colored_table]{thisplot::print_colored_table()}} when the result is printed.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{The message to print.}
}
\value{
An \code{integration_benchmark_result} list with \code{summary}, \code{metrics},
\code{runs}, and \code{srt}. Printing uses \code{\link[thisplot:print_colored_table]{thisplot::print_colored_table()}}.
}
\description{
Run one or more \code{\link[=RunIntegration]{RunIntegration()}} methods on the same input, score each
embedding with scIB-style batch-correction and bio-conservation metrics
(scaled iLISI/cLISI, ASW, graph connectivity, ARI/NMI), and return a
compact result for \code{\link[=IntegrationBenchmarkPlot]{IntegrationBenchmarkPlot()}}. Spatial-domain clustering
benchmarks stay in \code{\link[=RunBenchmark]{RunBenchmark()}}.
}
\examples{
\dontrun{
data(panc8_sub)
bench <- RunIntegrationBenchmark(
  panc8_sub,
  batch = "tech",
  celltype = "celltype",
  methods = c("Uncorrected", "Harmony"),
  nHVF = 500,
  linear_reduction_dims = 20,
  linear_reduction_dims_use = 1:10,
  perplexity = 10
)
bench
IntegrationBenchmarkPlot(bench)
}
}
\seealso{
\link{IntegrationBenchmarkPlot}, \link{RunIntegration}, \link{RunLISI}, \link{LISIPlot}
}
