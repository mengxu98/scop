% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCHOIR.R
\name{RunCHOIR}
\alias{RunCHOIR}
\title{Run CHOIR clustering}
\usage{
RunCHOIR(
  srt,
  assay = NULL,
  layer = NULL,
  key = "CHOIR",
  cluster_colname = "CHOIR_cluster",
  tool_name = "CHOIR",
  alpha = 0.05,
  p_adjust = c("bonferroni", "fdr", "none"),
  feature_set = c("var", "all"),
  exclude_features = NULL,
  n_iterations = 100,
  n_trees = 50,
  min_accuracy = 0.5,
  max_clusters = "auto",
  normalization_method = c("none", "SCTransform"),
  batch.by = NULL,
  batch_correction_method = NULL,
  reduction = NULL,
  var_features = NULL,
  atac = FALSE,
  n_cores = 1,
  seed = 1,
  store_tool = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay used by CHOIR. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used by CHOIR. If \code{NULL}, \code{"data"} is used for
\code{RNA} and \code{sketch} assays, and \code{"scale.data"} is used for \code{SCT} and
\code{integrated} assays. Other assays require an explicit layer.}

\item{key}{Name used by CHOIR to store its complete results in \code{srt@misc}.}

\item{cluster_colname}{Metadata column used for the final CHOIR clusters.}

\item{tool_name}{Name used to store the SCOP result summary in \code{srt@tools}.}

\item{alpha}{Significance level used for CHOIR permutation tests.}

\item{p_adjust}{Multiple-testing correction used by CHOIR.}

\item{feature_set}{Whether CHOIR random forests use variable or all features.}

\item{exclude_features}{Features excluded from CHOIR random forests.}

\item{n_iterations}{Number of bootstrap iterations for each permutation test.}

\item{n_trees}{Number of trees in each random forest.}

\item{min_accuracy}{Minimum classifier accuracy required to keep clusters
separate.}

\item{max_clusters}{Maximum extent of the initial clustering tree. The
upstream default \code{"auto"} is recommended.}

\item{normalization_method}{Normalization performed inside CHOIR. Use
\code{"none"} for previously normalized data or \code{"SCTransform"} with a counts
layer.}

\item{batch.by}{Optional metadata column containing batch labels.}

\item{batch_correction_method}{Batch correction performed by CHOIR. If
\code{NULL}, \code{"Harmony"} is selected when \code{batch.by} is supplied and \code{"none"}
otherwise.}

\item{reduction}{Optional existing dimensional reduction supplied to CHOIR.
This can be a reduction name in \code{srt} or a cell-by-dimension matrix.}

\item{var_features}{Features associated with \code{reduction}. If \code{NULL} and a
reduction is supplied, variable features from \code{assay} are used.}

\item{atac}{Whether the selected assay contains ATAC-seq data.}

\item{n_cores}{Number of cores used by CHOIR.}

\item{seed}{Random seed passed to CHOIR.}

\item{store_tool}{Whether to store a lightweight result summary in
\code{srt@tools[[tool_name]]}.}

\item{verbose}{Whether to print progress messages.}

\item{...}{Additional named arguments passed to the installed CHOIR entry
point. Unsupported arguments produce an error rather than being silently
ignored.}
}
\value{
A \code{Seurat} object containing CHOIR clusters in \code{cluster_colname},
complete upstream records in \code{srt@misc[[key]]}, and, when
\code{store_tool = TRUE}, a lightweight summary in \code{srt@tools[[tool_name]]}.
}
\description{
Runs the optional CHOIR backend on a single-modality \code{Seurat} object.
CHOIR builds and prunes a hierarchical clustering tree using random-forest
classifiers and permutation tests to identify statistically distinct cell
populations.

The upstream CHOIR records remain available in \code{srt@misc[[key]]}. This
wrapper also writes a stable cluster column to cell metadata and a lightweight
summary to \code{srt@tools[[tool_name]]}.
}
\examples{
\dontrun{
if (check_r("corceslab/CHOIR", verbose = FALSE)) {
  data(pancreas_sub)
  pancreas_sub <- Seurat::NormalizeData(pancreas_sub, verbose = FALSE)
  pancreas_sub <- RunCHOIR(
    pancreas_sub,
    assay = "RNA",
    n_cores = 2,
    verbose = FALSE
  )
  CellDimPlot(pancreas_sub, group.by = "CHOIR_cluster")
}
}
}
\references{
Sant, C. et al. CHOIR improves significance-based detection of cell types
and states from single-cell data. \emph{Nature Genetics} 57, 1309-1319
(2025). \doi{10.1038/s41588-025-02148-8}
}
