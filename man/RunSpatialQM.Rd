% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialQM.R
\name{RunSpatialQM}
\alias{RunSpatialQM}
\title{Run SpatialQM quality metrics}
\usage{
RunSpatialQM(
  srt,
  assay = NULL,
  layer = "counts",
  metrics = c("n_cells", "tx_per_cell", "sparsity", "entropy"),
  features = NULL,
  sample_id = NULL,
  platform = NULL,
  tool_name = "SpatialQM",
  store_results = TRUE,
  on_error = c("error", "warning"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay used for SpatialQM input. If \code{NULL}, the default assay is
used.}

\item{layer}{Assay layer used as counts for SpatialQM.}

\item{metrics}{SpatialQM metrics to run. Supported aliases include
\code{"n_cells"}, \code{"tx_per_cell"}, \code{"tx_per_area"}, \code{"tx_per_nuc"},
\code{"mean_expression"}, \code{"mean_signal_ratio"}, \code{"cell_tx_fraction"},
\code{"max_ratio"}, \code{"max_detection"}, \code{"mecr"}, \code{"morans"},
\code{"silhouette"}, \code{"sparsity"}, and \code{"entropy"}.}

\item{features}{Optional feature vector passed to metrics that support a
\code{features} argument.}

\item{sample_id, platform}{Optional values used to populate missing
\code{sample_id} and \code{platform} metadata columns expected by some SpatialQM
metrics.}

\item{tool_name}{Name used to store results in \code{srt@tools}.}

\item{store_results}{Whether to store results in \code{srt@tools}.}

\item{on_error}{Whether a failed metric should stop the wrapper (\code{"error"})
or be recorded in the result bundle with a warning (\code{"warning"}).}

\item{verbose}{Whether to print progress messages.}

\item{...}{Additional named arguments passed to SpatialQM metric functions
when the installed function exposes matching formal arguments.}
}
\value{
A \code{Seurat} object. When \code{store_results = TRUE}, results are stored in
\code{srt@tools[[tool_name]]}.
}
\description{
Run selected \code{SpatialQM} quality-control metrics on a spatial \code{Seurat}
object and store a compact, scop-style result bundle in \code{srt@tools}.
\code{SpatialQM} currently expects a \code{RNA} assay for object-first metrics, so this
wrapper maps the selected assay layer to a temporary \code{RNA} assay without
modifying the returned object. \code{SpatialQM} is an optional GitHub dependency
installable with \code{remotes::install_github("Center-for-Spatial-OMICs/SpatialQM")}.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- subset(
  visium_human_pancreas_sub,
  cells = colnames(visium_human_pancreas_sub)[1:80],
  features = rownames(visium_human_pancreas_sub)[1:300]
)

if (
  requireNamespace("SpatialQM", quietly = TRUE) &&
    identical(Sys.getenv("SCOP_RUN_SPATIAL_BACKEND_EXAMPLES"), "true")
) {
  spatial <- RunSpatialQM(
    spatial,
    assay = "Spatial",
    layer = "counts",
    metrics = c("n_cells", "tx_per_cell", "sparsity", "entropy"),
    platform = "Visium",
    verbose = FALSE
  )
  spatial@tools$SpatialQM$summary
}
}
