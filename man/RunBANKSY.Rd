% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunBANKSY.R
\name{RunBANKSY}
\alias{RunBANKSY}
\title{Run BANKSY spatial clustering}
\usage{
RunBANKSY(
  srt,
  assay = NULL,
  layer = "data",
  features = NULL,
  image = NULL,
  coord.cols = c("col", "row"),
  lambda = 0.2,
  k_geom = 15,
  M = 1,
  npcs = 20,
  use_agf = FALSE,
  algo = "leiden",
  k_neighbors = 50,
  resolution = 0.6,
  group = NULL,
  seed = 1,
  compute_banksy_params = list(),
  run_pca_params = list(),
  cluster_banksy_params = list(),
  cluster_source = NULL,
  cluster_colname = "BANKSY_cluster",
  tool_name = "BANKSY",
  store_results = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{layer}{Assay layer used as BANKSY input.}

\item{features}{Optional features to use. If \code{NULL}, all assay features are
used after zero-count filtering.}

\item{image}{Name of the Seurat spatial image used to recover spot
coordinates when they are not already present in metadata. For regular
Visium data with only pixel \code{x}/\code{y} coordinates, BayesSpace array
coordinates are inferred from the spatial grid.}

\item{coord.cols}{Metadata coordinate columns used when no image coordinate
source is available.}

\item{lambda}{BANKSY spatial weighting parameter.}

\item{k_geom}{Number of spatial neighbors used by BANKSY.}

\item{M}{Highest azimuthal Fourier harmonic passed to BANKSY.}

\item{npcs}{Number of principal components to compute.}

\item{use_agf}{Whether to use azimuthal Gabor filters.}

\item{algo}{Clustering algorithm passed to \code{Banksy::clusterBanksy()}.}

\item{k_neighbors}{Number of neighbors for graph clustering.}

\item{resolution}{Graph clustering resolution.}

\item{group}{Optional metadata column used by BANKSY for multi-sample
scaling. It is copied into the \code{SpatialExperiment} colData.}

\item{seed}{Optional seed for PCA and clustering.}

\item{compute_banksy_params}{Additional parameters passed to
\code{Banksy::computeBanksy()}.}

\item{run_pca_params}{Additional parameters passed to
\code{Banksy::runBanksyPCA()}.}

\item{cluster_banksy_params}{Additional parameters passed to
\code{Banksy::clusterBanksy()}.}

\item{cluster_source}{Optional BANKSY \code{colData} column to copy. If \code{NULL},
the first cluster name reported by \code{Banksy::clusterNames()} is used when
available.}

\item{cluster_colname}{Metadata column used for BANKSY clusters.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed BANKSY results in
\code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with BANKSY clusters in metadata. When
\code{store_results = TRUE}, detailed results are stored in
\code{srt@tools[[tool_name]]}.
}
\description{
Build neighborhood-augmented BANKSY features from a spatial \code{Seurat} object
and store spatial domain or microenvironment clusters in metadata.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
spatial$BANKSY_cluster <- factor(
  paste0("BANKSY", (seq_len(ncol(spatial)) - 1) \%\% 3 + 1)
)

SpatialSpotPlot(
  spatial,
  group.by = "BANKSY_cluster",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)

if (
  requireNamespace("Banksy", quietly = TRUE)
) {
spatial <- RunBANKSY(
  spatial,
  assay = "Spatial",
  layer = "counts",
  coord.cols = c("x", "y"),
  features = rownames(spatial)[1:300],
  lambda = 0.2,
  k_geom = 8,
  resolution = 0.6,
  verbose = FALSE
)
}
}
