% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunmcRigor.R
\name{RunmcRigor}
\alias{RunmcRigor}
\title{Run mcRigor metacell partition assessment}
\usage{
RunmcRigor(
  srt,
  cell_membership = NULL,
  metacell.by = NULL,
  mode = c("detect", "optimize"),
  tgamma = NULL,
  gamma_names = NULL,
  assay_type = c("RNA", "ATAC"),
  Gammas = NULL,
  aggregate_method = c("mean", "sum", "geom"),
  output_file = NULL,
  Nrep = 1,
  gene_filter = 0.1,
  feature_use = 2000,
  cor_method = c("pearson", "spearman"),
  prePro = TRUE,
  test_cutoff = 0.01,
  thre_smooth = TRUE,
  thre_bw = 1/6,
  D_bw = 10,
  optim_method = c("tradeoff", "dub_rate_large", "dub_rate_small"),
  weight = 0.5,
  dub_rate = 0.1,
  draw = FALSE,
  pur_metric = NULL,
  check_purity = TRUE,
  fields = NULL,
  step_save = FALSE,
  prefix = "mcRigor",
  tool_name = "mcRigor",
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} object containing the original single-cell data.}

\item{cell_membership}{A data frame or matrix with cells in rows and one or
more metacell partitions in columns. Row names should be cell names. If row
names are missing and the row count equals \code{ncol(srt)}, cells are matched in
\code{colnames(srt)} order.}

\item{metacell.by}{Metadata column(s) in \code{srt} used as metacell partitions
when \code{cell_membership = NULL}.}

\item{mode}{mcRigor task. \code{"detect"} calls \code{mcRigor_DETECT()} for one
partition; \code{"optimize"} calls \code{mcRigor_OPTIMIZE()} across candidate
partitions.}

\item{tgamma}{Target partition/gamma for \code{"detect"}. Can be a membership
column name or the numeric gamma label used by mcRigor. If \code{NULL}, the first
membership column is used.}

\item{gamma_names}{Optional gamma labels for membership columns. mcRigor
requires numeric-like column labels; non-numeric labels are mapped internally
to \code{1:ncol(cell_membership)} and recorded in the stored result.}

\item{assay_type}{Assay type passed to mcRigor.}

\item{Gammas}{Candidate gamma labels for \code{"optimize"}. Can use original
membership column names or mapped mcRigor gamma labels.}

\item{aggregate_method}{Metacell aggregation method passed to mcRigor.}

\item{output_file}{Optional path where mcRigor writes the \code{TabMC} RDS file.
If \code{NULL}, a temporary file is used to avoid creating files in the working
directory.}

\item{Nrep}{Number of permutation repetitions used by mcRigor.}

\item{gene_filter, feature_use, cor_method, prePro, test_cutoff, thre_smooth, thre_bw}{Parameters forwarded to mcRigor.}

\item{D_bw, optim_method, weight, dub_rate}{Optimization parameters forwarded
to \code{mcRigor_OPTIMIZE()}.}

\item{draw, pur_metric, check_purity, fields, step_save}{Plotting, purity, and
intermediate-save parameters forwarded to mcRigor.}

\item{prefix}{Prefix for metadata columns written to \code{srt}.}

\item{tool_name}{Name of the \code{srt@tools} entry used to store results.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with mcRigor metadata and a result list stored in
\code{srt@tools[[tool_name]]}.
}
\description{
Run mcRigor metacell partition assessment
}
\examples{
data(pancreas_sub)
set.seed(11)
pancreas_sub <- standard_scop(
  pancreas_sub,
  nHVF = 500,
  linear_reduction_dims = 20,
  linear_reduction_dims_use = 1:20,
  nonlinear_reduction_dims = 2,
  verbose = FALSE
)
mc <- RunMetaCell(
  pancreas_sub,
  method = "supercell",
  gamma = 25
)

membership <- data.frame(
  Metacell = mc@misc[["cell_membership"]],
  row.names = names(mc@misc[["cell_membership"]])
)

pancreas_sub <- RunmcRigor(
  mc@misc[["original_srt"]],
  cell_membership = membership,
  Nrep = 1,
  feature_use = 100,
  draw = FALSE
)

table(pancreas_sub$mcRigor_status)

CellDimPlot(
  pancreas_sub,
  group.by = "mcRigor_metacell"
)

CellDimPlot(
  pancreas_sub,
  group.by = "mcRigor_status"
)
}
\references{
Liu, P. and Li, J.J. (2024). mcRigor: a statistical method to enhance the
rigor of metacell partitioning in single-cell data analysis.
\emph{bioRxiv}. \doi{10.1101/2024.10.30.621093}
}
