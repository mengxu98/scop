% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunRareQ.R
\name{RunRareQ}
\alias{RunRareQ}
\title{RareQ rare-cell population detection}
\usage{
RunRareQ(
  srt,
  assay = NULL,
  reduction = "pca",
  dims = 1:30,
  k.param = 20,
  k = 6,
  Q_cut = 0.6,
  ratio = 0.2,
  max_iter = 100,
  run_neighbors = TRUE,
  force_recalc = FALSE,
  neighbor_name = NULL,
  find_neighbors_params = list(),
  rare_threshold = 0.01,
  prefix = "RareQ",
  cluster_colname = paste0(prefix, "_cluster"),
  q_colname = paste0(prefix, "_Q"),
  size_colname = paste0(prefix, "_cluster_size"),
  rare_colname = paste0(prefix, "_is_rare"),
  tool_name = "RareQ",
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{reduction}{Reduction used to build nearest neighbors when the required
\verb{\{assay\}.nn} neighbor slot is absent or \code{force_recalc = TRUE}. If \code{NULL},
\code{\link[=DefaultReduction]{DefaultReduction()}} is used.}

\item{dims}{Dimensions from \code{reduction} used for nearest-neighbor search.}

\item{k.param}{Number of nearest neighbors to compute with
\code{\link[Seurat:FindNeighbors]{Seurat::FindNeighbors()}} when neighbor search is needed.}

\item{k}{Number of nearest neighbors used by RareQ to compute Q values.}

\item{Q_cut}{Q-value threshold passed to \code{RareQ::FindRare()}.}

\item{ratio}{Merge-ratio threshold passed to \code{RareQ::FindRare()}.}

\item{max_iter}{Maximum number of RareQ propagation iterations.}

\item{run_neighbors}{Whether to build the required Seurat neighbor slot if
it is missing.}

\item{force_recalc}{Whether to rebuild the Seurat neighbor slot before
running RareQ.}

\item{neighbor_name}{Name of the Seurat \code{Neighbor} object to reuse or create.
If \code{NULL}, defaults to \verb{\{assay\}.nn}, which is the neighbor slot required by
\code{RareQ::ComputeQ()} and \code{RareQ::FindRare()}. A non-default neighbor is copied
to \verb{\{assay\}.nn} before running RareQ because RareQ reads that slot directly.}

\item{find_neighbors_params}{Additional named parameters passed to
\code{\link[Seurat:FindNeighbors]{Seurat::FindNeighbors()}} when neighbor search is run.}

\item{rare_threshold}{Cluster-size threshold used to mark rare clusters. A
value smaller than 1 is treated as a fraction of cells; a value of 1 or
larger is treated as a cell count. Set to \code{NULL} to skip rare flags.}

\item{prefix}{Prefix used for metadata columns.}

\item{cluster_colname, q_colname, size_colname, rare_colname}{Metadata column
names for RareQ clusters, Q values, cluster sizes, and rare-cluster flags.}

\item{tool_name}{Name of the \code{srt@tools} entry.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with RareQ results in metadata and
\code{srt@tools[[tool_name]]}.
}
\description{
RareQ rare-cell population detection
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(
  pancreas_sub,
  verbose = FALSE
)
pancreas_sub <- RunRareQ(
  pancreas_sub,
  dims = 1:20
)

CellDimPlot(
  pancreas_sub,
  group.by = "RareQ_cluster"
)

CellDimPlot(
  pancreas_sub,
  group.by = "RareQ_is_rare"
)

FeatureDimPlot(
  pancreas_sub,
  features = "RareQ_Q"
)
}
\references{
Fa, B. et al. Cell neighborhood topology directs rare cell population
identification. \emph{Nature Communications} (2026).
\doi{10.1038/s41467-026-71180-x}
}
