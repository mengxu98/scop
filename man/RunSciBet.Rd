% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSciBet.R
\name{RunSciBet}
\alias{RunSciBet}
\title{Annotate single cells with a C++ SciBet-compatible classifier}
\usage{
RunSciBet(
  srt_query,
  srt_ref,
  ref_group,
  query_group = NULL,
  query_assay = NULL,
  ref_assay = NULL,
  query_layer = "counts",
  ref_layer = "counts",
  features = NULL,
  nfeatures = 1000,
  additional_features_per_class = 0,
  input_transform = c("auto", "none", "expm1"),
  prefix = "scibet",
  store_model = TRUE,
  store_probabilities = FALSE,
  return_object = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt_query}{An object of class Seurat to be annotated with cell types.}

\item{srt_ref}{An object of class Seurat storing the reference cells.}

\item{ref_group}{Column name in the \code{srt_ref} metadata that represents the cell grouping.}

\item{query_group}{Column name in the \code{srt_query} metadata that represents the cell grouping.}

\item{query_assay}{Assay to be used for the query data.
Default is the default assay of the \code{srt_query} object.}

\item{ref_assay}{Assay to be used for the reference data.
Default is the default assay of the \code{srt_ref} object.}

\item{query_layer, ref_layer}{Assay layers used for query and reference.}

\item{features}{Candidate features used by SciBet. If \code{NULL}, common genes
between query and reference are used.}

\item{nfeatures}{Number of entropy-test features selected from \code{features}.}

\item{additional_features_per_class}{Additional high-expression features
selected per reference class.}

\item{input_transform}{How to transform extracted values before SciBet's
internal \code{log1p} step. \code{"auto"} applies \code{expm1} to \code{"data"} layers and no
transform otherwise.}

\item{prefix}{Prefix for metadata columns.}

\item{store_model}{Whether to store the SciBet core and probabilities in
\code{srt_query@tools}.}

\item{store_probabilities}{Whether to store the full cell-by-class
probability matrix. The default keeps annotations and maximum probability
scores while avoiding a large result object on full-scale data.}

\item{return_object}{Whether to return the annotated \code{Seurat} query object.
If \code{FALSE}, return a lightweight list with annotations, scores,
probabilities, and model components without copying \code{srt_query}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with SciBet annotations in metadata and results in
\code{srt_query@tools[["SciBet"]]}.
}
\description{
Run a C++ implementation of the core SciBet classifier using a
labeled reference \code{Seurat} object.
}
\examples{
data(panc8_sub)
keep <- panc8_sub$celltype \%in\% c("ductal", "alpha", "beta")
panc8_sub <- Seurat::NormalizeData(panc8_sub[, keep], verbose = FALSE)
reference <- panc8_sub[, panc8_sub$tech != "fluidigmc1"]
query <- panc8_sub[, panc8_sub$tech == "fluidigmc1"]
query <- RunSciBet(
  srt_query = query, srt_ref = reference,
  ref_group = "celltype", nfeatures = 200, verbose = FALSE
)
query <- RunStandardWorkflow(query, verbose = FALSE, linear_reduction_dims = 10)
CellDimPlot(
  query, group.by = "scibet_annotation",
  label = TRUE, legend.position = "bottom"
)
}
