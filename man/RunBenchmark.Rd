% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunBenchmark.R
\name{RunBenchmark}
\alias{RunBenchmark}
\title{Benchmark spatial domain clustering methods}
\usage{
RunBenchmark(
  srt,
  gold_standard,
  methods = NULL,
  method_params = list(),
  n_clusters = NULL,
  metrics = c("ARI", "NMI"),
  keep_objects = FALSE,
  install_missing = FALSE,
  seed = 1,
  timeout = Inf,
  poll_interval = 0.1,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A spatial \code{Seurat} object.}

\item{gold_standard}{Either one metadata column in \code{srt} or a named vector
whose names match the spot names in \code{srt}.}

\item{methods}{Spatial domain methods to benchmark. \code{NULL} uses every stable
registered domain producer. Use \code{"all"} to additionally include supported
legacy baselines such as GiottoCluster, or select them explicitly. Method
names may be written with or without the \code{Run} prefix.}

\item{method_params}{Named list of per-method argument lists. Arguments are
passed to the corresponding SCOP producer, never directly to its backend.}

\item{n_clusters}{Optional common number of domains. When \code{NULL}, methods
that require a cluster count use the number of non-missing gold-standard
classes. A method-specific \code{q} or \code{n_clusters} in \code{method_params} wins.}

\item{metrics}{Quality metrics selected by default when plotting. Supported
values are \code{"ARI"}, \code{"NMI"}, and \code{"purity"}. All three are retained in
the result summary.}

\item{keep_objects}{Whether to keep each successful method's full producer
result, including standalone legacy results when explicitly selected. The
default keeps only aligned labels and compact run metadata.}

\item{install_missing}{Whether missing optional backends may enter their
producer's normal \code{check_r()} installation path. The default records them as
unavailable without changing the R library.}

\item{seed}{Seed set inside each isolated method process and forwarded to
producers with a public \code{seed} argument unless overridden in
\code{method_params}.}

\item{timeout}{Maximum wall time in seconds for each isolated run, including
process start and result serialization. \code{Inf} disables the timeout.}

\item{poll_interval}{Seconds between process-tree memory samples.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{scop_benchmark} object. Use \code{as.data.frame()} for its summary,
\code{plot()} or \code{\link[=BenchmarkPlot]{BenchmarkPlot()}} for visualization, and \verb{$predictions} for the
aligned labels.
}
\description{
Run registered spatial domain clustering methods from the same immutable
input, compare their aligned labels with a gold standard, and record
classification quality, elapsed time, and sampled peak process-tree memory.
Each method runs in an isolated R process so one failed backend does not
corrupt the input or prevent the remaining methods from being assessed.
}
\examples{
\dontrun{
data(visium_human_pancreas_sub)
visium_human_pancreas_sub$gold_domain <- factor(
  paste0("domain_", (seq_len(ncol(visium_human_pancreas_sub)) - 1) \%\% 3 + 1)
)
bench <- RunBenchmark(
  visium_human_pancreas_sub,
  gold_standard = "gold_domain",
  method_params = list(
    BayesSpace = list(n.PCs = 5, n.HVGs = 200),
    BANKSY = list(layer = "counts"),
    SmoothClust = list(layer = "counts", min_spots = 1)
  )
)
bench
BenchmarkPlot(data = bench)
}
}
\concept{spatial-producer}
