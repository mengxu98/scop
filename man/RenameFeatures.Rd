% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rename.R
\name{RenameFeatures}
\alias{RenameFeatures}
\title{Rename features for the Seurat object}
\usage{
RenameFeatures(
  object,
  newnames = NULL,
  assays = NULL,
  verbose = TRUE,
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{newnames}{A vector with the same length of features in Seurat object,
or characters named with old features.}

\item{assays}{Assays to rename.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\description{
Rename features for the Seurat object
}
\examples{
data(panc8_sub)
head(rownames(panc8_sub))
# Simply convert genes from human to mouse and preprocess the data
genenames <- make.unique(
  thisutils::capitalize(rownames(panc8_sub),
    force_tolower = TRUE
  )
)
names(genenames) <- rownames(panc8_sub)
panc8_rename <- RenameFeatures(
  panc8_sub,
  newnames = genenames
)
head(rownames(panc8_rename))
}
