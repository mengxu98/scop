% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialIntegration.R
\name{SpatialIntegrationPlot}
\alias{SpatialIntegrationPlot}
\title{Plot spatial integration results}
\usage{
SpatialIntegrationPlot(
  object,
  method = NULL,
  plot_type = c("spatial", "embedding", "alignment", "composition"),
  group.by = NULL,
  sample.by = NULL,
  reduction = NULL,
  cluster_colname = NULL,
  coord.cols = c("col", "row"),
  use_aligned = FALSE,
  tool_name = "SpatialIntegration",
  combine = TRUE,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  ...,
  image.scale = c("lowres", "hires"),
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object containing spatial integration results.}

\item{method}{Stored integration method. If \code{NULL}, the active method stored
in \code{srt@tools[[tool_name]]} is used.}

\item{plot_type}{Plot type: \code{"spatial"}, \code{"embedding"}, \code{"alignment"}, or
\code{"composition"}.}

\item{group.by}{Metadata column used for coloring. Defaults to the stored
spatial domain column.}

\item{sample.by}{Metadata column used for facets or composition grouping.
Defaults to the stored sample column.}

\item{reduction}{Reduction used for embedding plots. Defaults to the stored
integration reduction.}

\item{cluster_colname}{Backward-compatible alias for \code{group.by}.}

\item{coord.cols}{Metadata coordinate columns used when no image is available.}

\item{use_aligned}{Whether spatial plots should use aligned coordinates when
available.}

\item{tool_name}{Name of the \code{srt@tools} entry created by
\code{\link[=RunSpatialIntegration]{RunSpatialIntegration()}}.}

\item{combine}{Whether to combine plots when delegated plotting returns a
list.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{theme_use}{Theme name or function.}

\item{theme_args}{Theme name or function, plus extra theme arguments.}

\item{...}{Additional arguments passed to \code{\link[=SpatialSpotPlot]{SpatialSpotPlot()}} or
\code{\link[=CellDimPlot]{CellDimPlot()}}. For spatial maps, \code{image} can be one image or a named
sample-to-image vector using names in the merged object. By default, each
sample uses its uniquely matching image. \code{nrow}, \code{ncol} and \code{byrow} control
the layout of separate image panels.}

\item{image.scale}{Image scale factor matching the raster stored in the
selected image. Use \code{"hires"} for a hires raster; do not modify Seurat
scale-factor slots.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\value{
A \code{ggplot}, patchwork object, or list of plots.
}
\description{
Visualize standardized results produced by \code{\link[=RunSpatialIntegration]{RunSpatialIntegration()}}.
}
\details{
Raw spatial maps retain every sample and use consistent category colors.
Aligned maps use the stored aligned coordinates without a raw tissue-image
overlay. This plotting option does not perform image registration.
}
\examples{
\dontrun{
# Template: visium_S1 and visium_S2 are existing spatial Seurat samples.
spatial <- RunSpatialIntegration(
  list(S1 = visium_S1, S2 = visium_S2),
  method = "PRECAST",
  assay = "Spatial",
  image = c(S1 = "slice1", S2 = "slice1")
)
SpatialIntegrationPlot(spatial, method = "PRECAST", plot_type = "spatial")
}
}
\seealso{
\code{\link[=RunSpatialIntegration]{RunSpatialIntegration()}}
}
