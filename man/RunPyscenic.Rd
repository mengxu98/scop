% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunPyscenic.R
\name{RunPyscenic}
\alias{RunPyscenic}
\title{Run pySCENIC gene regulatory network analysis}
\usage{
RunPyscenic(
  srt,
  assay = NULL,
  layer = "counts",
  ranking_dbs,
  motif_annotations,
  tf_list,
  work_dir,
  prefix = "pyscenic",
  metacell = TRUE,
  metacell.by = NULL,
  metacell_resolution = NULL,
  metacell_target = NULL,
  metacell_resolution_candidates = c(0.5, 1, 2, 5, 10, 20, 30, 40, 50, 75, 100),
  metacell_reduction = "pca",
  metacell_dims = 1:30,
  min_expr_cells = 3,
  min_regulon_size = 10,
  cores = 1,
  aucell_batch_size = 500,
  seed = 1234,
  force = FALSE,
  assay_name = "pyscenic",
  tool_name = "Pyscenic",
  return_seurat = TRUE,
  envname = NULL,
  conda = "auto",
  prepare_env = TRUE,
  progress = verbose,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{layer}{Assay layer used as the count matrix.}

\item{ranking_dbs}{Character vector of cisTarget ranking feather files.}

\item{motif_annotations}{Motif annotation table used by \verb{pyscenic ctx}.}

\item{tf_list}{Transcription-factor list used by GRNBoost2.}

\item{work_dir}{Directory used for pySCENIC input and output files.}

\item{prefix}{Prefix for pySCENIC output files.}

\item{metacell}{Whether to build a metacell count matrix for GRNBoost2.}

\item{metacell.by}{Optional metadata column(s) used to keep metacells within
groups such as samples or cell types.}

\item{metacell_resolution}{Resolution passed to \code{\link[Seurat:FindClusters]{Seurat::FindClusters()}} for
overclustering. If \code{NULL}, candidate resolutions are scanned and the one
closest to \code{metacell_target} is used.}

\item{metacell_target}{Target number of metacells used when
\code{metacell_resolution = NULL}. If \code{NULL}, a default target is chosen from the
number of cells.}

\item{metacell_resolution_candidates}{Candidate resolutions scanned when
\code{metacell_resolution = NULL}.}

\item{metacell_reduction}{Reduction used to build the metacell neighbor graph.
The default \code{"pca"} keeps the original behavior and recomputes PCA from the
selected assay. To use an already batch-corrected embedding such as Harmony,
run it before \code{RunPyscenic()} and set \code{metacell_reduction = "Harmony"}.
Only the metacell grouping uses this reduction; GRNBoost2 still uses raw
count sums per metacell.}

\item{metacell_dims}{Dimensions used for metacell overclustering.}

\item{min_expr_cells}{Minimum number of cells or metacells where a gene must
be detected before GRNBoost2.}

\item{min_regulon_size}{Minimum regulon size kept after \verb{pyscenic ctx}.}

\item{cores}{Number of workers used by GRNBoost2, \verb{pyscenic ctx}, and
AUCell batch scoring. If multicore execution is not supported, this is
automatically reduced to one core.}

\item{aucell_batch_size}{Number of cells scored in each AUCell batch.}

\item{seed}{Random seed used by GRNBoost2 and Seurat overclustering.}

\item{force}{Whether to rebuild existing pySCENIC outputs.}

\item{assay_name}{Name of the assay used to store regulon activity scores.}

\item{tool_name}{Name of the \code{srt@tools} entry.}

\item{return_seurat}{Whether to return the modified Seurat object. If
\code{FALSE}, a result list is returned.}

\item{envname}{Python environment used for pySCENIC. If \code{NULL}, the isolated
\code{"pyscenic_env"} environment is used.}

\item{conda}{The path or command name of a conda-compatible executable.}

\item{prepare_env}{Whether to prepare and configure the pySCENIC Python
environment before running.}

\item{progress}{Whether to show a stage-level progress bar.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat object with pySCENIC results, or a result list when
\code{return_seurat = FALSE}.
}
\description{
Run pySCENIC from a Seurat object. The GRN and cisTarget steps use a
metacell count matrix by default, while AUCell scores are calculated on the
original single-cell count matrix.
}
\examples{
\dontrun{
srt <- RunPyscenic(
  srt,
  ranking_dbs = c(
    "/path/to/hg38_500bp_up_100bp_down.genes_vs_motifs.rankings.feather",
    "/path/to/hg38_10kbp_up_10kbp_down.genes_vs_motifs.rankings.feather"
  ),
  motif_annotations = "/path/to/motifs-v10nr_clust-nr.hgnc-m0.001-o0.0.tbl",
  tf_list = "/path/to/hsa_hgnc_tfs.motifs-v10.txt",
  work_dir = "./pyscenic",
  cores = 8
)
}
}
