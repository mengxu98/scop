% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunscMalignantFinder.R
\name{RunscMalignantStates}
\alias{RunscMalignantStates}
\title{Run scMalignantFinder cancer cell state scoring}
\usage{
RunscMalignantStates(
  srt = NULL,
  adata = NULL,
  h5ad = NULL,
  assay = "RNA",
  layer = "data",
  cells = NULL,
  gene_sets,
  norm_type = FALSE,
  prefix = "scMalignantState_",
  return_seurat = !is.null(srt),
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{adata}{Optional Python AnnData object.}

\item{h5ad}{Optional path to an \code{.h5ad} file.}

\item{assay}{Assay used when \code{srt} is supplied. Default is \code{"RNA"}.}

\item{layer}{Layer used when \code{srt} is supplied. Default is \code{"data"}.}

\item{cells}{Optional cells to run. If supplied with \code{srt}, results are
appended to these cells and other cells receive \code{NA}.}

\item{gene_sets}{Path to a \code{.gmt} file containing cancer cell state gene
sets, such as \code{Malignant_MPs.Gavish_2023.gmt} from the scMalignantFinder
resources.}

\item{norm_type}{Passed to \code{scMalignantFinder}. Use \code{TRUE} for raw counts
that should be library-size normalized; use \code{FALSE} for already normalized
input. Default is \code{FALSE}.}

\item{prefix}{Optional prefix for output metadata columns. Default preserves
the original \code{scMalignantFinder} column names.}

\item{return_seurat}{Whether to return a Seurat object when \code{srt} is
supplied. If \code{FALSE}, returns a data frame of predictions.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat object with cancer-state AUCell scores, or a data frame when
\code{return_seurat = FALSE}.
}
\description{
Score cancer cell state gene sets with \code{scMalignantFinder} AUCell utilities
and append the resulting activity scores to Seurat metadata.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
srt <- RunscMalignantStates(
  srt,
  gene_sets = "path/to/Malignant_MPs.Gavish_2023.gmt"
)
\dontshow{\}) # examplesIf}
}
