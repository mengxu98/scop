% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LISI.R
\name{LISIPlot}
\alias{LISIPlot}
\title{Plot LISI scores}
\usage{
LISIPlot(
  srt,
  features = NULL,
  tool_name = NULL,
  reduction = NULL,
  plot_boxplot = TRUE,
  boxplot_jitter = FALSE,
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{features}{Metadata columns containing LISI scores.
Default is \code{NULL}, which will use columns stored in \code{tool_name}, or all
metadata columns ending with \code{"_LISI"} when \code{tool_name} is \code{NULL}.}

\item{tool_name}{Tool entry created by \code{\link[=RunLISI]{RunLISI()}}. Default is \code{NULL}.}

\item{reduction}{Dimensional reduction used for feature plots.
If \code{NULL}, the reduction recorded in \code{tool_name} is used when available;
otherwise \code{\link[=DefaultReduction]{DefaultReduction()}} is used.}

\item{plot_boxplot}{Whether to add boxplots. Default is \code{TRUE}.}

\item{boxplot_jitter}{Whether to overlay jittered points on boxplots.
Default is \code{FALSE}.}

\item{combine}{Combine plots into a single \code{patchwork} object.
If \code{FALSE}, return a list of ggplot objects.}

\item{nrow}{Number of rows in the combined plot.
Default is \code{NULL}, which means determined automatically based on the number of plots.}

\item{ncol}{Number of columns in the combined plot.
Default is \code{NULL}, which means determined automatically based on the number of plots.}

\item{byrow}{Whether to arrange the plots by row in the combined plot.
Default is \code{TRUE}.}

\item{pt.size}{The size of the points in the plot.}

\item{pt.alpha}{The transparency of the data points.
Default is \code{1}.}

\item{palette}{Color palette name.
Available palettes can be found in \link[thisplot:show_palettes]{thisplot::show_palettes}.
Default is \code{"Chinese"}.}

\item{palcolor}{Custom colors used to create a color palette.
Default is \code{NULL}.}

\item{theme_use}{Theme used. Can be a character string or a theme function.
Default is \code{"theme_scop"}.}

\item{theme_args}{Other arguments passed to the \code{theme_use}.
Default is \code{list()}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{The message to print.}
}
\value{
If \code{combine = TRUE}, returns a combined \code{patchwork} plot.
If \code{combine = FALSE}, returns a named list of ggplot objects.
}
\description{
Backward-compatible wrapper around \code{\link[=BenchmarkPlot]{BenchmarkPlot()}} for LISI scores.
Visualize LISI scores on a dimensional reduction and compare methods with a
summary boxplot.
}
\seealso{
\link{RunLISI}, \link{FeatureDimPlot}
}
