% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunBulk.R
\name{RunBulk}
\alias{RunBulk}
\title{Unified bulk analysis entrypoint}
\usage{
RunBulk(
  srt = NULL,
  bulk_se = NULL,
  method = "de_edgeR_qlf",
  sample.by = NULL,
  condition.by = NULL,
  group.by = NULL,
  ref_srt = NULL,
  celltype.by = NULL,
  assay = NULL,
  layer = "counts",
  bulk_assay = "counts",
  ref_assay = NULL,
  ref_layer = "counts",
  condition1 = NULL,
  condition2 = NULL,
  de_markers_type = "single",
  markers_type = NULL,
  run_enrichment = FALSE,
  run_gsea = FALSE,
  enrichment_args = list(),
  gsea_args = list(),
  de_args = list(),
  deconv_args = list(),
  csde_args = list(),
  method_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object used for pseudobulk mode.}

\item{bulk_se}{A \code{SummarizedExperiment} object used for true bulk mode.}

\item{method}{Character vector specifying methods to run.
Supported values are \code{"de_limma_voom"}, \code{"de_edgeR_qlf"}, \code{"de_DESeq2"},
\code{"de_dream"}, \code{"deconv_MuSiC"}, \code{"deconv_BisqueRNA"},
\code{"deconv_BayesPrism"}, and \code{"csde_TOAST"}.}

\item{sample.by}{Metadata column containing sample IDs in pseudobulk mode.}

\item{condition.by}{Metadata column containing condition labels.
Required when \verb{.arg method} includes \code{"de"} or \code{"csde"}.
Optional for deconvolution-only runs.}

\item{group.by}{Optional metadata column used to stratify DE by subgroup.}

\item{ref_srt}{Optional \code{Seurat} reference for deconvolution/CSDE.
If omitted in pseudobulk mode, \code{srt} is used as reference.}

\item{celltype.by}{Metadata column in \code{ref_srt} defining reference cell types.
Required when deconvolution or CSDE is requested.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{bulk_assay}{Assay name in \code{bulk_se} used as counts matrix.}

\item{ref_assay}{Assay name in \code{ref_srt} for reference profiles.}

\item{ref_layer}{Layer name in \code{ref_srt} for reference counts.}

\item{condition1}{First condition label for pairwise comparison.}

\item{condition2}{Second condition label for pairwise comparison.}

\item{de_markers_type}{DE comparison mode.
One of \code{"single"} (one pairwise comparison),
\code{"all"} (each condition vs the rest),
or \code{"paired"} (all pairwise condition comparisons).}

\item{markers_type}{Alias of \verb{.arg de_markers_type} for consistency with \link{RunDEtest}.
If provided, it overrides \verb{.arg de_markers_type}.}

\item{run_enrichment}{Whether to run \link{RunEnrichment} from bulk DE results.}

\item{run_gsea}{Whether to run \link{RunGSEA} from bulk DE results.}

\item{enrichment_args}{Named list of additional parameters for \link{RunEnrichment}.}

\item{gsea_args}{Named list of additional parameters for \link{RunGSEA}.}

\item{de_args}{Named list of additional parameters for DE method functions.}

\item{deconv_args}{Named list of additional parameters for deconvolution method functions.
Current deconvolution wrappers use \code{backend = "internal"} by default and
record the computational engine in the result bundle.}

\item{csde_args}{Named list of additional parameters for CSDE method functions.
Current \code{csde_TOAST} uses \code{backend = "limma_interaction"} by default and
records the computational engine in the result bundle.}

\item{method_args}{Named list of method-specific parameters. Names should be
RunBulk method names, such as \code{"de_edgeR_qlf"} or \code{"csde_TOAST"}.
Method-specific values override \verb{.arg de_args}, \verb{.arg deconv_args},
\verb{.arg csde_args}, and \verb{.arg ...}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters forwarded to method functions.}
}
\value{
Returns a \code{Seurat} object in pseudobulk mode with results in
\code{srt@tools[["Bulk"]]}. If \verb{.arg run_enrichment} or \verb{.arg run_gsea} is
\code{TRUE}, successful pathway results are also stored in the standard
\code{srt@tools[["Enrichment_Bulk_wilcox"]]} and
\code{srt@tools[["GSEA_Bulk_wilcox"]]} slots used by \link{EnrichmentPlot} and
\link{GSEAPlot}. True bulk mode returns a \code{SummarizedExperiment} object
with results in \code{metadata(bulk_se)[["Bulk"]]}.
}
\description{
\code{RunBulk()} provides one bulk-strategy entrypoint for Seurat-centered
workflows. Methods are selected with one character vector, so users do not
need to choose a separate module argument.
}
\examples{
# ----------------------------
# Example 1: Seurat -> pseudobulk (DE) using scop built-in dataset
# ----------------------------
if (requireNamespace("edgeR", quietly = TRUE)) {
  data("pancreas_sub", package = "scop")
  srt <- pancreas_sub

  # Build sample/condition labels for pseudobulk aggregation.
  srt$sample_id <- paste0(
    "sample_",
    (seq_len(ncol(srt)) - 1L) \%\% 8L + 1L
  )
  srt$condition <- ifelse(
    srt$sample_id \%in\% paste0("sample_", 1:4),
    "A",
    "B"
  )

  srt <- RunBulk(
    srt = srt,
    method = "de_edgeR_qlf",
    sample.by = "sample_id",
    condition.by = "condition",
    verbose = FALSE
  )
  srt@tools$Bulk$status$de$status
}

# ----------------------------
# Example 2: pure bulk (SummarizedExperiment -> DE)
# ----------------------------
if (requireNamespace("edgeR", quietly = TRUE) &&
  requireNamespace("SummarizedExperiment", quietly = TRUE) &&
  requireNamespace("S4Vectors", quietly = TRUE)) {
  data("pancreas_sub", package = "scop")
  srt <- pancreas_sub
  srt$sample_id <- paste0(
    "sample_",
    (seq_len(ncol(srt)) - 1L) \%\% 8L + 1L
  )
  srt$condition <- ifelse(
    srt$sample_id \%in\% paste0("sample_", 1:4),
    "A",
    "B"
  )

  counts <- GetAssayData5(srt, layer = "counts")
  sid <- as.character(srt$sample_id)
  sid_levels <- unique(sid)
  bulk_counts <- do.call(cbind, lapply(sid_levels, function(x) {
    Matrix::rowSums(counts[, sid == x, drop = FALSE])
  }))
  colnames(bulk_counts) <- sid_levels
  rownames(bulk_counts) <- rownames(counts)

  sample_condition <- tapply(
    as.character(srt$condition),
    sid,
    function(x) unique(x)[1]
  )
  bulk_se <- SummarizedExperiment::SummarizedExperiment(
    assays = list(counts = as.matrix(bulk_counts)),
    colData = S4Vectors::DataFrame(
      condition = as.character(sample_condition[colnames(bulk_counts)]),
      row.names = colnames(bulk_counts)
    )
  )

  bulk_se <- RunBulk(
    bulk_se = bulk_se,
    method = "de_edgeR_qlf",
    condition.by = "condition",
    verbose = FALSE
  )
  S4Vectors::metadata(bulk_se)$Bulk$status$de$status
}

# ----------------------------
# Example 3: deconvolution + CSDE in one call
# ----------------------------
if (requireNamespace("limma", quietly = TRUE)) {
  data("pancreas_sub", package = "scop")
  srt <- pancreas_sub
  srt$sample_id <- paste0(
    "sample_",
    (seq_len(ncol(srt)) - 1L) \%\% 8L + 1L
  )
  srt$condition <- ifelse(
    srt$sample_id \%in\% paste0("sample_", 1:4),
    "A",
    "B"
  )
  if ("CellType" \%in\% colnames(srt@meta.data)) {
    srt$celltype <- as.character(srt$CellType)
  } else {
    srt$celltype <- as.character(SeuratObject::Idents(srt))
  }

  srt <- RunBulk(
    srt = srt,
    method = c("deconv_MuSiC", "csde_TOAST"),
    sample.by = "sample_id",
    condition.by = "condition",
    condition1 = "A",
    condition2 = "B",
    celltype.by = "celltype",
    verbose = FALSE
  )
  srt@tools$Bulk$status$deconv$status
  srt@tools$Bulk$status$csde$status
}

}
