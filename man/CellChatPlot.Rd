% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CellChatPlot.R
\name{CellChatPlot}
\alias{CellChatPlot}
\title{Plot CellChat analysis results}
\usage{
CellChatPlot(
  srt,
  plot_type = "aggregate",
  condition = NULL,
  pathway = NULL,
  dirpath = NULL,
  output_format = "pdf",
  top_n = 10,
  base_height = 1,
  base_width = 1,
  res = 300,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object that has been processed with \link{RunCellChat}.}

\item{plot_type}{Type of plot to create.
Options: \code{"aggregate"}, \code{"pathway"}, \code{"comparison"},
\code{"heatmap"}, \code{"circle"}, \code{"bubble"}, \code{"gene"}.}

\item{condition}{Condition to plot (if multiple conditions exist).}

\item{pathway}{Specific pathway to visualize (for pathway, bubble, and gene plots).
If \code{NULL}, uses top pathways.}

\item{dirpath}{Directory to save plots.}

\item{output_format}{Format of output figure: \code{"png"} or \code{"pdf"}.
Default is \code{"png"}.}

\item{top_n}{Number of top pathways to use for plotting.
Default is \code{10}.}

\item{base_height}{Base height multiplier for all plots.
Default is \code{1}.}

\item{base_width}{Base width multiplier for all plots.
Default is \code{1}.}

\item{res}{Resolution for PNG output.
Default is \code{300}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\description{
\link{CellChatPlot} creates various visualizations for CellChat analysis results stored in a Seurat object.
}
\examples{
options(log_message.verbose = FALSE)
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)
pancreas_sub <- RunCellChat(
  pancreas_sub,
  group.by = "CellType",
  species = "Mus_musculus"
)

CellChatPlot(pancreas_sub, plot_type = "aggregate")

CellChatPlot(pancreas_sub, plot_type = "pathway")

CellChatPlot(pancreas_sub, plot_type = "bubble")

CellChatPlot(pancreas_sub, plot_type = "gene")

CellChatPlot(pancreas_sub, plot_type = "heatmap")
}
\seealso{
\link{RunCellChat}
}
