% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSecAct.R
\name{RunSecActCCC}
\alias{RunSecActCCC}
\title{Run SecAct cell-cell communication analysis}
\usage{
RunSecActCCC(
  srt = NULL,
  inputProfile = NULL,
  mode = c("scRNAseq", "scST"),
  cellType_meta,
  condition_meta = NULL,
  conditionCase = NULL,
  conditionControl = NULL,
  scale.factor = NULL,
  act_diff_cutoff = 2,
  exp_logFC_cutoff = 0.2,
  exp_mean_all_cutoff = 2,
  exp_fraction_case_cutoff = 0.1,
  padj_cutoff = 0.01,
  sigMatrix = "SecAct",
  is.group.sig = TRUE,
  is.group.cor = 0.9,
  lambda = 5e+05,
  nrand = 1000,
  radius = 20,
  ratio_cutoff = 0.2,
  coreNo = 6,
  tool_name = "SecAct_CCC",
  store_results = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{Optional Seurat object. When \code{mode = "scRNAseq"}, this is passed
to \code{SecAct.activity.inference.scRNAseq}. When \code{mode = "matrix"}, expression
is extracted from \code{srt} if \code{inputProfile} is not supplied.}

\item{inputProfile}{Expression matrix, Seurat object, or SpaCET object.
Matrix input must be genes x samples, cells, or spatial spots.}

\item{mode}{\code{scRNAseq} or \code{scST}.}

\item{cellType_meta}{Metadata column containing cell type or state labels for
\code{mode = "scRNAseq"} when \code{is.singleCellLevel = FALSE}.}

\item{condition_meta}{Metadata column containing condition labels for
scRNA-seq CCC.}

\item{conditionCase, conditionControl}{Case and control labels for scRNA-seq
CCC.}

\item{scale.factor}{Spot-level scale factor passed to
\code{SecAct.activity.inference.ST}.}

\item{act_diff_cutoff, exp_logFC_cutoff, exp_mean_all_cutoff, exp_fraction_case_cutoff, padj_cutoff}{Cutoffs passed to \code{SecAct.CCC.scRNAseq}.}

\item{sigMatrix}{SecAct signature matrix name.}

\item{is.group.sig, is.group.cor, lambda, nrand}{Parameters passed to SecAct
signature grouping and randomization routines.}

\item{radius, ratio_cutoff, coreNo}{Parameters passed to \code{SecAct.CCC.scST}.}

\item{tool_name}{Name used in \code{srt@tools}.}

\item{store_results}{Whether to store raw SecAct results in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat or SpaCET object with SecAct CCC results.
}
\description{
Run SecAct cell-cell communication modules for Seurat scRNA-seq input or
SpaCET single-cell-resolution spatial transcriptomics input.
}
