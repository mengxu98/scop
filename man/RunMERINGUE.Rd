% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMERINGUE.R
\name{RunMERINGUE}
\alias{RunMERINGUE}
\title{Run MERINGUE spatial autocorrelation analysis}
\usage{
RunMERINGUE(
  srt,
  assay = NULL,
  layer = "data",
  image = NULL,
  coord.cols = c("col", "row"),
  features = NULL,
  mode = c("autocorrelation", "cross_correlation", "modules"),
  nfeatures = 2000,
  min_spots = 5,
  filterDist = NA_real_,
  binary = TRUE,
  alternative = "greater",
  nperm = 0,
  ncores = 1,
  pairwise_features = NULL,
  set_variable_features = FALSE,
  store_results = TRUE,
  verbose = TRUE,
  seed = 11,
  neighbor_params = list(),
  moran_params = list(),
  cross_cor_params = list(),
  module_params = list(),
  coordinate_space = c("raw", "legacy_display"),
  backend = c("cpp", "r")
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{layer}{Assay layer used for expression values.}

\item{image}{Spatial image name. Required when multiple images are present;
a single image is selected automatically when \code{NULL}.}

\item{coord.cols}{Metadata coordinate columns used when no image is available.}

\item{features}{Features to score. If \code{NULL}, current variable features are
used; if no variable features are present, all assay features are used.}

\item{mode}{MERINGUE analysis modes to run. \code{"autocorrelation"} computes
spatial autocorrelation, \code{"cross_correlation"} computes pairwise spatial
cross-correlation, and \code{"modules"} detects spatial gene modules.}

\item{nfeatures}{Number of top spatial features stored in
\code{srt@tools[["SpatialVariableFeatures"]]}.}

\item{min_spots}{Minimum number of spots with non-zero expression required
for a feature to be tested.}

\item{filterDist}{Euclidean distance cutoff passed to
\code{MERINGUE::getSpatialNeighbors()}, expressed in the selected coordinate
units.}

\item{binary}{Whether to binarize the MERINGUE spatial neighbor matrix.}

\item{alternative}{Alternative hypothesis passed to MERINGUE Moran tests.}

\item{nperm}{Number of label permutations used for empirical p values. The
default \code{0} skips p-value calculation.}

\item{ncores}{Number of cores passed to MERINGUE permutation tests.}

\item{pairwise_features}{Features used for spatial cross-correlation. If
\code{NULL}, top spatially autocorrelated features are used.}

\item{set_variable_features}{Whether to set the top spatial features as
variable features for \code{assay}.}

\item{store_results}{Whether to store the full result in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed}{Random seed used for permutation tests.}

\item{neighbor_params, moran_params, cross_cor_params, module_params}{Named
lists of additional arguments passed to the corresponding MERINGUE steps.}

\item{coordinate_space}{Coordinate system used for MERINGUE distances. The
default is raw acquisition coordinates; \code{"legacy_display"} remains an
explicit compatibility option.}

\item{backend}{Implementation used for the Moran autocorrelation tests.
\code{"cpp"} uses a scop-compiled kernel that is numerically equivalent to
\code{MERINGUE::moranTest()} and \code{MERINGUE::moranPermutationTest()} and is
substantially faster for permutation tests; \code{"r"} calls the original
MERINGUE functions directly. When \code{"cpp"} is selected, \code{moran_params} are
ignored with a warning because the compiled kernel has no extra arguments.}
}
\value{
A \code{Seurat} object with MERINGUE results stored in
\code{srt@tools[["MERINGUE"]]}. Top autocorrelated features are available at
\code{srt@tools[["MERINGUE"]]$summary$top_features}.
}
\description{
Run \code{MERINGUE} spatial autocorrelation, spatial cross-correlation, and
spatial module analysis for a spatial \code{Seurat} object.
}
\examples{
data(visium_human_pancreas_results_sub)
mer_result <- visium_human_pancreas_results_sub@tools$MERINGUE
mer_result$summary
SpatialSpotPlot(
  visium_human_pancreas_results_sub,
  features = mer_result$autocorrelation$feature[1],
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)

\dontrun{
check_r("JEFworks-Lab/MERINGUE", verbose = FALSE)
spatial <- RunMERINGUE(
  visium_human_pancreas_results_sub,
  assay = "Spatial", coord.cols = c("x", "y"),
  mode = "autocorrelation", nfeatures = 20, verbose = FALSE
)
}
}
