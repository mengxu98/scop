% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DimsEstimate.R
\name{RunDimsEstimate}
\alias{RunDimsEstimate}
\title{Estimate useful dimensions from a reduction}
\usage{
RunDimsEstimate(
  srt,
  reduction = NULL,
  reduction_method = NULL,
  k = 30L,
  min_dims = 5L,
  fallback_max_dims = 50L,
  skip_first = FALSE,
  use_stored = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{reduction}{Name of the dimensional reduction to inspect.
Default is \code{NULL}, which automatically selects a PCA-like reduction via
\code{\link[=DefaultReduction]{DefaultReduction()}} with \code{pattern = "pca"}.}

\item{reduction_method}{Optional reduction method name.
When set to \code{"nmf"} or \code{"glmpca"}, all available dimensions will be retained.}

\item{k}{Number of neighbors used by \link[intrinsicDimension:maxLik]{intrinsicDimension::maxLikGlobalDimEst}.
Default is \code{30}.}

\item{min_dims}{Minimum number of dimensions kept when intrinsic-dimension estimation succeeds.
Default is \code{5}.}

\item{fallback_max_dims}{Maximum number of dimensions kept when no valid estimate is available.
Default is \code{50}.}

\item{skip_first}{Whether to drop the first dimension from the returned result.
Useful for \code{TFIDF/LSI} workflows. Default is \code{FALSE}.}

\item{use_stored}{Whether to use \code{misc$dims_estimate} already stored in the reduction when available.
Default is \code{TRUE}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
An integer vector of dimensions to use.
}
\description{
Estimate useful dimensions from a reduction
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)
RunDimsEstimate(pancreas_sub)

DimsEstimatePlot(pancreas_sub)
}
\seealso{
\link{DimsEstimatePlot}
}
