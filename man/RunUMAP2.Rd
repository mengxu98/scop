% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunUMAP2.R
\name{RunUMAP2}
\alias{RunUMAP2}
\alias{RunUMAP2.Seurat}
\alias{RunUMAP2.default}
\title{Run UMAP}
\usage{
RunUMAP2(object, ...)

\method{RunUMAP2}{Seurat}(
  object,
  reduction = "pca",
  dims = NULL,
  features = NULL,
  neighbor = NULL,
  graph = NULL,
  assay = NULL,
  layer = "data",
  umap.method = "uwot",
  reduction.model = NULL,
  n_threads = NULL,
  return.model = FALSE,
  n.neighbors = 30L,
  n.components = 2L,
  metric = "cosine",
  n.epochs = 200L,
  cores = 1,
  min.dist = 0.3,
  set.op.mix.ratio = 1,
  local.connectivity = 1L,
  negative.sample.rate = 5L,
  a = NULL,
  b = NULL,
  learning.rate = 1,
  repulsion.strength = 1,
  reduction.name = "umap",
  reduction.key = "UMAP_",
  verbose = TRUE,
  seed.use = 11,
  ...
)

\method{RunUMAP2}{default}(
  object,
  assay = NULL,
  umap.method = "uwot",
  reduction.model = NULL,
  n_threads = NULL,
  return.model = FALSE,
  n.neighbors = 30L,
  n.components = 2L,
  metric = "cosine",
  n.epochs = 200L,
  cores = 1,
  min.dist = 0.3,
  set.op.mix.ratio = 1,
  local.connectivity = 1L,
  negative.sample.rate = 5L,
  a = NULL,
  b = NULL,
  learning.rate = 1,
  repulsion.strength = 1,
  reduction.key = "UMAP_",
  verbose = TRUE,
  seed.use = 11L,
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} object, matrix-like object, \code{Neighbor}, or \code{Graph}.}

\item{...}{Passed to the UMAP implementation.}

\item{reduction}{Linear reduction used as input.}

\item{dims}{Dimensions to use. Supply only one of \code{dims}, \code{features},
\code{neighbor}, or \code{graph}.}

\item{features}{Features used instead of a reduction.}

\item{neighbor, graph}{Existing \code{Neighbor} or \code{Graph} object name.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{layer}{Assay layer to use.}

\item{umap.method}{\code{"uwot"} or \code{"naive"}.}

\item{reduction.model}{Pre-trained UMAP \code{DimReduc} used to embed new data.}

\item{n_threads}{Number of threads.}

\item{return.model}{Store the UMAP model.}

\item{n.neighbors, n.components, metric, n.epochs}{UMAP layout parameters.
String \code{metric} values include \code{"euclidean"}, \code{"manhattan"}, \code{"cosine"},
\code{"pearson"}, and \code{"pearson2"}.}

\item{cores}{Number of CPU cores.}

\item{min.dist}{Minimum embedding distance (how tightly points pack).}

\item{set.op.mix.ratio}{Mix of fuzzy union (\code{1}) and intersection (\code{0}).}

\item{local.connectivity, negative.sample.rate}{Fuzzy simplicial set and
optimization sampling.}

\item{a, b}{UMAP curve parameters. \code{NULL} estimates them automatically.}

\item{learning.rate, repulsion.strength}{Layout optimization rates.}

\item{reduction.name, reduction.key}{Stored reduction name and embedding prefix.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed.use}{Random seed.}
}
\description{
Run UMAP
}
\examples{
data(pancreas_sub)
pancreas_sub <- RunStandardWorkflow(pancreas_sub)
pancreas_sub <- RunUMAP2(pancreas_sub, dims = 1:30)
CellDimPlot(
  pancreas_sub,
  group.by = "CellType",
  reduction = "umap"
)
}
