% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCSIDE.R
\name{RunCSIDE}
\alias{RunCSIDE}
\title{Run C-SIDE spatial differential expression}
\usage{
RunCSIDE(
  srt,
  rctd_result = NULL,
  explanatory.variable = NULL,
  group.by = NULL,
  condition.by = NULL,
  design = NULL,
  region_list = NULL,
  barcodes = NULL,
  mode = c("auto", "single", "regions", "general", "intercept"),
  assay = NULL,
  layer = "counts",
  celltypes = NULL,
  features = NULL,
  prefix = "CSIDE",
  tool_name = "CSIDE",
  store_results = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Spatial \code{Seurat} object used as the RCTD query.}

\item{rctd_result}{Optional old-api \code{spacexr} RCTD object. If \code{NULL},
\code{srt@tools[["RCTD"]]$object} from \code{\link[=RunRCTD]{RunRCTD()}} is used.}

\item{explanatory.variable}{Named numeric vector used by
\code{spacexr::run.CSIDE.single()}. Names must match spatial spot names.}

\item{group.by}{Metadata column used to build C-SIDE regions when
\code{region_list} is not supplied.}

\item{condition.by}{Binary metadata column converted to a 0/1 explanatory
variable for \code{spacexr::run.CSIDE.single()}.}

\item{design}{Numeric design matrix used by \code{spacexr::run.CSIDE()}.
Row names must match \code{barcodes} or spatial spot names.}

\item{region_list}{Named list of barcode vectors used by
\code{spacexr::run.CSIDE.regions()}.}

\item{barcodes}{Barcodes used by \code{spacexr::run.CSIDE()} or
\code{spacexr::run.CSIDE.intercept()}. If \code{NULL}, row names of \code{design} or all
spatial spots are used where applicable.}

\item{mode}{C-SIDE mode. \code{"auto"} dispatches to \code{"general"}, \code{"regions"},
\code{"single"}, or \code{"intercept"} based on the supplied design inputs.}

\item{assay}{Assay used in \code{srt}. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used as the spatial expression source.}

\item{celltypes}{Optional cell types passed to C-SIDE as \code{cell_types}.}

\item{features}{Optional features retained in the normalized result table.
C-SIDE itself still applies its own gene filtering through backend
parameters such as \code{gene_threshold}.}

\item{prefix}{Prefix for metadata columns.}

\item{tool_name}{Name used to store detailed C-SIDE results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed RCTD results in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional named parameters passed to the selected C-SIDE
backend, such as \code{cell_type_threshold}, \code{gene_threshold},
\code{doublet_mode}, \code{cell_type_specific}, or \code{params_to_test}. When using the
stored result from \code{\link[=RunRCTD]{RunRCTD()}} with \code{rctd_mode = "full"}, \code{doublet_mode}
defaults to \code{FALSE} unless explicitly supplied.}
}
\value{
A \code{Seurat} object with C-SIDE summary metadata and detailed results
stored in \code{srt@tools[[tool_name]]} when \code{store_results = TRUE}.
}
\description{
Run \code{spacexr} C-SIDE after RCTD to test cell type-specific spatial or
condition-aware differential expression.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
spatial$region <- ifelse(spatial$x > stats::median(spatial$x), "right", "left")
spatial$CSIDE_n_sig <- ifelse(spatial$region == "right", 12, 4)
spatial$CSIDE_mode <- "regions"
cside_result <- data.frame(
  feature = rownames(spatial)[1:4],
  celltype = rep(c("Ductal", "Endocrine"), each = 2),
  parameter = "right_vs_left",
  logFC = c(1.2, 0.8, -0.9, -1.1),
  statistic = c(4.1, 3.5, -3.2, -3.8),
  p_value = c(0.001, 0.004, 0.006, 0.002),
  q_value = c(0.004, 0.008, 0.010, 0.006),
  significant = TRUE,
  method = "regions"
)
spatial@tools$CSIDE <- list(
  result_table = cside_result,
  parameters = list(mode = "regions", group.by = "region")
)

SpatialSpotPlot(
  spatial,
  group.by = "CSIDE_n_sig",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
SpatialSpotPlot(
  spatial,
  features = cside_result$feature[1:2],
  assay = "Spatial",
  layer = "counts",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)

  spatial <- RunCSIDE(
    spatial,
    group.by = "region",
    celltypes = c("Ductal", "Endocrine"),
    gene_threshold = 0.00005,
    cell_type_threshold = 125
  )
}
