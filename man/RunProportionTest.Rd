% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunProportionTest.R
\name{RunProportionTest}
\alias{RunProportionTest}
\title{Proportion Test}
\usage{
RunProportionTest(
  object,
  group.by,
  split.by = NULL,
  comparison = NULL,
  proportion_method,
  sample.by = NULL,
  pseudo_sample_n = 3L,
  allow_pseudo_samples = FALSE,
  n_permutations = 1000,
  FDR_threshold = 0.05,
  log2FD_threshold = log2(1.5),
  include_all_cells = FALSE,
  seed = 11,
  verbose = TRUE,
  ...,
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{group.by}{Metadata column(s) used to color cells.}

\item{split.by}{Metadata column that identifies the condition groups to
compare. For sample-level methods, if \code{split.by} is omitted and \code{sample.by}
is provided, \code{sample.by} is treated as the condition column; a separate
biological sample column is still required unless descriptive virtual
samples are explicitly enabled.}

\item{comparison}{Optional pairs of condition labels. Each pair is
\code{c(group1, group2)}. \code{obs_log2FD} is \code{log2(group1 / group2)}, matching
\link{RunDEtest} / Seurat \code{ident.1} vs \code{ident.2}: positive values mean the cell
type is more abundant in \code{group1}. If \code{NULL}, all pairwise comparisons are
generated in both directions.}

\item{proportion_method}{Differential abundance method.
One of \code{"permutation"}, \code{"milo"}, \code{"sccoda"}, or \code{"propeller"}.
This argument is required.
Alias values such as \code{"permutation_test"} and \code{"perm"} are accepted and
normalized to \code{"permutation"}.}

\item{sample.by}{Metadata column that identifies biological samples.
Required for \code{"milo"}, \code{"sccoda"}, and \code{"propeller"} unless
\code{allow_pseudo_samples = TRUE}. For Milo and Propeller, sample IDs may recur
across two conditions for a fully paired design; partially paired
comparisons are rejected.}

\item{pseudo_sample_n}{Number of virtual samples per \code{split.by} group when
a sample-level method has no usable \code{sample.by} and
\code{allow_pseudo_samples = TRUE}. Virtual samples are descriptive only;
inferential statistics are set to \code{NA}.}

\item{allow_pseudo_samples}{Explicitly allow descriptive virtual samples
when biological sample IDs are unavailable. The default is \code{FALSE} because
partitioning cells does not create biological replicates.}

\item{n_permutations}{Number of permutations for permutation-based test.}

\item{FDR_threshold}{FDR value cutoff for significance.}

\item{log2FD_threshold}{Absolute value of log2FD cutoff for significance.}

\item{include_all_cells}{Whether to include all cell types in the complete grid
for permutation mode.}

\item{seed}{Random seed, including for permutation testing.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to the selected method function.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\description{
\link{RunProportionTest} performs differential abundance testing for cell proportions.
The function acts as a dispatcher and routes to one of the method-specific
implementations: permutation, milo, sccoda, or propeller.
}
\examples{
data(pancreas_sub)
pancreas_sub <- RunProportionTest(
  pancreas_sub,
  group.by = "CellType",
  split.by = "Phase",
  proportion_method = "permutation",
  comparison = list(c("G2M", "G1"))
)

ProportionTestPlot(
  pancreas_sub
)
}
\references{
Miller SA, Policastro RA, Sriramkumar S, et al. (2021). LSD1 and Aberrant
DNA Methylation Mediate Persistence of Enteroendocrine Progenitors That
Support BRAF-Mutant Colorectal Cancer. Cancer Research, 81(14), 3791-3805.
doi:10.1158/0008-5472.CAN-20-3562

\href{https://github.com/rpolicastro/scProportionTest}{scProportionTest},
\href{https://bioconductor.org/packages/miloR}{miloR},
\href{https://github.com/theislab/scCODA}{scCODA},
\href{https://bioconductor.org/packages/speckle}{propeller/speckle}
}
\seealso{
\link{RunPermutation}, \link{RunMilo}, \link{RunscCODA}, \link{RunPropeller},
\link{ProportionTestPlot}
}
