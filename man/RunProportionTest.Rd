% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunProportionTest.R
\name{RunProportionTest}
\alias{RunProportionTest}
\title{Proportion Test}
\usage{
RunProportionTest(
  srt,
  group.by,
  split.by = NULL,
  comparison = NULL,
  proportion_method,
  sample.by = NULL,
  pseudo_sample_n = 3L,
  n_permutations = 1000,
  FDR_threshold = 0.05,
  log2FD_threshold = log2(1.5),
  include_all_cells = FALSE,
  seed = 11,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{group.by}{Name of one or more meta.data columns to group (color) cells by.}

\item{split.by}{Metadata column that identifies the condition groups to
compare. For sample-level methods, if \code{split.by} is omitted and \code{sample.by}
is provided, \code{sample.by} is treated as the condition column and virtual
samples are created within each condition.}

\item{comparison}{Optional: specify comparisons to perform.}

\item{proportion_method}{Differential abundance method.
One of \code{"permutation"}, \code{"milo"}, \code{"sccoda"}, or \code{"propeller"}.
This argument is required.
Alias values such as \code{"permutation_test"} and \code{"perm"} are accepted and
normalized to \code{"permutation"}.}

\item{sample.by}{Metadata column that identifies biological samples.
For \code{"milo"}, \code{"sccoda"}, and \code{"propeller"}, when \code{sample.by} is omitted
or identical to \code{split.by}, virtual samples are created within each
\code{split.by} group for convenience.}

\item{pseudo_sample_n}{Number of virtual samples per \code{split.by} group when
a sample-level method has no usable \code{sample.by}.}

\item{n_permutations}{Number of permutations for permutation-based test.}

\item{FDR_threshold}{FDR value cutoff for significance.}

\item{log2FD_threshold}{Absolute value of log2FD cutoff for significance.}

\item{include_all_cells}{Whether to include all cell types in the complete grid
for permutation mode.}

\item{seed}{Random seed.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to the selected method function.}
}
\description{
\link{RunProportionTest} performs differential abundance testing for cell proportions.
The function acts as a dispatcher and routes to one of the method-specific
implementations: permutation, milo, sccoda, or propeller.
}
\examples{
data(pancreas_sub)
pancreas_sub <- RunProportionTest(
  pancreas_sub,
  group.by = "CellType",
  split.by = "Phase",
  proportion_method = "permutation",
  comparison = list(c("G2M", "G1"))
)

ProportionTestPlot(
  pancreas_sub
)
}
\references{
\href{https://doi.org/10.1158/0008-5472.can-20-3562}{Miller et al. paper},
\href{https://github.com/rpolicastro/scProportionTest}{scProportionTest},
\href{https://bioconductor.org/packages/miloR}{miloR},
\href{https://github.com/theislab/scCODA}{scCODA},
\href{https://bioconductor.org/packages/speckle}{propeller/speckle}
}
\seealso{
\link{RunPermutation}, \link{RunMilo}, \link{RunscCODA}, \link{RunPropeller},
\link{ProportionTestPlot}
}
