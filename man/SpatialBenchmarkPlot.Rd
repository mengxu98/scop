% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpatialBenchmarkPlot.R
\name{SpatialBenchmarkPlot}
\alias{SpatialBenchmarkPlot}
\alias{BenchmarkPlot}
\title{Plot spatial domain clustering benchmarks}
\usage{
SpatialBenchmarkPlot(
  srt = NULL,
  data = NULL,
  features = NULL,
  metrics = NULL,
  tool_name = NULL,
  reduction = NULL,
  plot_type = c("auto", "overview", "quality", "efficiency", "heatmap", "feature",
    "boxplot", "bar", "funkyheatmap"),
  sort_by = c("quality", "method", "runtime", "memory"),
  show_values = TRUE,
  show_status = TRUE,
  resource_scale = c("auto", "linear", "log10"),
  plot_boxplot = TRUE,
  boxplot_jitter = FALSE,
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)

BenchmarkPlot(
  srt = NULL,
  data = NULL,
  features = NULL,
  metrics = NULL,
  tool_name = NULL,
  reduction = NULL,
  plot_type = c("auto", "overview", "quality", "efficiency", "heatmap", "feature",
    "boxplot", "bar", "funkyheatmap"),
  sort_by = c("quality", "method", "runtime", "memory"),
  show_values = TRUE,
  show_status = TRUE,
  resource_scale = c("auto", "linear", "log10"),
  plot_boxplot = TRUE,
  boxplot_jitter = FALSE,
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{data}{Optional \code{spatial_benchmark_result} / \code{benchmark_result} object
or summary \code{data.frame} containing at least \code{metric} and \code{value}, and
optionally \code{method}, \code{workflow}, and \code{direction}.}

\item{features}{Metadata columns containing per-cell benchmark scores.}

\item{metrics}{One or more summary metric names to visualize. Default is
\code{NULL}, which uses all available summary metrics.}

\item{tool_name}{Tool entries created by benchmark-related workflows.
This can be a character vector. For per-cell metrics, benchmark columns are
resolved from tool entries that contain \code{colnames}; for summary metrics,
entries containing \code{summary} or \code{metrics$summary} are used.}

\item{reduction}{Dimensional reduction used for per-cell feature plots.
Default is \code{NULL}, which uses the reduction stored in \code{tool_name} when
available, otherwise \code{\link[=DefaultReduction]{DefaultReduction()}}.}

\item{plot_type}{Plot type. \code{"overview"}, \code{"quality"}, \code{"efficiency"}, and
\code{"heatmap"} consume spatial benchmark results. Existing \code{"feature"},
\code{"boxplot"}, \code{"bar"}, and \code{"funkyheatmap"} modes remain supported.}

\item{sort_by}{Method ordering for spatial benchmark plots. \code{"quality"}
sorts by the mean selected quality metric; other choices sort by method,
runtime, or peak memory.}

\item{show_values}{Whether to print raw metric values on quality and heatmap
panels.}

\item{show_status}{Whether the overview should add a status strip for
failed, unavailable, or timed-out methods.}

\item{resource_scale}{Resource-axis transformation. \code{"auto"} independently
uses log10 for runtime or memory when positive values span at least tenfold.}

\item{plot_boxplot}{Whether to add the summary boxplot when per-cell metrics
are shown.}

\item{boxplot_jitter}{Whether to overlay jittered points on the boxplot.}

\item{combine, nrow, ncol, byrow}{Combine plots with \link[patchwork:patchwork]{patchwork}. \code{combine = FALSE}
returns a list of ggplots.}

\item{pt.size, pt.alpha}{Point size and transparency. \code{pt.size = NULL} scales
with \code{sqrt(n)} (minimum \code{0.3}). Rasterized points keep at least a two-pixel
radius at \code{raster.dpi = c(512, 512)} and scale with \code{raster.dpi}.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{theme_use, theme_args}{Theme name or function, plus extra theme arguments.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{The message to print.}
}
\value{
A ggplot, patchwork plot, or \code{funkyheatmap} object depending on the selected
mode. If \code{combine = FALSE} in per-cell mode, a named list of plots is
returned.
}
\description{
Visualize a \code{spatial_benchmark_result} from \code{\link[=RunSpatialBenchmark]{RunSpatialBenchmark()}}, or a
summary \code{data.frame}. Spatial results default to a publication-oriented
overview that pairs clustering quality with runtime and peak-memory
efficiency. For scRNA integration metrics such as LISI, use
\code{\link[=IntegrationBenchmarkPlot]{IntegrationBenchmarkPlot()}}.
}
\examples{
metrics_df <- data.frame(
  method = c("Raw", "Raw", "Harmony", "Harmony"),
  metric = c("batch_ASW_mixing", "celltype_ASW", "batch_ASW_mixing", "celltype_ASW"),
  value = c(0.42, 0.71, 0.68, 0.66)
)
SpatialBenchmarkPlot(
  data = metrics_df,
  plot_type = "bar"
)

data("pbmcmultiome_sub", package = "scop")
pbmcmultiome_sub[["MethodA_batch_LISI"]] <-
  seq_len(ncol(pbmcmultiome_sub)) / ncol(pbmcmultiome_sub)
pbmcmultiome_sub[["MethodB_batch_LISI"]] <-
  rev(pbmcmultiome_sub[["MethodA_batch_LISI", drop = TRUE]])
SpatialBenchmarkPlot(
  pbmcmultiome_sub,
  features = c("MethodA_batch_LISI", "MethodB_batch_LISI"),
  plot_type = "boxplot"
)

}
\seealso{
\link{RunSpatialBenchmark}, \link{IntegrationBenchmarkPlot}
}
