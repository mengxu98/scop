% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LISI.R
\name{RunLISI}
\alias{RunLISI}
\title{Compute LISI scores on a Seurat object}
\usage{
RunLISI(
  srt,
  reductions = NULL,
  reduction = NULL,
  dims = NULL,
  label_colnames = NULL,
  prefix = NULL,
  tool_name = NULL,
  perplexity = 30,
  tol = 1e-05,
  max_iter = 50,
  knn_algorithm = c("auto", "brute_force", "clustered"),
  cores = NULL,
  max_dense_bytes = Inf,
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{reductions}{Character vector of dimensional reductions used to compute LISI.
If \code{NULL}, \code{\link[=DefaultReduction]{DefaultReduction()}} is used.}

\item{reduction}{Deprecated alias of \code{reductions}.}

\item{dims}{Dimensions to use from the reduction. Default is \code{NULL},
which uses all available dimensions.}

\item{label_colnames}{Character vector of metadata columns used for LISI.
If \code{NULL}, \code{RunLISI()} will try to use \code{srt@misc[["integration_batch"]]}.}

\item{prefix}{Prefix used for the stored LISI metadata columns.
If \code{NULL}, the reduction names are used.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.
Default is \code{"LISI"} when multiple reductions are provided, otherwise
\code{paste0(prefix, "_LISI")}.}

\item{perplexity}{Effective neighborhood size.}

\item{tol}{Tolerance used in the binary search for the target perplexity.}

\item{max_iter}{Maximum number of binary-search iterations.}

\item{knn_algorithm}{Exact nearest-neighbor strategy passed to
\code{\link[thisutils:compute_lisi]{thisutils::compute_lisi()}}.}

\item{cores}{Number of LISI C++ worker threads. \code{NULL} (the default) lets
\code{\link[thisutils:compute_lisi]{thisutils::compute_lisi()}} select the available hardware threads.}

\item{max_dense_bytes}{Maximum estimated bytes allowed for LISI's dense
input and C++ copy. Default is \code{Inf}, which preserves unrestricted behavior.}

\item{overwrite}{Whether to overwrite existing metadata columns.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A modified \code{Seurat} object.
}
\description{
Compute per-cell Local Inverse Simpson's Index (LISI) scores from a
dimensional reduction and store them in the \code{meta.data} and \code{tools} slots
of a \code{Seurat} object.
}
\examples{
data(panc8_sub)
set.seed(1)
demo_embedding <- matrix(
  stats::rnorm(ncol(panc8_sub) * 5),
  nrow = ncol(panc8_sub),
  dimnames = list(colnames(panc8_sub), paste0("DEMO_", 1:5))
)
panc8_sub[["demo"]] <- SeuratObject::CreateDimReducObject(
  embeddings = demo_embedding,
  key = "DEMO_",
  assay = SeuratObject::DefaultAssay(panc8_sub)
)
names(panc8_sub@reductions)

panc8_sub <- RunLISI(
  panc8_sub,
  reductions = "demo",
  label_colnames = "tech",
  perplexity = 10
)
LISIPlot(
  panc8_sub,
  combine = TRUE
)
}
\seealso{
\link[thisutils:compute_lisi]{thisutils::compute_lisi}, \link{LISIPlot}
}
