% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FerrisWheelPlot.R
\name{FerrisWheelPlot}
\alias{FerrisWheelPlot}
\title{Ferris Wheel Plot}
\usage{
FerrisWheelPlot(
  data = NULL,
  res = NULL,
  label_col = "pathway",
  up_col = "up",
  down_col = "down",
  total_up = NULL,
  total_down = NULL,
  de_results = NULL,
  term_col = "Description",
  group_col = "Groups",
  count_col = "Count",
  padj_col = "p.adjust",
  up_group = "Up",
  down_group = "Down",
  padj_cutoff = 0.05,
  top_n = 10,
  up_label = "Up-regulated Genes",
  down_label = "Down-regulated Genes",
  up_color = NULL,
  down_color = NULL,
  palette = "Chinese",
  palcolor = NULL,
  center_label = "Significant Genes",
  center_radius = 1.25,
  center_width = 0.28,
  outer_distance = 2.55,
  outer_width = 0.12,
  min_outer_radius = 0.28,
  max_outer_radius = 0.48,
  label_wrap = 26,
  label_case = c("title", "none"),
  label.stroke = 0.2,
  label.stroke.color = "white",
  label.size = 3,
  number.size = 3,
  center.size = 4.5,
  legend.size = 3.5,
  line.color = "grey35",
  line.linewidth = 0.35,
  legend.position = c("bottom", "none"),
  direction = 1,
  start = pi/2,
  theme_use = "theme_scop",
  theme_args = list()
)
}
\arguments{
\item{data}{A \code{data.frame} containing category labels and two non-negative
count columns. If \code{NULL}, the table is built from \code{res}.}

\item{res}{A result list returned by \code{\link[=RunEnrichment]{RunEnrichment()}} or an enrichment
\code{data.frame}. When provided, \code{FerrisWheelPlot()} summarizes enriched terms
into up/down counts internally.}

\item{label_col}{Column name for category labels.
Default is \code{"pathway"}.}

\item{up_col}{Column name for up-regulated counts.
Default is \code{"up"}.}

\item{down_col}{Column name for down-regulated counts.
Default is \code{"down"}.}

\item{total_up, total_down}{Total counts shown in the center donut. When
\code{NULL}, the corresponding column sum is used. For enrichment input, these
can be inferred from \code{de_results}.}

\item{de_results}{Optional differential expression result table used to infer
\code{total_up} and \code{total_down} from \code{avg_log2FC}.}

\item{term_col, group_col, count_col, padj_col}{Column names used when \code{res} is
an enrichment result. Defaults match \code{\link[=RunEnrichment]{RunEnrichment()}} output.}

\item{up_group, down_group}{Group labels in enrichment results. Default
matches examples that use \code{"Up"} and \code{"Down"} as \code{geneID_groups}.}

\item{padj_cutoff}{Adjusted p-value cutoff used to select enriched terms
from \code{res}.}

\item{top_n}{Number of enriched terms to show when \code{res} is provided.
Default is \code{10}.}

\item{up_label, down_label}{Legend labels and center text labels.
Defaults are \code{"Up-regulated Genes"} and \code{"Down-regulated Genes"}.}

\item{up_color, down_color}{Optional colors for up/down counts. When \code{NULL},
colors are selected from the \code{"Chinese"} palette used by \code{scop}.}

\item{palette, palcolor}{Palette passed to \code{\link[thisplot:palette_colors]{thisplot::palette_colors()}} when
\code{up_color} or \code{down_color} is not provided.}

\item{center_label}{Text shown inside the center donut.
Default is \code{"Significant Genes"}.}

\item{center_radius}{Radius of the center donut.
Default is \code{1.25}.}

\item{center_width}{Width of the center donut band.
Default is \code{0.28}.}

\item{outer_distance}{Distance from plot center to outer donut centers.
Default is \code{2.55}.}

\item{outer_width}{Width of outer donut bands.
Default is \code{0.12}.}

\item{min_outer_radius, max_outer_radius}{Minimum and maximum outer donut
radii after scaling by category count.}

\item{label_wrap}{Maximum characters per category label line. Set \code{NULL} to
disable wrapping.}

\item{label_case}{Label case for category names. \code{"title"} capitalizes words
by default; \code{"none"} keeps labels unchanged.}

\item{label.stroke}{White outline width around text labels. Default is
\code{0.1}. Set \code{0} to disable the outline.}

\item{label.stroke.color}{Outline color for text labels.}

\item{label.size, number.size, center.size, legend.size}{Text sizes.}

\item{line.color, line.linewidth}{Connector line styling.}

\item{legend.position}{Position of the compact legend. Use \code{"bottom"} or
\code{"none"}.}

\item{direction}{Drawing direction for the outer donuts. \code{1} is clockwise and
\code{-1} is counterclockwise.}

\item{start}{Start angle in radians for the first category.
Default is \code{pi / 2}.}

\item{theme_use}{Theme function name.
Default is \code{"theme_scop"}.}

\item{theme_args}{Additional arguments passed to \code{theme_use}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw a Ferris wheel-style donut plot for up/down counts across pathways or
other categories. The center donut shows the total counts, and outer donuts
show category-level counts with radius scaled by category size.
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)
pancreas_sub <- RunDEtest(
  pancreas_sub,
  group.by = "CellType",
  only.pos = FALSE
)

de_df <- pancreas_sub@tools$DEtest_CellType$AllMarkers_wilcox
de_df <- de_df[
  de_df$p_val_adj < 0.05 & abs(de_df$avg_log2FC) > 0.25,
  ,
  drop = FALSE
]
de_df$direction <- ifelse(de_df$avg_log2FC > 0, "Up", "Down")

enrich_out <- RunEnrichment(
  geneID = de_df$gene,
  geneID_groups = de_df$direction,
  db = "GO_BP",
  species = "Mus_musculus"
)
FerrisWheelPlot(
  res = enrich_out,
  de_results = de_df
)
}
\seealso{
\link{EnrichmentPlot}, \link{DEtestPlot}
}
