% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialVariableFeatures.R
\name{RunSpatialVariableFeatures}
\alias{RunSpatialVariableFeatures}
\title{Run spatial variable feature detection}
\usage{
RunSpatialVariableFeatures(
  srt,
  assay = NULL,
  layer = "data",
  features = NULL,
  method = c("moran", "geary", "SPARKX", "nnSVG"),
  image = NULL,
  coord.cols = c("x", "y"),
  k = 6,
  nfeatures = 2000,
  min_spots = 5,
  nperm = 0,
  set_variable_features = TRUE,
  store_results = TRUE,
  verbose = TRUE,
  seed = 11,
  coordinate_space = c("legacy_display", "raw"),
  ...
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{layer}{Assay layer used for expression values.}

\item{features}{Features to score. If \code{NULL}, current variable features are
used; if no variable features are present, all assay features are used.}

\item{method}{Spatial variable feature detection method.}

\item{image}{Name of the Seurat spatial image used by the spatial workflow.
If \code{NULL}, the first image is used when present.}

\item{coord.cols}{Metadata coordinate columns used by the spatial workflow
when no image is available.}

\item{k}{Number of nearest spatial neighbors per spot.}

\item{nfeatures}{Number of top spatial features stored in
\code{srt@misc[["SpatialVariableFeatures"]]}.}

\item{min_spots}{Minimum number of spots with non-zero expression required
for a feature to be tested.}

\item{nperm}{Number of label permutations used for empirical p values. The
default \code{0} skips p-value calculation.}

\item{set_variable_features}{Whether to set the top spatial features as
variable features for \code{assay}.}

\item{store_results}{Whether to store the full result in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed}{Random seed used for permutation tests.}

\item{coordinate_space}{Coordinate system used for distance-sensitive
analysis. \code{"legacy_display"} preserves the current behavior; \code{"raw"}
uses unscaled, unflipped source coordinates.}

\item{...}{Additional arguments passed to external backends.}
}
\value{
A \code{Seurat} object with spatial variable feature results stored in
\code{srt@tools[["SpatialVariableFeatures"]]} and top feature names stored in
\code{srt@misc[["SpatialVariableFeatures"]]}.
}
\description{
Score genes by spot-level spatial autocorrelation. The native \code{"moran"} and
\code{"geary"} methods use a lightweight coordinate KNN graph. \code{"SPARKX"} and
\code{"nnSVG"} use optional external backends when their packages are installed.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- Seurat::NormalizeData(
  visium_human_pancreas_sub,
  assay = "Spatial",
  verbose = FALSE
)
spatial <- Seurat::FindVariableFeatures(
  spatial,
  assay = "Spatial",
  nfeatures = 100,
  verbose = FALSE
)

SpatialSpotPlot(
  spatial,
  features = Seurat::VariableFeatures(spatial, assay = "Spatial")[1:2]
)

spatial <- RunSpatialVariableFeatures(
  spatial,
  assay = "Spatial",
  nfeatures = 50
)
SpatialVariableFeaturePlot(spatial, plot_type = "combined", nfeatures = 2)
}
