% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCell2location.R
\name{Cell2locationPlot}
\alias{Cell2locationPlot}
\title{Plot cell2location spatial results}
\usage{
Cell2locationPlot(
  srt,
  plot_type = c("proportion", "abundance", "dominant", "pie"),
  cell_types = NULL,
  prefix = "Cell2location",
  tool_name = "Cell2location",
  image = NULL,
  overlay_image = TRUE,
  coord.cols = c("col", "row"),
  ...,
  image.scale = c("lowres", "hires")
)
}
\arguments{
\item{srt}{A \code{Seurat} object returned by \code{\link[=RunCell2location]{RunCell2location()}}.}

\item{plot_type}{Result to draw: normalized proportion, q05 absolute
abundance, dominant cell type, or a spot-level proportion pie.}

\item{cell_types}{Optional cell types to display for abundance, proportion,
or pie plots.}

\item{prefix}{Metadata prefix used by \code{\link[=RunCell2location]{RunCell2location()}}.}

\item{tool_name}{Name of the \code{srt@tools} result entry.}

\item{image}{Spatial image name. Required when multiple images are present;
a single image is selected automatically when \code{NULL}.}

\item{overlay_image}{Whether to draw the selected spatial image.}

\item{coord.cols}{Metadata coordinate columns used when no image is available.}

\item{...}{Additional arguments passed to \code{\link[=SpatialSpotPlot]{SpatialSpotPlot()}}.}

\item{image.scale}{Image scale factor matching the raster stored in the
selected image. Use \code{"hires"} for a hires raster; do not modify Seurat
scale-factor slots.}
}
\value{
A \code{ggplot}, \code{patchwork}, or list of plots.
}
\description{
Plot cell2location spatial results
}
\details{
Use an object returned by \code{\link[=RunCell2location]{RunCell2location()}} with \code{store_results = TRUE}.
The selected \code{tool_name} must contain its fitted posterior results.
Abundance plots use q05 posterior abundance; proportion plots use the
stored row-normalized proportions. An input-only Visium object has neither
result, so run the model before plotting.
}
