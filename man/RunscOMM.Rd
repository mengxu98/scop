% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunscOMM.R
\name{RunscOMM}
\alias{RunscOMM}
\title{Run scOMM label prediction}
\usage{
RunscOMM(
  srt,
  reference,
  reference_assay = NULL,
  query_assay = NULL,
  reference_label = NULL,
  features = NULL,
  prediction_prefix = "predicted_",
  evaluate = FALSE,
  truth_col = NULL,
  tool_name = "scOMM",
  rare_threshold = 0.05,
  scomm_python = NULL,
  scomm_hidden_nodes = c(128, 64),
  scomm_epochs = 10,
  scomm_batch_size = 32,
  scomm_threshold = 0.5,
  scomm_seed = 11,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{reference}{Reference \code{Seurat} object used for supervision.}

\item{reference_assay}{Assay used in the reference object.}

\item{query_assay}{Assay used in the query object.}

\item{reference_label}{Metadata column in the reference used as supervision labels.}

\item{features}{Shared features passed to \code{scOMM}. If \code{NULL}, reference variable
features are used.}

\item{prediction_prefix}{Prefix added to prediction metadata columns.}

\item{evaluate}{Whether to compute prediction metrics against a truth label.}

\item{truth_col}{Metadata column in \code{srt} used as the truth label when
\code{evaluate = TRUE}.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{rare_threshold}{Maximum class proportion used to define rare classes
when calculating \code{rare_recall}.}

\item{scomm_python}{Optional Python binary used by the \code{scOMM} backend.
If \code{NULL}, \code{SCOP_SCOMM_PYTHON} is consulted and reticulate defaults are used
otherwise.}

\item{scomm_hidden_nodes, scomm_epochs, scomm_batch_size, scomm_threshold, scomm_seed}{Parameters passed to the \code{scOMM} backend.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with \code{scOMM} predictions stored in metadata and \code{tools}.
}
\description{
Run \code{scOMM} on shared features between a reference object and a query object,
write predicted labels and class scores into query metadata, and optionally
evaluate predictions against a truth label.
}
