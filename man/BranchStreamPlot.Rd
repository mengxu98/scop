% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BranchStreamPlot.R
\name{BranchStreamPlot}
\alias{BranchStreamPlot}
\title{Branch Stream Plot}
\usage{
BranchStreamPlot(
  object,
  group.by,
  lineages = NULL,
  lineage.merge = c("min", "mean", "max"),
  labels = NULL,
  trunk_groups = NULL,
  branch_groups = NULL,
  branch_center = "auto",
  branch_steepness = 11,
  branch_power = 1.1,
  branch_amplitude = 0.28,
  n_branches = 2,
  n_grid = 800,
  bw = 0.15,
  pad = 0.035,
  count_power = 0.3,
  normalize_pseudotime = FALSE,
  scale_to = "auto",
  min_visible_width = 1e-04,
  xlim = NULL,
  ylim = NULL,
  xlabel = "Pseudotime",
  xticks = "auto",
  axis_y = "auto",
  label_positions = "auto",
  label_size = 4,
  label_fontface = "bold",
  label_fill = NULL,
  label_fill_alpha = 0.85,
  label_outline_width = 0.0018,
  label_outline_alpha = 0.8,
  ribbon_alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  axis_arrow = grid::arrow(length = grid::unit(0.08, "inches")),
  title = NULL,
  legend.position = "none",
  legend.direction = "vertical",
  theme_use = "theme_scop",
  theme_args = list(),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE
)
}
\arguments{
\item{object}{A \code{Seurat} object or a \code{data.frame} containing cell metadata.}

\item{group.by}{Column containing cell-state or branch labels.}

\item{lineages}{Pseudotime columns to use. If \code{NULL}, lineage-like
pseudotime columns such as \code{Lineage1}, \code{Lineage2}, \code{prefix_Lineage1}, or
\code{pseudotime} are detected and merged into one global pseudotime for a
single panel. Use \code{"all"} to plot each detected lineage in separate panels.}

\item{lineage.merge}{How to merge multiple lineage pseudotime columns when
\code{lineages} is \code{NULL} or \code{"merge"}.}

\item{labels}{Optional order of groups to plot. Factor levels are used when
omitted.}

\item{trunk_groups}{Groups placed on the shared trunk. If \code{NULL}, groups are
inferred from KDE peak positions.}

\item{branch_groups}{Optional named numeric vector/list mapping group labels
to branch amplitudes. Positive and negative amplitudes bend branches in
opposite directions.}

\item{branch_center, branch_steepness, branch_power, branch_amplitude}{Parameters controlling automatic branch centerlines. Use \code{"auto"} for
\code{branch_center} to place the split point on the current pseudotime scale.}

\item{n_branches}{Number of branch amplitudes used when \code{branch_groups} is
inferred.}

\item{n_grid}{Number of pseudotime grid points.}

\item{bw, pad}{SciPy-style Gaussian KDE bandwidth factor and taper padding.}

\item{count_power}{Exponent used to scale profiles by group abundance.}

\item{normalize_pseudotime}{Whether to min-max normalize pseudotime before
KDE. Default is \code{FALSE}, so the original pseudotime scale is shown. Use
\code{"auto"} to normalize only when values fall outside \verb{[0, 1]}.}

\item{scale_to}{Maximum ribbon thickness after global rescaling. Use
\code{"auto"} for a sensible default or \code{NULL} to disable rescaling.}

\item{min_visible_width}{Ribbons thinner than this threshold are hidden.}

\item{xlim, ylim}{Plot limits. \code{xlim = NULL} uses the observed pseudotime
range.}

\item{xlabel}{X-axis label.}

\item{xticks}{Pseudotime tick locations. Use \code{"auto"} for pretty breaks
on the current pseudotime scale.}

\item{axis_y}{Y position of the arrow-style pseudotime axis. Use \code{"auto"} or
\code{NULL}.}

\item{label_positions}{Optional data.frame/list with label, x, y, and
optional size columns. \code{"auto"} places labels at ribbon maxima.}

\item{label_size, label_fontface, label_fill, label_fill_alpha}{Label styling.
If \code{label_fill} is \code{NULL}, labels are drawn as OmicVerse-style colored
text with a white outline.}

\item{label_outline_width, label_outline_alpha}{Outline styling used when
\code{label_fill} is \code{NULL}.}

\item{ribbon_alpha}{Ribbon transparency.}

\item{palette}{Color palette name.}

\item{palcolor}{Custom colors used to create a color palette.}

\item{axis_arrow}{Arrow used for the pseudotime axis.}

\item{title}{Plot title. \code{NULL} hides the title for merged/single panels.
When multiple lineages are plotted and \code{title} is \code{NULL}, each panel is
titled with its lineage column.}

\item{legend.position}{The position of legends,
one of \code{"none"}, \code{"left"}, \code{"right"}, \code{"bottom"}, \code{"top"}.
Default is \code{"right"}.}

\item{legend.direction}{The direction of the legend in the plot.
Can be one of \code{"vertical"} or \code{"horizontal"}.}

\item{theme_use}{Theme used. Can be a character string or a theme function.
Default is \code{"theme_scop"}.}

\item{theme_args}{Other arguments passed to the \code{theme_use}.
Default is \code{list()}.}

\item{combine}{Whether to combine multiple lineage plots with \code{patchwork}.}

\item{nrow, ncol, byrow}{Layout controls for combined lineage plots.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw branch-aware pseudotime ribbons from cell-state annotations. The plot is
a visualization of pseudotime density by group; it does not infer a lineage
tree by itself.
}
\examples{
branch_df <- data.frame(
  cell_type = rep(c("Root", "Branch A", "Branch B"), each = 30),
  pseudotime = c(
    seq(0, 30, length.out = 30),
    seq(25, 80, length.out = 30),
    seq(25, 100, length.out = 30)
  )
)
BranchStreamPlot(
  branch_df,
  group.by = "cell_type",
  lineages = "pseudotime"
)
}
