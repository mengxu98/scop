% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSTdeconvolve.R
\name{RunSTdeconvolve}
\alias{RunSTdeconvolve}
\title{Run STdeconvolve reference-free spatial deconvolution}
\usage{
RunSTdeconvolve(
  srt,
  assay = NULL,
  layer = "counts",
  features = NULL,
  k = NULL,
  k_candidates = 2:9,
  opt = "min",
  clean_counts = TRUE,
  clean_counts_params = list(),
  restrict_corpus = TRUE,
  restrict_corpus_params = list(),
  fit_lda_params = list(),
  get_beta_theta_params = list(),
  prefix = "STdeconvolve",
  tool_name = "STdeconvolve",
  store_results = TRUE,
  round_counts = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Spatial \code{Seurat} object used as the RCTD query.}

\item{assay}{Assay used in \code{srt}. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used as STdeconvolve input.}

\item{features}{Features used for RCTD. If \code{NULL}, shared features are used.}

\item{k}{Number of topics. If \code{NULL}, models are fit over \code{k_candidates} and
\code{STdeconvolve::optimalModel()} is used to choose a model.}

\item{k_candidates}{Candidate topic numbers used when \code{k = NULL}.}

\item{opt}{Optimal model selector passed to \code{STdeconvolve::optimalModel()}.}

\item{clean_counts}{Whether to call \code{STdeconvolve::cleanCounts()}.}

\item{clean_counts_params}{Additional parameters passed to
\code{STdeconvolve::cleanCounts()}.}

\item{restrict_corpus}{Whether to call \code{STdeconvolve::restrictCorpus()}.}

\item{restrict_corpus_params}{Additional parameters passed to
\code{STdeconvolve::restrictCorpus()}.}

\item{fit_lda_params}{Additional parameters passed to
\code{STdeconvolve::fitLDA()}.}

\item{get_beta_theta_params}{Additional parameters passed to
\code{STdeconvolve::getBetaTheta()}.}

\item{prefix}{Prefix for metadata columns.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed RCTD results in \code{srt@tools}.}

\item{round_counts}{Whether to round non-integer counts before model fitting.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters passed to the RCTD run step.}
}
\value{
A \code{Seurat} object with topic proportions in metadata and detailed
results stored in \code{srt@tools[[tool_name]]} when \code{store_results = TRUE}.
}
\description{
Estimate spot-level topic proportions from a spatial \code{Seurat} object using
the optional \code{STdeconvolve} package. The producer example is a
non-executing template; \code{\link[=STdeconvolvePlot]{STdeconvolvePlot()}} demonstrates a validated stored
result without rerunning the backend.
}
\examples{
\dontrun{
check_r("JEFworks-Lab/STdeconvolve", verbose = FALSE)
data(visium_human_pancreas_sub)
keep_spots <- unique(round(seq(
  1,
  ncol(visium_human_pancreas_sub),
  length.out = 120
)))
spatial <- visium_human_pancreas_sub[, keep_spots]
spatial <- RunSTdeconvolve(
  spatial,
  assay = "Spatial",
  layer = "counts",
  features = head(rownames(spatial), 300),
  k = 3,
  verbose = FALSE
)
STdeconvolvePlot(
  spatial,
  topics = 1L,
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
}
}
