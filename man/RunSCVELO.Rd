% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSCVELO.R
\name{RunSCVELO}
\alias{RunSCVELO}
\title{Run scVelo workflow}
\usage{
RunSCVELO(
  srt = NULL,
  adata = NULL,
  assay_x = "RNA",
  layer_x = "counts",
  assay_y = c("spliced", "unspliced"),
  layer_y = "counts",
  group.by = NULL,
  linear_reduction = NULL,
  nonlinear_reduction = NULL,
  basis = NULL,
  mode = "stochastic",
  fitting_by = "stochastic",
  magic_impute = FALSE,
  knn = 5,
  t = 2,
  min_shared_counts = 30,
  n_pcs = 30,
  n_neighbors = 30,
  filter_genes = TRUE,
  min_counts = 3,
  min_counts_u = 3,
  normalize_per_cell = TRUE,
  log_transform = TRUE,
  use_raw = FALSE,
  diff_kinetics = FALSE,
  stream_smooth = NULL,
  stream_density = 2,
  arrow_length = 5,
  arrow_size = 5,
  arrow_density = 0.5,
  denoise = FALSE,
  denoise_topn = 3,
  kinetics = FALSE,
  kinetics_topn = 100,
  calculate_velocity_genes = FALSE,
  compute_velocity_confidence = TRUE,
  compute_velocity_graph = NULL,
  compute_terminal_states = FALSE,
  compute_pseudotime = FALSE,
  compute_paga = FALSE,
  top_n = 6,
  cores = 1,
  palette = "Chinese",
  palcolor = NULL,
  legend.position = "on data",
  show_plot = TRUE,
  save_plot = FALSE,
  plot_format = c("pdf", "png", "svg"),
  plot_dpi = 300,
  plot_prefix = "scvelo",
  dirpath = "./scvelo",
  backend = c("python", "cpp"),
  max_dense_gib = 8,
  return_seurat = !is.null(srt),
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.
If provided, \code{adata} will be ignored.}

\item{adata}{An anndata object.}

\item{assay_x}{Assay to convert as the main data matrix in the anndata object.}

\item{layer_x}{Layer name for assay_x in the Seurat object.}

\item{assay_y}{Assays to convert as layers in the anndata object.}

\item{layer_y}{Layer names for the assay_y in the Seurat object.}

\item{group.by}{Metadata column(s) used to color cells.}

\item{basis}{The basis to use for reduction, e.g., \code{"UMAP"}.}

\item{mode}{Velocity estimation models to use.
Can be \code{"deterministic"}, \code{"stochastic"}, or \code{"dynamical"}.
If the corresponding velocity reduction (e.g. \code{stochastic_umap}) is not
found, falls back to the scVelo convention (e.g. \code{velocity_umap}), which is
what an object converted from an AnnData (via \link{adata_to_srt}) contains.}

\item{fitting_by}{Method used to fit gene velocities for dynamical modeling.}

\item{magic_impute}{Flag indicating whether to perform magic imputation.}

\item{knn}{The number of nearest neighbors for \code{magic.MAGIC}.}

\item{t}{Power to which the diffusion operator is powered for \code{magic.MAGIC}.}

\item{min_shared_counts}{Minimum number of counts (both unspliced and spliced) required for a gene.}

\item{n_pcs}{Number of principal components to use for linear reduction.}

\item{n_neighbors}{Number of neighbors to use for constructing the KNN graph.}

\item{filter_genes}{Whether to filter genes based on minimum counts.}

\item{min_counts}{Minimum counts for gene filtering.}

\item{min_counts_u}{Minimum unspliced counts for gene filtering.}

\item{normalize_per_cell}{Whether to normalize counts per cell.}

\item{log_transform}{Whether to apply log transformation.}

\item{use_raw}{Whether to use raw data for dynamical modeling.}

\item{diff_kinetics}{Whether to use differential kinetics.}

\item{stream_smooth}{Multiplication factor for scale in Gaussian kernel around grid point.}

\item{stream_density}{Controls the closeness of streamlines.
When density = 2 (default), the domain is divided into a 60x60 grid,
whereas density linearly scales this grid.
Each cell in the grid can have, at most, one traversing streamline.}

\item{arrow_length}{Length of arrows.}

\item{arrow_size}{Size of arrows.}

\item{arrow_density}{Amount of velocities to show.}

\item{denoise}{Boolean flag indicating whether to denoise.}

\item{denoise_topn}{Number of genes with highest likelihood selected to infer velocity directions.}

\item{kinetics}{Boolean flag indicating whether to estimate RNA kinetics.}

\item{kinetics_topn}{Number of genes with highest likelihood selected to infer velocity directions.}

\item{calculate_velocity_genes}{Boolean flag indicating whether to calculate velocity genes.}

\item{compute_velocity_confidence}{Whether to compute velocity confidence metrics.}

\item{compute_velocity_graph}{Whether to compute and store the velocity graph
for downstream terminal-state or pseudotime calculations. If \code{NULL}, compute
and store the graph only when terminal states or pseudotime are requested.
The velocity embedding itself is always graph-projected, matching
\code{scv.tl.velocity_embedding}.}

\item{compute_terminal_states}{Whether to compute terminal states (root and end points).}

\item{compute_pseudotime}{Whether to compute velocity pseudotime.}

\item{compute_paga}{Whether to compute PAGA (Partition-based graph abstraction).}

\item{top_n}{The number of top features to plot.}

\item{cores}{The number of cores to use for \code{cellrank}.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{legend.position}{Position of legend in plots.
Can be \code{"on data"}, \code{"right margin"}, \code{"bottom right"}, etc.}

\item{show_plot}{Whether to show the plot.}

\item{save_plot}{Whether to save plots to files.}

\item{plot_format}{Format for saved plots: \code{"png"} (default), \code{"pdf"}, or \code{"svg"}.}

\item{plot_dpi}{Resolution (DPI) for saved plots.}

\item{plot_prefix}{Prefix for saved plot filenames.}

\item{dirpath}{The directory to save the plots.}

\item{backend}{Backend used to compute RNA velocity. \code{"python"} keeps the
original scVelo workflow. \code{"cpp"} uses the package C++ implementation for a
stochastic velocity embedding that is compatible with \link{VelocityPlot}.}

\item{max_dense_gib}{Maximum estimated GiB allowed for the dense expression
working matrices used by the C++ path.}

\item{return_seurat}{Whether to return a Seurat object instead of an anndata object.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\description{
scVelo is a scalable toolkit for RNA velocity analysis in single cells.
Runs an enhanced scVelo workflow on a Seurat object with improved error handling,
version compatibility, and modular design.
}
\examples{
data(pancreas_sub)
pancreas_sub <- RunStandardWorkflow(pancreas_sub)
pancreas_sub <- RunSCVELO(
  pancreas_sub,
  assay_x = "RNA",
  group.by = "SubCellType",
  linear_reduction = "PCA",
  nonlinear_reduction = "UMAP",
  backend = "cpp",
  show_plot = FALSE
)

FeatureDimPlot(
  pancreas_sub,
  c(
    "stochastic_length",
    "stochastic_confidence"
  )
)

VelocityPlot(
  pancreas_sub,
  reduction = "UMAP",
  plot_type = "stream"
)

CellDimPlot(
  pancreas_sub,
  group.by = "SubCellType",
  reduction = "UMAP",
  pt.size = NA,
  velocity = "stochastic"
)
}
\seealso{
\link{VelocityPlot}, \link{CellDimPlot}, \link{RunPAGA}
}
