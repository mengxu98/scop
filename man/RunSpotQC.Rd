% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpotQC.R
\name{RunSpotQC}
\alias{RunSpotQC}
\title{Run spot-level quality control}
\usage{
RunSpotQC(
  srt,
  assay = NULL,
  return_filtered = FALSE,
  qc_metrics = c("outlier", "umi", "gene", "mito"),
  outlier_threshold = c("log10_nCount:lower:3", "log10_nFeature:lower:3",
    "spot_featurecount_dist:lower:3"),
  outlier_n = 1,
  UMI_threshold = 500,
  gene_threshold = 200,
  mito_threshold = 20,
  mito_pattern = c("MT-", "Mt-", "mt-"),
  mito_gene = NULL,
  verbose = TRUE,
  seed = 11
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{return_filtered}{Logical indicating whether to return a spot-filtered
Seurat object. Default is \code{FALSE}.}

\item{qc_metrics}{QC metrics to apply. Available metrics are \code{"outlier"},
\code{"umi"}, \code{"gene"}, and \code{"mito"}.}

\item{outlier_threshold}{Character vector specifying outlier thresholds as
\code{"metric:direction:nmads"}. Available default metrics are
\code{"log10_nCount"}, \code{"log10_nFeature"}, and \code{"spot_featurecount_dist"}.}

\item{outlier_n}{Minimum number of outlier metrics required to fail a spot.}

\item{UMI_threshold}{Minimum UMI count required to pass \code{"umi"} QC.}

\item{gene_threshold}{Minimum detected gene count required to pass \code{"gene"}
QC.}

\item{mito_threshold}{Maximum mitochondrial percentage allowed by \code{"mito"}
QC.}

\item{mito_pattern}{Regex patterns used to identify mitochondrial genes.}

\item{mito_gene}{Optional explicit mitochondrial gene vector. When provided,
\code{mito_pattern} is ignored.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A \code{Seurat} object with spot QC metadata columns.
}
\description{
Calculate common spot-level QC metrics for spatial transcriptomics data and
label failed spots without running single-cell-specific checks such as
doublet calling or ambient RNA decontamination.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- RunSpotQC(
  visium_human_pancreas_sub,
  assay = "Spatial"
)
SpatialSpotPlot(spatial, group.by = "SpotQC")
}
