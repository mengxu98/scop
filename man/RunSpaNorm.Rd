% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpaNorm.R
\name{RunSpaNorm}
\alias{RunSpaNorm}
\title{Run SpaNorm spatial normalization}
\usage{
RunSpaNorm(
  srt,
  assay = NULL,
  layer = "counts",
  image = NULL,
  coord.cols = c("col", "row"),
  new_assay = "SpaNorm",
  tool_name = "SpaNorm",
  store_results = TRUE,
  store_spe = FALSE,
  verbose = TRUE,
  coordinate_space = c("raw", "legacy_display"),
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{layer}{Assay layer used for expression values.}

\item{image}{Spatial image name. Required when multiple images are present;
a single image is selected automatically when \code{NULL}.}

\item{coord.cols}{Metadata coordinate columns used when no image is available.}

\item{new_assay}{Name of the assay used to store SpaNorm-normalized data.}

\item{tool_name}{Name used to store detailed SpaNorm results in \code{srt@tools}.}

\item{store_results}{Whether to store the full result in \code{srt@tools}.}

\item{store_spe}{Whether to store the backend \code{SpatialExperiment} returned
by \code{SpaNorm}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{coordinate_space}{Coordinate system supplied to SpaNorm. The default
is raw acquisition coordinates; use \code{"legacy_display"} explicitly to
reproduce the display-scaled coordinates used before scop 0.9.0.}

\item{...}{Additional arguments passed to \code{SpaNorm::SpaNorm()}, such as
\code{sample.p}.}
}
\value{
A \code{Seurat} object with SpaNorm-normalized expression stored in
\code{new_assay}. When \code{store_results = TRUE}, parameters, coordinates, features,
cells, and optional backend output are stored in \code{srt@tools[[tool_name]]}.
}
\description{
Normalize spatial transcriptomics counts with the optional Bioconductor
\code{SpaNorm} backend and store the normalized expression in a new Seurat assay.
}
\examples{
\dontrun{
data(visium_human_pancreas_sub)
keep_spots <- unique(round(seq(
  1,
  ncol(visium_human_pancreas_sub),
  length.out = 120
)))
spatial <- visium_human_pancreas_sub[, keep_spots]
spatial <- RunSpaNorm(
  spatial,
  assay = "Spatial",
  layer = "counts",
  coord.cols = c("x", "y"),
  new_assay = "SpaNorm",
  store_spe = FALSE,
  sample.p = 0.25,
  verbose = FALSE
)

SpatialSpotPlot(
  spatial,
  features = rownames(spatial[["SpaNorm"]])[1],
  assay = "SpaNorm",
  layer = "data",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
}
}
