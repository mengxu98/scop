% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunRCTD.R
\name{RunRCTD}
\alias{RunRCTD}
\title{Run RCTD spatial deconvolution}
\usage{
RunRCTD(
  srt,
  reference,
  reference_label = "celltype",
  assay = NULL,
  reference_assay = NULL,
  layer = "counts",
  reference_layer = "counts",
  features = NULL,
  image = NULL,
  coord.cols = c("x", "y"),
  rctd_mode = c("full", "multi", "doublet"),
  max_cores = 1,
  min_cells = 25,
  prefix = "RCTD",
  store_results = TRUE,
  round_counts = TRUE,
  create_rctd_params = list(),
  run_rctd_params = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Spatial \code{Seurat} object used as the RCTD query.}

\item{reference}{Reference \code{Seurat} object containing annotated single cells.}

\item{reference_label}{Metadata column in \code{reference} with cell type labels.}

\item{assay}{Assay used in \code{srt}. If \code{NULL}, the default assay is used.}

\item{reference_assay}{Assay used in \code{reference}.}

\item{layer, reference_layer}{Assay layers used for spatial and reference
raw counts.}

\item{features}{Features used for RCTD. If \code{NULL}, shared features are used.}

\item{image}{Name of the Seurat spatial image used to recover coordinates
when \code{coord.cols} are not available.}

\item{coord.cols}{Metadata coordinate columns used when no image coordinate
source is requested or available.}

\item{rctd_mode}{RCTD mode passed to \code{spacexr}. \code{"full"} is the default for
Visium spot deconvolution.}

\item{max_cores}{Number of cores passed to \code{spacexr}.}

\item{min_cells}{Minimum number of reference cells required for each cell
type. Old \code{spacexr} RCTD requires at least 25 cells per type.}

\item{prefix}{Prefix for metadata columns.}

\item{store_results}{Whether to store detailed RCTD results in \code{srt@tools}.}

\item{round_counts}{Whether to round non-integer counts to the nearest
integer before passing data to \code{spacexr}. RCTD requires integer count
matrices; this defaults to \code{TRUE} so bundled example data with scaled
non-integer reference counts can run directly.}

\item{create_rctd_params}{Additional parameters passed to
\code{spacexr::createRctd()} or \code{spacexr::create.RCTD()}.}

\item{run_rctd_params}{Additional parameters passed to
\code{spacexr::runRctd()} or \code{spacexr::run.RCTD()}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters passed to the RCTD run step.}
}
\value{
A \code{Seurat} object with RCTD proportion columns in metadata and
dominant cell type summaries. When \code{store_results = TRUE}, detailed results
are also stored in \code{srt@tools[["RCTD"]]}.
}
\description{
Estimate spot-level cell type proportions from a spatial \code{Seurat} object
using a single-cell \code{Seurat} reference and \code{spacexr} RCTD.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
rctd_weights <- data.frame(
  RCTD_prop_Ductal = seq(0.75, 0.15, length.out = ncol(spatial)),
  RCTD_prop_Endocrine = seq(0.15, 0.65, length.out = ncol(spatial)),
  RCTD_prop_Stromal = 0.10,
  row.names = colnames(spatial)
)
rctd_weights <- rctd_weights / rowSums(rctd_weights)
spatial <- Seurat::AddMetaData(spatial, rctd_weights)
spatial$RCTD_dominant_type <- sub(
  "^RCTD_prop_",
  "",
  colnames(rctd_weights)[max.col(rctd_weights)]
)
spatial$RCTD_max_prop <- apply(rctd_weights, 1, max)

SpatialSpotPlot(
  spatial,
  group.by = "RCTD_dominant_type",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
  SpatialSpotPlot(
    spatial,
    group.by = "RCTD_dominant_type",
    plot_type = "pie",
    overlay_image = FALSE,
    coord.cols = c("x", "y")
  )

data(pancreas_sub)
features_use <- head(intersect(rownames(spatial), rownames(pancreas_sub)), 300)

spatial <- RunRCTD(
  srt = spatial,
  reference = pancreas_sub,
  reference_label = "CellType",
  assay = "Spatial",
  reference_assay = "RNA",
  layer = "counts",
  reference_layer = "counts",
  features = features_use,
  rctd_mode = "full",
  max_cores = 1,
  min_cells = 5,
  prefix = "RCTD"
)

rctd_cols <- grep("^RCTD_prop_", colnames(spatial@meta.data), value = TRUE)
SpatialSpotPlot(
  spatial,
  group.by = "RCTD_dominant_type",
  overlay_image = FALSE,
  coord.cols = c("x", "y"),
  theme_use = "theme_scop"
)
SpatialSpotPlot(
  spatial,
  group.by = rctd_cols[1:min(3, length(rctd_cols))],
  palette = "Spectral",
  overlay_image = FALSE,
  coord.cols = c("x", "y"),
  theme_use = "theme_scop"
)
}
