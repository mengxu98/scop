% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialSketch.R
\name{RunSpatialSketch}
\alias{RunSpatialSketch}
\title{Cluster a spatial sketch and project labels to the full assay}
\usage{
RunSpatialSketch(
  object,
  assay = NULL,
  image = NULL,
  coord.cols = c("x", "y"),
  ncells = 50000L,
  nfeatures = 2000L,
  npcs = 30L,
  method = c("LeverageScore", "Uniform"),
  resolution = 0.6,
  prefix = "SpatialSketch",
  seed = 11L,
  max_dense_gb = 2,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object with one selected spatial assay/context.}

\item{assay, image, coord.cols}{See SpatialDataInfo.}

\item{ncells}{Maximum number of sampled observations (at least 3).}

\item{nfeatures}{Maximum number of variable genes.}

\item{npcs}{Number of PCA components; must be smaller than the sketch size
and number of usable features.}

\item{method}{Seurat sketch method: LeverageScore or Uniform.}

\item{resolution}{Seurat clustering resolution.}

\item{prefix}{Prefix for the new assay, reductions, graphs, metadata and tools.
Existing outputs with these names are rejected; choose a new prefix to rerun.}

\item{seed}{Random seed passed to sampling, PCA and clustering.}

\item{max_dense_gb}{Maximum estimated GB for the sketch scaled matrix alone.
This is a preflight guard, not a bound on total process memory.}

\item{verbose}{Whether to print progress.}
}
\value{
A Seurat object with prefix_cluster for sampled cells and
  prefix_projected for every cell in the selected full assay. The tools entry
  records selected cells, projected cells, source and exact output names.
}
\description{
Use Seurat's sketch/PCA/clustering/ProjectData path. The selected assay must
match the complete selected image context. Full counts are preserved; scaled
expression is created only for the sketch. The result records sampled cells
separately from projected labels. These are expression clusters, not inferred
spatial domains, and sketch sampling is not spatially stratified.
A single observed sketch cluster projects as the same constant label; this
explicit one-class case does not imply evidence for multiple populations.
}
\seealso{
ReadSpatialData, SpatialDataInfo
}
