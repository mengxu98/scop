% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunProportionTest.R
\name{RunProportionTestPermutation}
\alias{RunProportionTestPermutation}
\title{Permutation-based proportion test}
\usage{
RunProportionTestPermutation(
  srt,
  group.by,
  split.by,
  comparison = NULL,
  n_permutations = 1000,
  include_all_cells = FALSE,
  backend = c("cpp", "r"),
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{group.by}{Name of one or more meta.data columns to group (color) cells by.}

\item{split.by}{Name of a column in meta.data column to split plot by.
Default is \code{NULL}.}

\item{comparison}{Optional: specify comparisons to perform.}

\item{n_permutations}{Number of permutations for permutation-based test.
Also used as default bootstrap iterations in method fallbacks.}

\item{include_all_cells}{Whether to include all cell types in the complete grid
for permutation mode.}

\item{backend}{Permutation backend. \code{"cpp"} is the default and uses a native
permutation/bootstrap loop. \code{"r"} uses the original R implementation. The C++
backend is statistically equivalent but does not reproduce the exact random
draws from R's \code{sample()}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A method result bundle used internally by \link{RunProportionTest}.
}
\description{
Method-specific implementation used by \link{RunProportionTest} when
\code{proportion_method = "permutation"}.
This method is a permutation-based statistical test for compositional shift
rather than a dedicated biological model.
Bootstrap confidence intervals are reported as uncertainty estimates for
observed log2 fold-differences.
}
