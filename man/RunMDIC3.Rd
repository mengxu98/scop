% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMDIC3.R
\name{RunMDIC3}
\alias{RunMDIC3}
\alias{RunMDIC3.Seurat}
\alias{RunMDIC3.matrix}
\alias{RunMDIC3.default}
\title{Run MDIC3 cell-cell communication inference}
\usage{
RunMDIC3(object, ...)

\method{RunMDIC3}{Seurat}(
  object,
  group.by,
  grn = NULL,
  grn_method = c("grnboost2", "gniplr", "correlation"),
  grn_backend = c("cpp", "python"),
  assay = NULL,
  layer = "data",
  backend = c("cpp", "python"),
  envname = "scop_env",
  conda = "auto",
  verbose = TRUE,
  ...
)

\method{RunMDIC3}{matrix}(object, ...)

\method{RunMDIC3}{default}(
  object,
  labels,
  grn = NULL,
  grn_method = c("grnboost2", "gniplr", "correlation"),
  grn_backend = c("cpp", "python"),
  genes_in = c("rows", "columns"),
  backend = c("cpp", "python"),
  envname = "scop_env",
  conda = "auto",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A Seurat object or a gene-by-cell expression matrix.}

\item{...}{Additional arguments passed to [RunGRNBoost2()] or [RunGNIPLR()]
when those methods are selected through `grn_method`.}

\item{group.by}{Metadata column used as cell labels when `object` is a Seurat
object.}

\item{grn}{Gene-by-gene GRN matrix aligned to expression genes, or a data
frame with columns `TF`, `target`, and `importance`.}

\item{grn_method}{GRN inference method used when `grn = NULL`.
`"grnboost2"` calls [RunGRNBoost2()]; `"gniplr"` calls [RunGNIPLR()];
`"correlation"` uses absolute expression correlation as a lightweight GRN
approximation.}

\item{grn_backend}{Backend passed to [RunGRNBoost2()] when
`grn_method = "grnboost2"`.}

\item{assay}{Assay used when `object` is a Seurat object.}

\item{layer}{Assay layer used when `object` is a Seurat object.}

\item{backend}{Runtime backend for the MDIC3 scoring step. Supports `"cpp"`
and `"python"`.}

\item{envname}{Python environment used when `backend = "python"`.}

\item{conda}{Conda-compatible executable used when `backend = "python"`.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{labels}{Cell labels used when `object` is a matrix.}

\item{genes_in}{Matrix orientation for matrix inputs. `"rows"` means genes x
cells; `"columns"` means cells x genes.}
}
\value{
A Seurat object with results stored in `object@tools[["MDIC3"]]`, or
a list for matrix input.
}
\description{
Run MDIC3 cell-cell communication inference
}
\examples{
data(pancreas_sub)
expr <- GetAssayData5(
  pancreas_sub,
  assay = SeuratObject::DefaultAssay(pancreas_sub),
  layer = "counts"
)
expr <- as.matrix(expr[, seq_len(8)])
expr <- expr[
  names(sort(apply(expr, 1, stats::var), decreasing = TRUE))[seq_len(5)],
]
labels <- as.character(pancreas_sub$SubCellType[seq_len(8)])

mdic3 <- RunMDIC3(
  expr,
  labels = labels,
  grn_method = "gniplr",
  correlation_threshold = 0,
  lasso_degree = 1,
  max_lag = 1,
  max_edges_per_target = 2
)
mdic3$celltype_communication
}
