% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scTenifold.R
\name{RunscTenifoldNet}
\alias{RunscTenifoldNet}
\title{Run scTenifoldNet network comparison}
\usage{
RunscTenifoldNet(
  object,
  y = NULL,
  group.by = NULL,
  condition1 = NULL,
  condition2 = NULL,
  assay = NULL,
  layer = "counts",
  features = NULL,
  qc = TRUE,
  qc_min_library_size = 1000,
  qc_remove_outlier_cells = TRUE,
  qc_min_pct = 0.05,
  qc_max_mt_ratio = 0.1,
  nc_nNet = 10,
  nc_nCells = 500,
  nc_nComp = 3,
  nc_symmetric = FALSE,
  nc_scaleScores = TRUE,
  nc_q = 0.05,
  td_K = 3,
  td_nDecimal = 1,
  td_maxIter = 1000,
  td_maxError = 1e-05,
  ma_nDim = 30,
  cores = 1,
  store_networks = TRUE,
  store_manifold = TRUE,
  tool_name = "scTenifoldNet",
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{Seurat} object or a raw count matrix with genes in rows and
cells in columns.}

\item{y}{A second raw count matrix. Required when \code{object} is a matrix and
ignored when \code{object} is a \code{Seurat} object.}

\item{group.by}{Metadata column used to split a \code{Seurat} object into the two
conditions being compared.}

\item{condition1, condition2}{Condition labels from \code{group.by}. If omitted,
the first two group levels are used.}

\item{assay, layer}{Assay and layer used as the count matrix when \code{object} is
a \code{Seurat} object.}

\item{features}{Optional genes to retain before running the comparison.}

\item{qc}{Whether to apply scTenifoldNet-style quality control.}

\item{qc_min_library_size, qc_remove_outlier_cells, qc_min_pct, qc_max_mt_ratio}{Quality-control parameters forwarded to \code{scTenifoldNet::scQC()}.}

\item{nc_nNet, nc_nCells, nc_nComp, nc_symmetric, nc_scaleScores, nc_q}{Network
construction parameters forwarded to \code{scTenifoldNet::makeNetworks()}.}

\item{td_K, td_nDecimal, td_maxIter, td_maxError}{Tensor decomposition
parameters forwarded to \code{scTenifoldNet::tensorDecomposition()}.}

\item{ma_nDim}{Manifold-alignment dimension forwarded to
\code{scTenifoldNet::manifoldAlignment()}.}

\item{cores}{Number of cores forwarded to \code{scTenifoldNet::scTenifoldNet()}.}

\item{store_networks}{Whether to keep tensor networks in the stored result
when \code{object} is a \code{Seurat} object.}

\item{store_manifold}{Whether to keep manifold-alignment coordinates in the
stored result when \code{object} is a \code{Seurat} object.}

\item{tool_name}{Name of the \code{object@tools} entry when \code{object} is a \code{Seurat}
object.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A scTenifoldNet result list for matrix input, or a \code{Seurat} object
with results stored in \code{object@tools[[tool_name]]}.
}
\description{
Run scTenifoldNet network comparison
}
\examples{
data(pancreas_sub)
counts <- GetAssayData5(pancreas_sub, assay = "RNA", layer = "counts")
detected <- names(sort(Matrix::rowSums(counts > 0), decreasing = TRUE))
features_use <- head(detected, 300)

pancreas_sub <- RunscTenifoldNet(
  pancreas_sub,
  group.by = "CellType",
  condition1 = "Ductal",
  condition2 = "Endocrine",
  features = features_use,
  qc = FALSE,
  nc_nNet = 3,
  nc_nCells = 200,
  td_maxIter = 200,
  ma_nDim = 2,
  store_networks = FALSE,
  store_manifold = TRUE
)

dr <- pancreas_sub@tools$scTenifoldNet$diffRegulation
head(dr)

scTenifoldNetPlot(pancreas_sub, plot_type = "effect")
}
