% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CCCHeatmap.R
\name{CCCHeatmap}
\alias{CCCHeatmap}
\title{CCC heatmap and dot matrix plot}
\usage{
CCCHeatmap(
  srt,
  method = NULL,
  condition = NULL,
  dataset = 1,
  comparison = c(1, 2),
  plot_type = c("heatmap", "dot", "bubble", "ligand_target", "role_heatmap",
    "diff_heatmap"),
  display_by = c("aggregation", "interaction"),
  sender.use = NULL,
  receiver.use = NULL,
  ligand.use = NULL,
  receptor.use = NULL,
  interaction.use = NULL,
  signaling = NULL,
  pairLR.use = NULL,
  slot.name = "net",
  thresh = 0.05,
  measure = c("count", "weight"),
  pattern = c("outgoing", "incoming", "all"),
  value = "sum",
  top_n = 500,
  top_anno = "bar",
  right_anno = "cell",
  left_anno = "bar",
  bottom_anno = "cell",
  bar_value = "sum",
  add_text = NULL,
  cluster_rows = FALSE,
  cluster_columns = FALSE,
  color.by = c("score", "pvalue"),
  x_text_angle = 90,
  facet_by = NULL,
  show_row_names = TRUE,
  show_column_names = TRUE,
  edge_value = c("sum", "mean", "max", "count"),
  border = TRUE,
  value_palette = "RdBu",
  value_palcolor = NULL,
  cell_palette = "Chinese",
  cell_palcolor = NULL,
  palette = "Chinese",
  palcolor = NULL,
  width = NULL,
  height = NULL,
  units = "inch",
  title = NULL,
  subtitle = NULL,
  legend.position = "right",
  legend.direction = "vertical",
  font.size = 10,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{method}{Communication result type to use.}

\item{condition}{Result name or comparison name.}

\item{dataset}{Dataset index or name.}

\item{comparison}{Comparison indices or names.}

\item{plot_type}{Plot type. One of \code{"heatmap"} or \code{"dot"}.
\code{"bubble"} is a CellChat-specific interaction bubble matrix.
\code{"ligand_target"} is a special heatmap path available only with
\code{NicheNet}/\code{MultiNicheNet} results. \code{"role_heatmap"} and \code{"diff_heatmap"}
are CellChat-specific pathway role views.}

\item{display_by}{Whether to summarize by \code{"aggregation"} or \code{"interaction"}.}

\item{sender.use}{Sender cell types to keep.}

\item{receiver.use}{Receiver cell types to keep.}

\item{ligand.use}{Ligands to keep.}

\item{receptor.use}{Receptors to keep.}

\item{interaction.use}{Interaction names to keep.}

\item{signaling}{Signaling pathway to focus on.}

\item{pairLR.use}{Specific ligand-receptor pair(s) to keep.}

\item{slot.name}{CellChat slot name.}

\item{thresh}{Significance threshold used when extracting communication results.}

\item{measure}{Summary measure for CellChat objects.}

\item{pattern}{Pattern used for pathway role plots.}

\item{value}{Value column or summary statistic to use.}

\item{top_n}{Number of top records to retain.}

\item{top_anno, bottom_anno}{Column-side annotations for sender groups.
Each side accepts \code{NULL}, \code{"bar"}, \code{"box"}, \code{"point"}, \code{"line"},
\code{"histogram"}, \code{"density"}, \code{"violin"}, \code{"cell"}, or a character vector
containing multiple values.
Defaults are \code{top_anno = "bar"} and \code{bottom_anno = "cell"}.}

\item{left_anno, right_anno}{Row-side annotations for receiver groups.
Each side accepts \code{NULL}, \code{"bar"}, \code{"box"}, \code{"point"}, \code{"line"},
\code{"histogram"}, \code{"density"}, \code{"violin"}, \code{"cell"}, or a character vector
containing multiple values.
Defaults are \code{left_anno = "bar"} and \code{right_anno = "cell"}.}

\item{bar_value}{Aggregation metric shown in the bar annotations.
One or more of \code{"count"}, \code{"sum"}, \code{"mean"}, or \code{"max"}.
Multiple values add multiple annotation tracks. Default \code{"sum"}.}

\item{add_text}{Logical. Show numeric value labels inside each cell (heatmap mode only).
Default \code{TRUE} for aggregation mode, \code{FALSE} for interaction mode.}

\item{cluster_rows, cluster_columns}{Whether to cluster heatmap rows/columns.
Defaults are both \code{FALSE}.}

\item{x_text_angle}{Rotation angle for x-axis labels.}

\item{facet_by}{Faceting variable for interaction-level plots.}

\item{edge_value}{Aggregation statistic for network edges.}

\item{border}{Logical. Whether to draw borders for the heatmap body and all
annotation tracks. Default \code{TRUE}.}

\item{value_palette}{Palette used for heatmap value fills.}

\item{value_palcolor}{Optional custom colors for \code{value_palette}.}

\item{cell_palette}{Cell annotation palette name.}

\item{cell_palcolor}{Custom cell annotation colors.}

\item{palette}{Main palette name.}

\item{palcolor}{Main custom palette colors.}

\item{width, height}{Optional heatmap body width and height. When only one is
supplied, the other is inferred from the matrix dimensions to keep cells
square. When both are \code{NULL}, a square-cell size is computed automatically.}

\item{units}{Units for \code{width} and \code{height}. Default \code{"inch"}.}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{legend.position}{Legend position.}

\item{legend.direction}{Legend direction.}

\item{font.size}{Base font size.}

\item{theme_use}{Theme function used for styling.}

\item{theme_args}{Arguments passed to the theme function.}

\item{verbose}{Whether to print messages.}

\item{...}{Additional plot-specific options.}
}
\value{
A ggplot / patchwork object wrapping the ComplexHeatmap grob.
}
\description{
CCC heatmap and dot matrix plot
}
\examples{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)

pc1 <- Seurat::Embeddings(pancreas_sub, "Standardpca")[, 1]
ct <- as.character(pancreas_sub$CellType)
ct_medians <- tapply(pc1, ct, median)
pancreas_sub$Condition <- ifelse(
  pc1 > ct_medians[ct],
  "ConditionA",
  "ConditionB"
)

pancreas_sub <- RunCellChat(
  pancreas_sub,
  group.by = "CellType",
  group_column = "Condition",
  group_cmp = list(c("ConditionA", "ConditionB")),
  species = "Mus_musculus"
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "dot",
  display_by = "aggregation",
  top_n = 20
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "dot",
  display_by = "interaction",
  facet_by = "sender",
  top_n = 20
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "dot",
  display_by = "interaction",
  facet_by = "receiver",
  top_n = 20
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "heatmap",
  display_by = "aggregation",
  top_n = 20
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "heatmap",
  display_by = "interaction",
  facet_by = "sender",
  top_n = 10
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "heatmap",
  display_by = "interaction",
  facet_by = "sender",
  color.by = "pvalue",
  top_n = 10
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "bubble",
  top_n = 5
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA_vs_ConditionB",
  plot_type = "bubble",
  top_n = 5
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "role_heatmap",
  #' pattern = "outgoing",
  width = 0.6,
  height = 2.5
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA",
  plot_type = "role_heatmap",
  pattern = "outgoing",
  width = 0.6,
  height = 2.5
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA_vs_ConditionB",
  plot_type = "role_heatmap",
  pattern = "incoming",
  palette = "Paired",
  width = 0.6,
  height = 3.5
)

CCCHeatmap(
  pancreas_sub,
  method = "CellChat",
  condition = "ConditionA_vs_ConditionB",
  plot_type = "diff_heatmap",
  pattern = "all",
  palette = "Paired",
  top_n = 20,
  width = 0.6,
  height = 3.5
)
}
