% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunDeconvolution.R
\name{RunDeconvolution}
\alias{RunDeconvolution}
\alias{RunDeconvolution.SummarizedExperiment}
\title{Run bulk or pseudobulk deconvolution}
\usage{
RunDeconvolution(object, ...)

\method{RunDeconvolution}{SummarizedExperiment}(
  object,
  reference = NULL,
  method = c("MuSiC", "BisqueRNA", "BayesPrism", "CIBERSORT"),
  group.by = NULL,
  sample.by = NULL,
  cellstate.by = NULL,
  bulk_assay = "counts",
  ref_assay = NULL,
  ref_layer = "counts",
  backend = c("cpp", "r"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{SummarizedExperiment} object containing bulk-like counts.}

\item{...}{Additional parameters forwarded to the internal deconvolution
backend.}

\item{reference}{A \code{Seurat} reference object used to build cell-type
profiles. Not required for \code{"CIBERSORT"}.}

\item{method}{Deconvolution method. One of \code{"MuSiC"}, \code{"BisqueRNA"},
\code{"BayesPrism"}, or \code{"CIBERSORT"}.}

\item{group.by}{Metadata column in \code{reference} defining reference cell
types.}

\item{sample.by}{Metadata column in \code{reference} defining biological
sample / donor IDs. Used by the \code{r} backends of \code{MuSiC} and \code{BisqueRNA}. If
\code{NULL}, SCOP will try to infer a suitable column automatically.}

\item{cellstate.by}{Metadata column in \code{reference} defining cell states for
the \code{r} backend of \code{BayesPrism}. If \code{NULL}, \code{group.by} is reused.}

\item{bulk_assay}{Assay name in \code{object} used as the bulk counts matrix.}

\item{ref_assay}{Assay name in \code{reference} used for the reference profiles.}

\item{ref_layer}{Layer name in \code{reference} used for reference counts.}

\item{backend}{Deconvolution engine backend. \code{"r"} uses the original method
package implementation. \code{"cpp"} is reserved for native SCOP implementations
when available.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{SummarizedExperiment} object with results stored in
\code{S4Vectors::metadata(object)[["Deconvolution"]]}.
}
\description{
Estimate cell-type proportions from a bulk-like expression matrix stored in a
\code{SummarizedExperiment} object, using a \code{Seurat} reference.
}
\examples{
data(islet_bulk)
islet_bulk <- RunDeconvolution(
  islet_bulk,
  method = "CIBERSORT",
  backend = "cpp",
  perm = 0
)
DeconvolutionPlot(islet_bulk, plot_type = "bar")

DeconvolutionPlot(
  islet_bulk,
  plot_type = "heatmap",
  sample_annotation = "condition",
  sample_split = "condition"
)

DeconvolutionPlot(islet_bulk, plot_type = "box")
}
\seealso{
\link{DeconvolutionPlot}
}
\concept{spatial-producer}
