% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CNVPlot.R
\name{CNVPlot}
\alias{CNVPlot}
\title{Plot SCOP CNV results}
\usage{
CNVPlot(
  srt,
  plot_type = c("heatmap", "dim", "spatial", "bar", "tree"),
  tool_name = "CNV",
  method = NULL,
  group.by = NULL,
  reduction = NULL,
  value = NULL,
  cluster_tree_by = c("cluster", "cell"),
  palette = "Chinese",
  palcolor = NULL,
  heatmap_palette = "RdBu",
  heatmap_palcolor = NULL,
  title = NULL,
  subtitle = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  ...
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{plot_type}{Plot type. \code{"heatmap"} draws a chromosome-ordered CNV
heatmap, \code{"dim"} shows CNV metadata on a reduction, \code{"spatial"} shows CNV
metadata on spatial coordinates, \code{"bar"} summarizes calls by group, and
\code{"tree"} returns a hierarchical clustering of cells or CNV clusters.}

\item{tool_name}{Name of the CNV tool bundle in \code{srt@tools}.}

\item{method}{Method stored under \code{srt@tools[[tool_name]]$methods}. If
\code{NULL}, the active method is used.}

\item{group.by}{Name of one or more meta.data columns to group (color) cells by.}

\item{reduction}{Which dimensionality reduction to use.
If not specified, will use the reduction returned by \link{DefaultReduction}.}

\item{value}{Metadata value used by \code{"dim"} and \code{"spatial"} plots. If
\code{NULL}, \code{CNV_prediction} is preferred when available, otherwise \code{CNV_score}.}

\item{cluster_tree_by}{For \code{plot_type = "tree"}, use \code{"cell"} or
\code{"cluster"} level profiles.}

\item{palette, palcolor}{Palette passed to SCOP discrete plots and
annotations.}

\item{heatmap_palette, heatmap_palcolor}{Palette used for CNV heatmap values.}

\item{title, subtitle}{Plot title and subtitle for ggplot-based views.}

\item{theme_use}{Theme used. Can be a character string or a theme function.
Default is \code{"theme_scop"}.}

\item{theme_args}{Other arguments passed to the \code{theme_use}.
Default is \code{list()}.}

\item{...}{Additional parameters forwarded to the underlying plotting
function.}
}
\value{
A plot object. For \code{"heatmap"}, returns a \code{ComplexHeatmap} object;
for \code{"tree"}, returns an \code{hclust} object.
}
\description{
Visualize results generated by \code{\link[=RunCNV]{RunCNV()}} from the unified
\code{srt@tools[[tool_name]]} schema.
}
\examples{
\dontrun{
srt <- RunCNV(srt, method = "copykat", genome = "hg38")

CNVPlot(srt, plot_type = "heatmap", group.by = "CNV_prediction")
CNVPlot(srt, plot_type = "dim", value = "CNV_prediction")
CNVPlot(srt, plot_type = "dim", value = "CNV_score")
CNVPlot(srt, plot_type = "bar", group.by = "seurat_clusters")
CNVPlot(srt, plot_type = "tree", cluster_tree_by = "cell")
}
}
\seealso{
\link{RunCNV}
}
