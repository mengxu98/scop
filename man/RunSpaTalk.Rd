% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpaTalk.R
\name{RunSpaTalk}
\alias{RunSpaTalk}
\title{Run official SpaTalk spatial communication analysis}
\usage{
RunSpaTalk(
  srt,
  group.by,
  mode = c("auto", "single_cell", "spot"),
  reference = NULL,
  reference.group.by = NULL,
  species = c("Human", "Mouse"),
  assay = NULL,
  layer = "counts",
  reference.assay = NULL,
  reference.layer = "counts",
  image = NULL,
  coord.cols = c("col", "row"),
  deconvolution = c("NNLM", "none"),
  result.name = "default",
  store.object = c("minimal", "full"),
  overwrite = FALSE,
  backend = c("cpp", "r"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A `Seurat` spatial object.}

\item{group.by}{Metadata column containing cell labels for single-cell mode.}

\item{mode}{Analysis mode. `"auto"` selects spot mode when `reference` is
supplied and single-cell mode otherwise.}

\item{reference}{Optional single-cell `Seurat` reference for spot mode.}

\item{reference.group.by}{Cell-type column in `reference`.}

\item{species}{SpaTalk species database.}

\item{assay, layer}{Spatial assay and raw-count layer.}

\item{reference.assay, reference.layer}{Reference assay and raw-count layer.}

\item{image}{Explicit spatial image. Required for multi-image objects.}

\item{coord.cols}{Metadata coordinate columns used when no image is present.}

\item{deconvolution}{Spot deconvolution mode. `"NNLM"` runs SpaTalk method 1;
`"none"` requires a named `dec_result` matrix in `...`.}

\item{result.name}{Stored result name.}

\item{store.object}{Whether to store the full upstream SpaTalk object.}

\item{overwrite}{Whether to replace an existing named result.}

\item{backend}{Backend used only for SCOP CCC result aggregation.}

\item{verbose}{Whether to print progress messages.}

\item{...}{Named arguments forwarded to compatible official SpaTalk steps.}
}
\value{
The input `Seurat` object with `srt@tools$SpaTalk` and unified CCC
  results updated.
}
\description{
Run the official SpaTalk R backend for single-cell spatial data or spot data
with a single-cell reference. Results are stored as a compact method bundle
and added to SCOP's unified CCC table.
}
\examples{
\dontrun{
available <- unname(unlist(thisutils::check_r(
  c("linxihui/NNLM", "ZJUFanLab/SpaTalk"), verbose = FALSE
)))
if (length(available) == 2L && all(available)) {
  spatial <- RunSpaTalk(
    spatial,
    group.by = "celltype",
    mode = "single_cell",
    image = "slice1"
  )
}
}
}
\references{
<https://github.com/ZJUFanLab/SpaTalk>
}
