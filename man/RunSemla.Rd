% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSemla.R
\name{RunSemlaSpatialNetwork}
\alias{RunSemlaSpatialNetwork}
\alias{RunSemlaLocalG}
\alias{RunSemlaRegionNeighbors}
\alias{RunSemlaRadialDistance}
\title{Run semla spatial analysis backends}
\usage{
RunSemlaSpatialNetwork(
  srt,
  image_type = "tissue_lowres",
  nNeighbors = 6,
  maxDist = NULL,
  minK = 0,
  coords = "pixels",
  tool_name = "SemlaSpatialNetwork",
  store_results = TRUE,
  verbose = TRUE,
  ...
)

RunSemlaLocalG(
  srt,
  features,
  alternative = NULL,
  store_in_metadata = TRUE,
  assay_name = "GiScores",
  image_type = "tissue_lowres",
  verbose = TRUE,
  ...
)

RunSemlaRegionNeighbors(
  srt,
  column_name,
  column_labels = NULL,
  mode = "outer",
  column_key = NULL,
  image_type = "tissue_lowres",
  verbose = TRUE,
  ...
)

RunSemlaRadialDistance(
  srt,
  column_name,
  selected_groups = NULL,
  column_suffix = NULL,
  image_type = "tissue_lowres",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object with spatial image data.}

\item{image_type}{Image scale used by \code{semla::UpdateSeuratForSemla()} when
the object does not already contain a Staffli object.}

\item{nNeighbors}{Number of nearest spatial neighbors.}

\item{maxDist}{Optional maximum neighbor distance.}

\item{minK}{Minimum number of retained neighbors per spot.}

\item{coords}{Coordinate system passed to \code{semla::GetSpatialNetwork()}.}

\item{tool_name}{Name used to store results in \code{srt@tools}.}

\item{store_results}{Whether to store the semla spatial network in
\code{srt@tools}.}

\item{verbose}{Whether to print progress messages.}

\item{...}{Additional arguments passed to semla.}

\item{features}{Features passed to \code{semla::RunLocalG()}.}

\item{alternative}{Alternative hypothesis passed to semla. Use \code{NULL} to
keep semla's default behavior.}

\item{store_in_metadata}{Whether semla should store results in metadata.}

\item{assay_name}{Assay name used by semla when \code{store_in_metadata = FALSE}.}

\item{column_name}{Metadata column containing labels or region labels.}

\item{column_labels}{Labels to find neighbors for. If \code{NULL}, semla uses all
labels in \code{column_name}.}

\item{mode}{Neighbor selection mode passed to semla.}

\item{column_key}{Prefix for metadata columns returned by semla.}

\item{selected_groups}{Region labels used by semla. If \code{NULL}, semla uses all
labels in \code{column_name}.}

\item{column_suffix}{Optional suffix for metadata columns returned by semla.}
}
\value{
A \code{Seurat} object.
}
\description{
Lightweight wrappers around optional \code{semla} spatial analysis methods.
\code{RunSemlaSpatialNetwork()} stores the network in \code{srt@tools};
\code{RunSemlaLocalG()}, \code{RunSemlaRegionNeighbors()}, and
\code{RunSemlaRadialDistance()} delegate result placement to semla.
}
