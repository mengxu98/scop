% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMistyR.R
\name{RunMistyR}
\alias{RunMistyR}
\title{Run mistyR multiview spatial modeling}
\usage{
RunMistyR(
  srt,
  assay = NULL,
  layer = "data",
  features = NULL,
  image = NULL,
  coord.cols = c("col", "row"),
  views = "para",
  para_l = 10,
  para_zoi = 0,
  para_family = c("gaussian", "exponential", "linear", "constant"),
  para_approx = 1,
  para_nn = NULL,
  juxta_neighbor_thr = 15,
  view_cached = FALSE,
  results_folder = NULL,
  seed = 42,
  target_subset = NULL,
  bypass_intra = FALSE,
  cv_folds = 10,
  model_cached = FALSE,
  append = FALSE,
  tool_name = "MistyR",
  store_results = TRUE,
  store_views = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay used for expression. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used for expression values.}

\item{features}{Features used by MISTy. If \code{NULL}, variable features are used
when available; otherwise all assay features are used.}

\item{image}{Name of the Seurat spatial image. If \code{NULL}, the first image is
used when present.}

\item{coord.cols}{Metadata coordinate columns used when no Seurat image
coordinates are available.}

\item{views}{Spatial views to add besides the required intraview. One or both
of \code{"para"} and \code{"juxta"}.}

\item{para_l, para_zoi, para_family, para_approx, para_nn}{Parameters passed to
\code{mistyR::add_paraview()}.}

\item{juxta_neighbor_thr}{Neighbor threshold passed to
\code{mistyR::add_juxtaview()}.}

\item{view_cached}{Whether generated mistyR views should use cache.}

\item{results_folder}{Folder passed to \code{mistyR::run_misty()}. If \code{NULL}, a
temporary folder is used.}

\item{seed, target_subset, bypass_intra, cv_folds, model_cached, append}{Parameters
passed to \code{mistyR::run_misty()}.}

\item{tool_name}{Name used to store results in \code{srt@tools}.}

\item{store_results}{Whether to store results in \code{srt@tools}.}

\item{store_views}{Whether to store the mistyR view composition in
\code{srt@tools}. This can be large.}

\item{verbose}{Whether to print progress messages.}

\item{...}{Additional named arguments passed to \code{mistyR::run_misty()}.}
}
\value{
A \code{Seurat} object with results stored in \code{srt@tools[[tool_name]]}
when \code{store_results = TRUE}.
}
\description{
Build a small \code{mistyR} view composition from a spatial \code{Seurat} object,
train MISTy models, collect results, and store a SCOP-style result bundle in
\code{srt@tools}. The intraview is always created from the selected assay layer;
optional juxtaview and paraview components describe local and broader spatial
context. \code{mistyR} is an optional Bioconductor dependency installable with
\code{BiocManager::install("mistyR")}.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- subset(
  visium_human_pancreas_sub,
  cells = colnames(visium_human_pancreas_sub)[1:80],
  features = rownames(visium_human_pancreas_sub)[1:40]
)
spatial <- Seurat::NormalizeData(spatial, assay = "Spatial", verbose = FALSE)

if (
  requireNamespace("mistyR", quietly = TRUE) &&
    identical(Sys.getenv("SCOP_RUN_SPATIAL_BACKEND_EXAMPLES"), "true")
) {
  spatial <- RunMistyR(
    spatial,
    assay = "Spatial",
    layer = "data",
    features = rownames(spatial)[1:10],
    coord.cols = c("x", "y"),
    views = "para",
    para_l = 5,
    cv_folds = 3,
    verbose = FALSE
  )
  spatial@tools$MistyR$summary
}
}
