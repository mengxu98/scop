% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMistyR.R
\name{RunMistyR}
\alias{RunMistyR}
\title{Run mistyR multiview spatial modeling}
\usage{
RunMistyR(
  object,
  assay = NULL,
  layer = "data",
  features = NULL,
  image = NULL,
  coord.cols = c("col", "row"),
  views = "para",
  para_l = 10,
  para_zoi = 0,
  para_family = c("gaussian", "exponential", "linear", "constant"),
  para_approx = 1,
  para_nn = NULL,
  juxta_neighbor_thr = 15,
  view_cached = FALSE,
  results_folder = NULL,
  seed = 42,
  target_subset = NULL,
  bypass_intra = FALSE,
  cv_folds = 10,
  model_cached = FALSE,
  append = FALSE,
  tool_name = "MistyR",
  store_results = TRUE,
  store_views = FALSE,
  verbose = TRUE,
  coordinate_space = c("raw", "legacy_display"),
  ...,
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{assay}{Assay used for expression. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used for expression values.}

\item{features}{Features used by MISTy. If \code{NULL}, variable features are used
when available; otherwise all assay features are used.}

\item{image}{Name of the Seurat spatial image. Required when multiple images
are present; a single image is selected automatically when \code{NULL}.}

\item{coord.cols}{Metadata coordinate columns used when no Seurat image
coordinates are available.}

\item{views}{Spatial views to add besides the required intraview. One or both
of \code{"para"} and \code{"juxta"}.}

\item{para_l, para_zoi, para_family, para_approx, para_nn}{Parameters passed to
\code{mistyR::add_paraview()}. \code{para_l} and \code{para_zoi} use the selected coordinate
units; \code{para_nn} is a unitless neighbor count.}

\item{juxta_neighbor_thr}{Neighbor threshold passed to
\code{mistyR::add_juxtaview()}, expressed in the selected coordinate units.}

\item{view_cached}{Whether generated mistyR views should use cache.}

\item{results_folder}{Folder passed to \code{mistyR::run_misty()}. If \code{NULL}, a
temporary folder is used.}

\item{seed, target_subset, bypass_intra, cv_folds, model_cached, append}{Parameters passed to \code{mistyR::run_misty()}.}

\item{tool_name}{Name used to store results in \code{srt@tools}.}

\item{store_results}{Whether to store results in \code{srt@tools}.}

\item{store_views}{Whether to store the mistyR view composition in
\code{srt@tools}. This can be large.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{coordinate_space}{Coordinate system used to build MISTy views. The
default is raw acquisition coordinates; \code{"legacy_display"} remains an
explicit compatibility option.}

\item{...}{Additional named arguments passed to \code{mistyR::run_misty()}.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\value{
A \code{Seurat} object with results stored in \code{srt@tools[[tool_name]]}
when \code{store_results = TRUE}.
}
\description{
Model feature expression using MISTy intraview and spatial predictors.
Juxtaview and paraview predictors describe local and broader neighborhoods.
}
\examples{
\dontrun{
data(visium_human_pancreas_sub)
spatial <- Seurat::NormalizeData(visium_human_pancreas_sub, verbose = FALSE)
# Paraview bandwidth: 1000 full-resolution image pixels.
spatial <- RunMistyR(
  spatial,
  assay = "Spatial", features = rownames(spatial)[1:5],
  image = "slice1", views = "para", para_l = 1000,
  cv_folds = 3, verbose = FALSE
)
MistyRPlot(spatial, type = "improvements", top_n = 5)
}
}
