% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SimulateProportionData.R
\name{SimulateProportionData}
\alias{SimulateProportionData}
\title{Simulate Single-Cell Data for Proportion Testing}
\usage{
SimulateProportionData(
  n_genes = 2000L,
  samples_per_condition = 5L,
  cells_per_sample = 350L,
  conditions = c("Control", "Treatment"),
  cell_types = c("Tcell", "Bcell", "Myeloid", "NK", "DC", "Mono", "Epithelial", "Fibro",
    "Mast"),
  base_props = c(0.2, 0.14, 0.16, 0.1, 0.08, 0.1, 0.12, 0.07, 0.03),
  condition_prop_shift = c(0.07, -0.03, -0.03, -0.01, -0.01, -0.01, 0.01, 0, 0.01),
  condition_prop_matrix = NULL,
  prop_concentration = 35,
  marker_genes_per_type = 30L,
  marker_fc = 4,
  condition_de_genes = 100L,
  condition_de_fc = 1.5,
  dispersion = 2,
  sample_prefix = "S",
  batch_levels = c("Batch1", "Batch2", "Batch3"),
  assay = "RNA",
  seed = 11,
  verbose = TRUE
)
}
\arguments{
\item{n_genes}{Number of genes to simulate.}

\item{samples_per_condition}{Number of biological samples per condition.}

\item{cells_per_sample}{Number of cells per sample.}

\item{conditions}{Condition labels.
Default is \code{c("Control", "Treatment")}.}

\item{cell_types}{Cell type labels.}

\item{base_props}{Baseline cell-type proportions for the first condition.
Must have the same length as \code{cell_types}.}

\item{condition_prop_shift}{Additive shift applied to \code{base_props} for the
second condition when \code{condition_prop_matrix = NULL}.}

\item{condition_prop_matrix}{Optional matrix of condition-specific proportions.
Rows correspond to \code{conditions}, columns correspond to \code{cell_types}.
When provided, \code{base_props} and \code{condition_prop_shift} are ignored.}

\item{prop_concentration}{Dirichlet concentration for sample-level proportion
variability. Higher values produce less variability among samples.}

\item{marker_genes_per_type}{Number of marker genes for each cell type.}

\item{marker_fc}{Fold-change multiplier for marker genes.}

\item{condition_de_genes}{Number of genes with global condition-level effect
(for non-reference conditions).}

\item{condition_de_fc}{Fold-change multiplier for global condition-level genes.}

\item{dispersion}{Negative binomial size parameter.}

\item{sample_prefix}{Prefix for simulated sample IDs.}

\item{batch_levels}{Batch labels assigned to samples in a round-robin manner.}

\item{assay}{Assay name used in \link[Seurat:reexports]{Seurat::CreateSeuratObject}.}

\item{seed}{Random seed.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with:
\itemize{
\item metadata columns: \code{Sample}, \code{Condition}, \code{Batch}, \code{CellType}, \code{SubCellType}, \code{Phase}
\item simulation metadata stored in \code{srt@tools[["SimulateProportionData"]]}
}
}
\description{
Generate a synthetic \code{Seurat} object with sample-level replicates, condition
labels, and controllable cell-type composition shifts.
The output is designed to be directly compatible with common \code{scop} workflows,
especially \link{RunProportionTest}.
Default settings provide richer cell-type diversity (including a rare group),
multiple batches, and stronger sample-level heterogeneity for DA regression.
}
\examples{
\dontrun{
srt_sim <- SimulateProportionData(
  n_genes = 1500,
  samples_per_condition = 4,
  cells_per_sample = 250,
  seed = 42
)

# Compatible with proportion testing
srt_sim <- RunProportionTest(
  srt_sim,
  group.by = "CellType",
  split.by = "Condition",
  sample.by = "Sample",
  proportion_method = "propeller",
  comparison = list(c("Control", "Treatment"))
)
}
}
