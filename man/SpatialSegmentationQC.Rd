% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpatialInput.R
\name{SpatialSegmentationQC}
\alias{SpatialSegmentationQC}
\title{Inspect segmentation area and transcript counts per cell}
\usage{
SpatialSegmentationQC(
  object,
  assay = NULL,
  image = NULL,
  coord.cols = c("x", "y"),
  boundaries = NULL,
  min_counts = NULL,
  area_range = NULL
)
}
\arguments{
\item{object}{A Seurat object.}

\item{assay, image, coord.cols}{See SpatialDataInfo.}

\item{boundaries}{Optional real boundary table, as accepted by SpatialCellPlot.}

\item{min_counts}{Minimum transcripts; NULL does not evaluate this criterion.}

\item{area_range}{Optional finite c(min, max) area thresholds in squared raw units.}
}
\value{
A data.frame of cell IDs, counts, detected genes, area, boundary status,
  and QC status. Attributes retain coordinate source and parameters. This is
  descriptive QC; it does not detect every segmentation error.
  Vendor boundary polygons may simplify the true segmentation masks; area
  refers to the supplied polygon representation.
}
\description{
Return descriptive cell-level QC without filtering or changing segmentation.
Missing boundaries are retained as not_evaluated. Polygon rings are validated
using the same boundary contract as SpatialCellPlot. Areas use squared raw
coordinate units, not necessarily square microns. No universal area cutoff
is imposed; thresholds must be chosen for the acquisition and tissue.
}
