% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunBayesSpace.R
\name{RunBayesSpace}
\alias{RunBayesSpace}
\title{Run BayesSpace spatial clustering}
\usage{
RunBayesSpace(
  srt,
  q,
  assay = NULL,
  platform = c("Visium", "VisiumHD", "ST"),
  image = NULL,
  use_reduction = NULL,
  dims = 1:15,
  preprocess = TRUE,
  n.PCs = 15,
  n.HVGs = 2000,
  skip.PCA = !is.null(use_reduction),
  spatial_preprocess_params = list(),
  spatial_cluster_params = list(),
  cluster_colname = "BayesSpace_cluster",
  init_colname = "BayesSpace_init",
  store_sce = TRUE,
  verbose = TRUE,
  coord.cols = c("col", "row")
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{q}{Number of BayesSpace clusters.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{platform}{Spatial sequencing platform.}

\item{image}{Name of the Seurat spatial image used to recover spot
coordinates when they are not already present in metadata. For regular
Visium data with only pixel \code{x}/\code{y} coordinates, BayesSpace array
coordinates are inferred from the spatial grid.}

\item{use_reduction}{Optional Seurat reduction to pass to BayesSpace as PCA.}

\item{dims}{Dimensions from \code{use_reduction} to use.}

\item{preprocess}{Whether to run \code{BayesSpace::spatialPreprocess()}.}

\item{n.PCs, n.HVGs}{Parameters passed to \code{spatialPreprocess()}.}

\item{skip.PCA}{Whether to skip PCA inside \code{spatialPreprocess()}.}

\item{spatial_preprocess_params}{Additional parameters passed to
\code{BayesSpace::spatialPreprocess()}.}

\item{spatial_cluster_params}{Additional parameters passed to
\code{BayesSpace::spatialCluster()}.}

\item{cluster_colname}{Metadata column used for BayesSpace clusters.}

\item{init_colname}{Metadata column used for BayesSpace initial clusters.}

\item{store_sce}{Whether to store the BayesSpace \code{SingleCellExperiment}
in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{coord.cols}{Two metadata columns containing raw x/y coordinates when
no spatial image is available.}
}
\value{
A \code{Seurat} object with BayesSpace clusters in metadata and raw
results in \code{srt@tools[["BayesSpace"]]}.
}
\description{
Run BayesSpace spatial clustering
}
\examples{
data(visium_human_pancreas_results_sub)
spatial <- RunBayesSpace(
  visium_human_pancreas_results_sub,
  q = 3,
  n.PCs = 5,
  n.HVGs = 200,
  store_sce = FALSE,
  spatial_cluster_params = list(nrep = 20, burn.in = 10, thin = 5),
  coord.cols = c("x", "y"),
  verbose = FALSE
)
SpatialSpotPlot(
  spatial,
  group.by = "BayesSpace_cluster",
  overlay_image = FALSE,
  coord.cols = c("x", "y"),
  pt.size = 1.5
)
}
