% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataConvert.R
\name{srt_to_adata}
\alias{srt_to_adata}
\title{Convert a Seurat object to an AnnData object}
\usage{
srt_to_adata(
  object,
  features = NULL,
  assay_x = "RNA",
  layer_x = "counts",
  assay_y = c("spliced", "unspliced"),
  layer_y = "counts",
  reductions = NULL,
  graphs = NULL,
  neighbors = NULL,
  convert_tools = FALSE,
  convert_misc = FALSE,
  prepare_env = TRUE,
  verbose = TRUE,
  srt = NULL
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{features}{Optional vector of features to include in the anndata object.
Default is all features in \code{assay_x}.}

\item{assay_x}{Assay to convert as the main data matrix in the anndata object.}

\item{layer_x}{Layer name for assay_x in the Seurat object.}

\item{assay_y}{Assays, or extra layers of \code{assay_x}, to convert as layers in
the anndata object. Each name is matched against the assays of the object
first, then against the layers of \code{assay_x}, which covers velocity matrices
such as \code{spliced} and \code{unspliced} stored as layers of a Seurat v5 \code{RNA}
assay.}

\item{layer_y}{Layer names for the assays in \code{assay_y}. It is ignored for
names that are matched as layers of \code{assay_x}, where the layer name itself is
used.}

\item{reductions}{Character vector specifying which Seurat reductions to
convert into \code{obsm}. Default is \code{NULL}, which converts all available
reductions.}

\item{graphs}{Character vector specifying which Seurat graphs to convert into
\code{obsp}. Default is \code{NULL}, which converts all available graphs.}

\item{neighbors}{Character vector specifying which Seurat neighbor objects to
convert into \code{obsp}. Default is \code{NULL}, which converts all available neighbor
objects.}

\item{convert_tools}{Whether to convert the tool-specific data.}

\item{convert_misc}{Whether to convert the miscellaneous data.}

\item{prepare_env}{Whether to prepare and validate the Scanpy Python
environment before conversion. Wrapper functions that already prepared the
environment should pass \code{FALSE} to keep one reticulate binding.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{srt}{Deprecated alias for \code{object}; supply exactly one of the two. It
will be removed in scop 1.0.0.}
}
\value{
A \code{anndata} object.
}
\description{
Convert a Seurat object to an AnnData object
}
\examples{
\dontrun{
data(pancreas_sub)
adata <- srt_to_adata(pancreas_sub)
adata

# Or save as a h5ad/loom file
adata$write_h5ad(
  "pancreas_sub.h5ad"
)
adata$write_loom(
  "pancreas_sub.loom",
  write_obsm_varm = TRUE
)
}
}
\seealso{
\link{adata_to_srt}
}
