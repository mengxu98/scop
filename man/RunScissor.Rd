% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunScissor.R
\name{RunScissor}
\alias{RunScissor}
\title{Run Scissor phenotype-associated cell selection}
\usage{
RunScissor(
  srt,
  bulk_dataset,
  phenotype = NULL,
  condition.by = NULL,
  positive = NULL,
  assay = NULL,
  bulk_assay = "counts",
  layer = "counts",
  features = NULL,
  family = c("gaussian", "binomial", "cox"),
  backend = c("cpp", "r"),
  alpha = NULL,
  cutoff = 0.2,
  tag = NULL,
  graph = NULL,
  dims = 1:10,
  nfeatures = 2000,
  seed = 123,
  prefix = "Scissor",
  tool_name = "Scissor",
  store_inputs = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A \code{Seurat} object containing single-cell expression data.}

\item{bulk_dataset}{A bulk expression matrix-like object or a
\code{SummarizedExperiment}. Rows are genes and columns are bulk samples.}

\item{phenotype}{Phenotype annotation for bulk samples. For
\code{family = "binomial"}, character or factor values are converted to 0/1
according to \code{positive}. If \code{NULL} and \code{bulk_dataset} is a
\code{SummarizedExperiment}, \code{condition.by} is used.}

\item{condition.by}{Column in \code{colData(bulk_dataset)} used as phenotype
when \code{phenotype = NULL}.}

\item{positive}{Positive phenotype level for binomial Scissor. If \code{NULL},
the second sorted level is used.}

\item{assay}{Which assay to use.
If \code{NULL}, the default assay of the Seurat object will be used.
When the object also contains \code{ChromatinAssay}, the default assay and
additional \code{ChromatinAssay} will be preprocessed sequentially.}

\item{bulk_assay}{Assay name used when \code{bulk_dataset} is a
\code{SummarizedExperiment}.}

\item{layer}{Assay layer used from \code{srt}.}

\item{features}{Optional genes used before intersecting bulk and
single-cell features.}

\item{family}{Regression family passed to Scissor.}

\item{backend}{Scissor backend. \code{"r"} calls the upstream package path and
\code{"cpp"} uses the optimized \code{scop} path. Legacy aliases \code{"original"} and
\code{"scop"} are accepted as \code{"r"} and \code{"cpp"}, respectively.}

\item{alpha}{Scissor alpha search values. If \code{NULL}, Scissor's default grid
is used.}

\item{cutoff}{Maximum selected-cell fraction used by Scissor's alpha search.}

\item{tag}{Optional phenotype labels for printed/stored summaries.}

\item{graph}{Existing Seurat graph used by the \code{"cpp"} backend. If \code{NULL},
an assay SNN graph or \code{standard_scop()} graph is reused when present,
otherwise a temporary Scissor-like graph is built.}

\item{dims}{PCA dimensions used when a temporary graph is built.}

\item{nfeatures}{Number of variable features used when a temporary graph is
built.}

\item{seed}{Random seed used by Scissor's alpha loop.}

\item{prefix}{Prefix for metadata column names.}

\item{tool_name}{Name of the \code{srt@tools} entry.}

\item{store_inputs}{Whether to store Scissor regression inputs in
\code{srt@tools}. Default is \code{FALSE} to avoid large objects.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with Scissor status and coefficient columns in
metadata and a Scissor result bundle in \code{srt@tools[[tool_name]]}.
}
\description{
Run Scissor phenotype-associated cell selection
}
\examples{
data(panc8_sub)
data(islet_bulk)
panc8_sub <- standard_scop(panc8_sub, verbose = FALSE)
panc8_sub <- RunScissor(
  panc8_sub,
  bulk_dataset = islet_bulk,
  condition.by = "condition",
  positive = "bfa",
  family = "binomial",
  features = head(
  intersect(
    rownames(panc8_sub),
    rownames(SummarizedExperiment::assay(islet_bulk, "counts"))
  ), 1000),
  alpha = 0.2,
  cutoff = 0.5
)

ScissorPlot(
  panc8_sub,
  xlab = "UMAP_1",
  ylab = "UMAP_2"
)
}
\references{
Sun, D. et al. Identifying phenotype-associated subpopulations by
integrating bulk and single-cell sequencing data. \emph{Nature
Biotechnology} (2021). \doi{10.1038/s41587-021-01091-3}
}
\seealso{
\link{ScissorPlot}
}
