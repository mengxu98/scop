% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scTenifoldKnkPlot.R
\name{scTenifoldNetPlot}
\alias{scTenifoldNetPlot}
\title{scTenifoldNet Plot}
\usage{
scTenifoldNetPlot(
  srt,
  tool_name = "scTenifoldNet",
  plot_type = c("qq", "effect", "network", "manifold"),
  network = c("X", "Y"),
  top_n = 20,
  FDR_threshold = 0.05,
  FC_threshold = 1,
  features = NULL,
  label = TRUE,
  nlabel = 10,
  pt.size = 2,
  pt.alpha = 0.85,
  label.size = 3.5,
  edge_top_n = 100,
  edge_threshold = NULL,
  arrow.length = 0.14,
  arrow.linewidth = 0.8,
  arrow.alpha = 0.8,
  start.pt.size = 2.2,
  end.pt.size = 3,
  manifold.aspect.ratio = 1,
  cols.sig = "#D7301F",
  cols.ns = "grey70",
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object containing a scTenifoldNet result in \code{srt@tools}.}

\item{tool_name}{Name of the \code{srt@tools} entry created by
\link{RunscTenifoldNet}.}

\item{plot_type}{Plot type. \code{"qq"} shows the common \code{FC} versus theoretical
chi-square quantile plot. \code{"effect"} ranks top differential-regulation
genes by \code{FC}. \code{"network"} draws a condition-specific tensor network
subgraph from saved tensor networks using \link[thisplot:GraphPlot]{thisplot::GraphPlot}.
\code{"manifold"} draws paired condition displacement arrows in the non-linear
manifold-alignment space.}

\item{network}{Tensor network to show for \code{plot_type = "network"}.
Upstream scTenifoldNet results usually contain \code{"X"} and \code{"Y"} networks.}

\item{top_n}{Number of top genes to show or label.}

\item{FDR_threshold}{Adjusted p-value cutoff used to mark significant genes.}

\item{FC_threshold}{FC cutoff used to mark significant genes.}

\item{features}{Character vector of genes to show or label. For
\code{plot_type = "network"} and \code{"manifold"}, these genes are included in the
displayed subset when present.}

\item{label}{Whether to label genes.}

\item{nlabel}{Number of labels to add when \code{features = NULL}.}

\item{pt.size}{Point size.}

\item{pt.alpha}{Point transparency.}

\item{label.size}{Label text size.}

\item{edge_top_n}{Maximum number of network edges to draw.}

\item{edge_threshold}{Minimum absolute edge weight for
\code{plot_type = "network"}. If \code{NULL}, the largest \code{edge_top_n} edges are used.}

\item{arrow.length}{Arrow head length for \code{plot_type = "manifold"}.}

\item{arrow.linewidth}{Arrow line width for \code{plot_type = "manifold"}.}

\item{arrow.alpha}{Arrow transparency for \code{plot_type = "manifold"}.}

\item{start.pt.size}{WT-position point size for \code{plot_type = "manifold"}.}

\item{end.pt.size}{KO-position point size for \code{plot_type = "manifold"}.}

\item{manifold.aspect.ratio}{Panel aspect ratio for
\code{plot_type = "manifold"}. Default \code{1} keeps the panel square.
Set \code{NULL} to use the device aspect ratio.}

\item{cols.sig}{Color for significant genes.}

\item{cols.ns}{Color for non-significant genes.}

\item{xlab, ylab}{Axis labels.}

\item{title}{Plot title. If \code{NULL}, a default title is used.}

\item{theme_use}{Theme function used to style the plot.}

\item{theme_args}{Other arguments passed to \code{theme_use}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize condition-level network comparison results generated by
\link{RunscTenifoldNet}.
}
\examples{
data(pancreas_sub)
counts <- GetAssayData5(pancreas_sub, assay = "RNA", layer = "counts")
detected <- names(sort(Matrix::rowSums(counts > 0), decreasing = TRUE))
features_use <- head(detected, 300)

pancreas_sub <- RunscTenifoldNet(
  pancreas_sub,
  group.by = "CellType",
  condition1 = "Ductal",
  condition2 = "Endocrine",
  features = features_use,
  qc = FALSE,
  nc_nNet = 3,
  nc_nCells = 200,
  td_maxIter = 200,
  ma_nDim = 2,
  store_networks = TRUE,
  store_manifold = TRUE
)

scTenifoldNetPlot(pancreas_sub, plot_type = "effect")

scTenifoldNetPlot(pancreas_sub, plot_type = "network", network = "X")

scTenifoldNetPlot(pancreas_sub, plot_type = "manifold")
}
