% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMilo.R
\name{RunMilo}
\alias{RunMilo}
\title{Milo differential abundance wrapper}
\usage{
RunMilo(
  srt,
  group.by,
  split.by,
  sample.by,
  comparison = NULL,
  milo_k = 20L,
  milo_d = 30L,
  reduction = NULL,
  backend = c("r", "cpp"),
  n_bootstrap = 500,
  seed = 11,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A Seurat object.}

\item{group.by}{Name of one or more meta.data columns to group (color) cells by.}

\item{split.by}{Metadata column that identifies the condition groups to
compare. For sample-level methods, if \code{split.by} is omitted and \code{sample.by}
is provided, \code{sample.by} is treated as the condition column and virtual
samples are created within each condition.}

\item{sample.by}{Metadata column that identifies biological samples.
For \code{"milo"}, \code{"sccoda"}, and \code{"propeller"}, when \code{sample.by} is omitted
or identical to \code{split.by}, virtual samples are created within each
\code{split.by} group for convenience.}

\item{comparison}{Optional: specify comparisons to perform.}

\item{milo_k}{Number of nearest neighbors used for Milo graph building.}

\item{milo_d}{Number of dimensions used by Milo.}

\item{reduction}{Dimensional reduction used for Milo graph construction.
If \code{NULL}, the default PCA-like reduction is used.}

\item{backend}{Backend used to compute Milo neighborhoods and tests.
\code{"r"} calls the upstream \code{miloR} implementation. \code{"cpp"} uses native
\code{scop} graph/neighborhood/counting kernels and keeps edgeR for the
neighborhood-level quasi-likelihood test.}

\item{n_bootstrap}{Number of bootstrap iterations used by the group-level summary.}

\item{seed}{Random seed.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A method result bundle used internally by \link{RunProportionTest}.
}
\description{
Method-specific implementation used by \link{RunProportionTest} when
\code{proportion_method = "milo"}.
The function always returns a group-level summary and additionally stores a
neighborhood-level result list under \code{neighborhood_results}.
}
