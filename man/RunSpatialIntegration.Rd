% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialIntegration.R
\name{RunSpatialIntegration}
\alias{RunSpatialIntegration}
\title{Run multi-sample spatial integration}
\usage{
RunSpatialIntegration(
  object,
  method = "PRECAST",
  sample.by = NULL,
  assay = NULL,
  layer = "counts",
  coord.cols = c("col", "row"),
  features = NULL,
  image = NULL,
  reduction.name = NULL,
  cluster_colname = NULL,
  tool_name = "SpatialIntegration",
  store_results = TRUE,
  verbose = TRUE,
  coordinate_space = c("raw", "legacy_display"),
  ...
)
}
\arguments{
\item{object}{A merged spatial \code{Seurat} object or a list of spatial \code{Seurat}
objects.}

\item{method}{Spatial integration backend.}

\item{sample.by}{Metadata column identifying samples for a merged \code{Seurat}
object. For list input, list names are copied into this column.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{layer}{Assay layer used for expression values.}

\item{coord.cols}{Metadata coordinate columns used when no image is available.}

\item{features}{Features to score. If \code{NULL}, current variable features are
used; if no variable features are present, all assay features are used.}

\item{image}{Spatial image name. Required when multiple images are present;
a single image is selected automatically when \code{NULL}.}

\item{reduction.name}{Name of the integrated embedding reduction. If \code{NULL},
a method-specific name is used.}

\item{cluster_colname}{Metadata column used for spatial domain labels. If
\code{NULL}, a method-specific name is used.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store the full result in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{coordinate_space}{Coordinate system used for integration distances
and aligned-coordinate input. The default is raw acquisition coordinates;
\code{"legacy_display"} remains an explicit compatibility option.}

\item{...}{Additional backend-specific arguments.}
}
\value{
A \code{Seurat} object with spatial integration results stored in
metadata, reductions, and \code{srt@tools[[tool_name]]}.
}
\description{
Integrate multi-slice or multi-sample spatial transcriptomics data with an
optional spatial backend and store standardized embeddings, domains, and
aligned coordinates in a \code{Seurat} object.
}
\examples{
data(visium_human_pancreas_pair_sub)
SpatialIntegrationPlot(
  visium_human_pancreas_pair_sub,
  plot_type = "spatial",
  group.by = "domain",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
SpatialIntegrationPlot(
  visium_human_pancreas_pair_sub,
  plot_type = "embedding"
)

\dontrun{
check_r("PRECAST", verbose = FALSE)
spatial <- RunSpatialIntegration(
  visium_human_pancreas_pair_sub,
  method = "PRECAST", sample.by = "sample", assay = "Spatial",
  coord.cols = c("x", "y"),
  features = rownames(visium_human_pancreas_pair_sub)[1:100],
  verbose = FALSE
)
}
}
