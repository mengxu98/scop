% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunDeconvolution.R
\name{RunCIBERSORT}
\alias{RunCIBERSORT}
\title{Run CIBERSORT deconvolution}
\usage{
RunCIBERSORT(
  object = NULL,
  count_matrix = NULL,
  sig_matrix = "LM22",
  bulk_assay = "counts",
  perm = 100,
  QN = TRUE,
  absolute = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{Optional \code{SummarizedExperiment} object or expression matrix.
When a \code{SummarizedExperiment} is provided, results are stored in
\code{metadata(object)[["Deconvolution"]]}.}

\item{count_matrix}{Optional expression matrix with genes in rows and samples
in columns. Used when \code{object} is not provided as a matrix.}

\item{sig_matrix}{Signature matrix, local file path, or \code{"LM22"}.}

\item{bulk_assay}{Assay name in \code{object} used as the bulk counts matrix.}

\item{perm}{Number of CIBERSORT permutations.}

\item{QN}{Whether CIBERSORT should use quantile normalization.}

\item{absolute}{Passed to CIBERSORT when supported by the installed package.}

\item{verbose}{Whether to print messages.}

\item{...}{Additional parameters forwarded to \code{CIBERSORT::cibersort()}.}
}
\value{
A deconvolution result bundle for matrix input, or the modified
\code{SummarizedExperiment} object for \code{SummarizedExperiment} input.
}
\description{
Estimate immune cell proportions from a bulk expression matrix using the
external \code{CIBERSORT} package. \code{sig_matrix = "LM22"} downloads the LM22
signature matrix from \code{mengxu98/datasets} and caches it locally.
}
\examples{
data(islet_bulk)

if (FALSE) {
# Run CIBERSORT
islet_bulk <- RunCIBERSORT(
  object = islet_bulk,
  sig_matrix = "LM22",
  bulk_assay = "counts",
  perm = 100,
  QN = TRUE
)

# Immune abundance stacked bar plot
p1 <- ImmuneAbundancePlot(
  object = islet_bulk,
  plot_type = "bar",
  group.by = "condition"
)
p1

# Immune cell correlation heatmap
p2 <- ImmuneAbundancePlot(
  object = islet_bulk,
  plot_type = "cor"
)
p2

# Gene-immune correlation butterfly plot
p3 <- GeneImmuneCorPlot(
  object = islet_bulk,
  features = rownames(SummarizedExperiment::assay(islet_bulk, "counts"))[1:3]
)
p3
}
}
