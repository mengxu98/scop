% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSmoothClust.R
\name{RunSmoothClust}
\alias{RunSmoothClust}
\title{Run smoothclust spatial domain clustering}
\usage{
RunSmoothClust(
  srt,
  assay = NULL,
  layer = "data",
  image = NULL,
  coord.cols = c("col", "row"),
  features = NULL,
  nfeatures = 2000,
  min_spots = 5,
  smooth_method = c("uniform", "kernel", "knn"),
  bandwidth = 0.05,
  k = 18,
  truncate = 0.05,
  n_threads = 1,
  n_clusters,
  n_pcs = 15,
  center = TRUE,
  scale = TRUE,
  nstart = 10,
  iter.max = 100,
  algorithm = "Hartigan-Wong",
  cluster_colname = "SmoothClust_cluster",
  tool_name = "SmoothClust",
  store_results = TRUE,
  store_smoothed = FALSE,
  seed = 11,
  verbose = TRUE,
  coordinate_space = c("raw", "legacy_display"),
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object.}

\item{assay}{Assay used for expression. If \code{NULL}, the default assay is used.}

\item{layer}{Assay layer used for expression values.}

\item{image}{Name of the Seurat spatial image. Required when multiple images
are present; a single image is selected automatically when \code{NULL}.}

\item{coord.cols}{Metadata coordinate columns used when no Seurat image is
available.}

\item{features}{Features to use. If \code{NULL}, current variable features are
used; if no variable features are present, the top \code{nfeatures} by variance
are used.}

\item{nfeatures}{Number of variance-ranked features to use when
\code{features = NULL} and no variable features are present.}

\item{min_spots}{Minimum number of spots with non-zero expression required
for a feature to be used.}

\item{smooth_method}{Smoothing method passed to \code{smoothclust::smoothclust()}.}

\item{bandwidth, k, truncate, n_threads}{Smoothing parameters passed to
\code{smoothclust::smoothclust()}. \code{bandwidth} uses the selected coordinate
units; \code{truncate} is the backend's dimensionless kernel cutoff, and \code{k} and
\code{n_threads} are unitless counts.}

\item{n_clusters}{Number of spatial domains for k-means clustering. This
must be supplied explicitly.}

\item{n_pcs}{Number of principal components used for k-means.}

\item{center, scale}{Whether to center and scale features before PCA.}

\item{nstart, iter.max, algorithm}{Parameters passed to \code{stats::kmeans()}.}

\item{cluster_colname}{Metadata column used for smoothclust clusters.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed results in \code{srt@tools}.}

\item{store_smoothed}{Whether to store the smoothed expression matrix in
\code{srt@tools[[tool_name]]}. This can be large.}

\item{seed}{Random seed used for k-means.}

\item{verbose}{Whether to print progress messages.}

\item{coordinate_space}{Coordinate system used for distance-sensitive
smoothing and smoothness calculations. The default is raw acquisition
coordinates. Use \code{"legacy_display"} explicitly to reproduce the
display-scaled behavior used before scop 0.9.0.}

\item{...}{Additional arguments passed to \code{smoothclust::smoothclust()}.}
}
\value{
A \code{Seurat} object with smoothclust clusters in metadata. When
\code{store_results = TRUE}, detailed outputs are stored in
\code{srt@tools[[tool_name]]}.
}
\description{
Smooth expression across spatial neighborhoods with the optional
\code{smoothclust} package, then cluster the smoothed profiles into spatial
domains with PCA and k-means.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
spatial$SmoothClust_cluster <- factor(
  paste0("SmoothClust", (seq_len(ncol(spatial)) - 1) \%\% 3 + 1)
)

SpatialSpotPlot(
  spatial,
  group.by = "SmoothClust_cluster",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)

spatial <- Seurat::NormalizeData(spatial, assay = "Spatial", verbose = FALSE)
spatial <- Seurat::FindVariableFeatures(
  spatial,
  assay = "Spatial",
  nfeatures = 200,
  verbose = FALSE
)

spatial <- RunSmoothClust(
  spatial,
  assay = "Spatial",
  n_clusters = 3,
  smooth_method = "knn",
  coord.cols = c("x", "y"),
  k = 6,
  verbose = FALSE
)

table(spatial$SmoothClust_cluster)
}
\concept{spatial-producer}
