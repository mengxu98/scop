% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RuntAge.R
\name{RuntAge}
\alias{RuntAge}
\title{Run tAge transcriptomic aging-clock prediction}
\usage{
RuntAge(
  object,
  model_paths = NULL,
  model_backend = c("auto", "r", "python"),
  clock = c("Chronoage", "NormalizedAge", "Mortality"),
  model_species = c("auto", "Multispecies", "Mouse", "Rodents"),
  model_tissue = "Multitissue",
  model_preprocessing = "scaled_diff",
  model_cache_dir = NULL,
  datasets_base_url = "https://raw.githubusercontent.com/mengxu98/datasets/main/tAge/EN",
  zenodo_record = "18763485",
  max_model_size = 1024^3,
  metadata = NULL,
  group.by = NULL,
  split.by = NULL,
  assay = NULL,
  layer = "counts",
  species = c("mouse", "human", "rat", "monkey"),
  mode = c("EN", "BR"),
  gene_mapping_type = c("Gene.Symbol", "Ensembl"),
  coverage_threshold = 1e+06,
  count_threshold = 10,
  percent_threshold = 20,
  control_group_column = NULL,
  control_group_label = NULL,
  remove_outliers = FALSE,
  outlier.by = split.by,
  outlier_n_components = 10,
  outlier_threshold_quantile = 0.99,
  outlier_min_samples = 10,
  min_samples = 5,
  shuffle = FALSE,
  seed = NULL,
  check_python = TRUE,
  tool_name = "tAge",
  store_eset = FALSE,
  store_processed = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{Seurat} object, \code{ExpressionSet}, or expression matrix-like
object with genes in rows and pseudobulk/bulk samples in columns.}

\item{model_paths}{Named list or character vector of local tAge model files
(\code{.rds} for \code{model_backend = "r"}, \code{.pkl} for \code{model_backend = "python"}).
Names must match one or more of \code{"scaled"}, \code{"scaled_diff"}, \code{"yugene"},
and \code{"yugene_diff"}. If \code{NULL}, model files are downloaded from
\code{mengxu98/datasets} for the R backend or Zenodo for the Python backend.}

\item{model_backend}{Model prediction backend. \code{"auto"} uses \code{"r"} for
\code{mode = "EN"} and \code{"python"} for \code{mode = "BR"}.}

\item{clock}{tAge clock family used when \code{model_paths = NULL}.}

\item{model_species}{Model species scope used when \code{model_paths = NULL}.
\code{"auto"} chooses \code{"Mouse"} for \code{species = "mouse"}, \code{"Rodents"} for
\code{species = "rat"}, and \code{"Multispecies"} otherwise.}

\item{model_tissue}{Model tissue scope used when \code{model_paths = NULL}.}

\item{model_preprocessing}{Preprocessing-specific model file(s) used when
\code{model_paths = NULL}. Values map to tAge model filename suffixes.}

\item{model_cache_dir}{Directory used to cache downloaded tAge models. If
\code{NULL}, uses \code{tools::R_user_dir("scop", "data")}.}

\item{datasets_base_url}{Base URL or local directory containing converted
tAge EN models from \code{mengxu98/datasets}.}

\item{zenodo_record}{Zenodo record ID for tAge model files.}

\item{max_model_size}{Maximum model file size, in bytes, allowed for
automatic download. Increase this or use \code{Inf} for large Bayesian Ridge
models.}

\item{metadata}{Sample metadata for matrix input. Row names must match
columns of \code{object}. If \code{NULL}, minimal \code{sample_id} metadata is created.}

\item{group.by}{Metadata columns used to form pseudobulk groups for \code{Seurat}
input. If \code{NULL}, cells are aggregated sequentially across the whole object.
\code{split.by} and \code{control_group_column} are automatically included in the
aggregation metadata when provided.}

\item{split.by}{Optional pseudobulk metadata column used to run tAge
separately by group, such as tissue.}

\item{assay, layer}{Assay and layer used as raw counts for \code{Seurat} input.}

\item{species}{Species passed to tAge. Supported values are \code{"mouse"},
\code{"human"}, \code{"rat"}, and \code{"monkey"}.}

\item{mode}{tAge model mode: \code{"EN"} for Elastic Net or \code{"BR"} for Bayesian
Ridge.}

\item{gene_mapping_type}{Gene identifier type passed to tAge preprocessing:
\code{"Gene.Symbol"} or \code{"Ensembl"}.}

\item{coverage_threshold}{Minimum cumulative read count per pseudobulk
sample for \code{Seurat} input.}

\item{count_threshold, percent_threshold}{Gene filtering thresholds passed to
\code{tAge::tAge_preprocessing()}.}

\item{control_group_column, control_group_label}{Optional control group used
by tAge control subtraction.}

\item{remove_outliers}{Whether to run \code{tAge::remove_outliers()} on the
pseudobulk \code{ExpressionSet}.}

\item{outlier.by}{Optional metadata column used to split samples before
outlier detection. Defaults to \code{split.by}.}

\item{outlier_n_components, outlier_threshold_quantile, outlier_min_samples}{Parameters passed to \code{tAge::remove_outliers()}.}

\item{min_samples}{Minimum pseudobulk samples per \code{split.by} group for
\code{tAge::tAge_by_group()}.}

\item{shuffle, seed}{Whether to shuffle cells during pseudobulk aggregation
and the random seed used for that shuffle.}

\item{check_python}{Whether to verify that the active \code{reticulate} Python can
import \code{joblib}, \code{pandas}, and \code{sklearn} before tAge prediction.}

\item{tool_name}{Name of the Seurat tool entry used to store results.}

\item{store_eset, store_processed}{Whether to store the pseudobulk
\code{ExpressionSet} and processed tAge \code{ExpressionSet} list in the returned
Seurat tool entry or list result.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A \code{Seurat} object with tAge results stored in \code{object@tools}, or a
list with \code{predictions}, metadata, and parameters for non-Seurat input.
}
\description{
Run tAge transcriptomic aging-clock prediction
}
\examples{
\dontrun{
data(pancreas_sub)
pancreas_sub <- RuntAge(
  pancreas_sub,
  group.by = "CellType",
  species = "mouse",
  mode = "EN"
)
head(pancreas_sub@tools$tAge$predictions)
}
}
\references{
Tyshkovskiy, A., Glubokov, D., Moliere, A., et al. (2026). Universal
transcriptomic hallmarks of mammalian ageing and mortality.
\emph{Nature}. \doi{10.1038/s41586-026-10542-3}
}
