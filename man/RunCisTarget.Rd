% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCisTarget.R
\name{RunCisTarget}
\alias{RunCisTarget}
\title{Run cisTarget motif enrichment on a GRN adjacency table}
\usage{
RunCisTarget(
  adj,
  species = c("Homo_sapiens", "Mus_musculus", "Drosophila_melanogaster"),
  backend = c("python", "r"),
  ranking_dbs = NULL,
  motif_annotations = NULL,
  expression_mtx = NULL,
  ctx_output = NULL,
  min_regulon_size = 10,
  gmt_output = NULL,
  txt_output = NULL,
  work_dir = NULL,
  prefix = "cisTarget",
  data_dir = NULL,
  envname = NULL,
  conda = "auto",
  cores = 1,
  force = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{adj}{A data frame with columns \code{TF}, \code{target}, and optionally
\code{importance}, or a path to a TSV adjacency file. This is typically the
output of \code{\link[=RunGRNBoost2]{RunGRNBoost2()}}, \code{\link[=RunGENIE3]{RunGENIE3()}}, or \code{\link[=RunSCENIC]{RunSCENIC()}} step 1.}

\item{species}{Species used to select cisTarget reference files when
\code{ranking_dbs}, \code{motif_annotations}, or \code{regulators} is \code{NULL}. Supported
values are \code{"Homo_sapiens"}, \code{"Mus_musculus"}, and
\code{"Drosophila_melanogaster"}.}

\item{backend}{cisTarget runtime backend.
\describe{
\item{\code{"python"}}{Uses the pySCENIC/ctxcore Python pipeline. This is the
most tested backend and produces results identical to official SCENIC.}
\item{\code{"r"}}{Uses the \code{RcisTarget} Bioconductor package. Requires the
cisTarget ranking databases and motif annotations to be available in
the same format as the Python backend (feather files and motif2tf
table).}
}}

\item{ranking_dbs}{Character vector of cisTarget ranking feather files.}

\item{motif_annotations}{Motif annotation table path (motif2tf).}

\item{expression_mtx}{Optional expression matrix path (CSV). When
\code{NULL} and \code{backend = "python"}, the expression matrix is reconstructed
from the unique genes in \code{adj} as a minimal stub.}

\item{ctx_output}{Optional output file path for the cisTarget result.}

\item{min_regulon_size}{Minimum number of target genes per regulon.}

\item{gmt_output}{Optional output path for the regulon GMT file.}

\item{txt_output}{Optional output path for the regulon TXT file.}

\item{work_dir}{Working directory used by Python backend.}

\item{prefix}{Prefix for output files.}

\item{data_dir}{Directory used to cache automatically prepared SCENIC
reference files. If \code{NULL}, files are stored under
\verb{tools::R_user_dir("scop", "data")/SCENIC/<species>}.}

\item{envname}{Python environment name (Python backend only).}

\item{conda}{Conda-compatible executable (Python backend only).}

\item{cores}{Number of workers.}

\item{force}{Whether to rebuild existing outputs.}

\item{verbose}{Whether to print progress messages.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
A named list with components \code{regulons} (list of gene vectors),
\code{ctx_file} (path to raw cisTarget output), \code{gmt_file}, and \code{txt_file}.
}
\description{
cisTarget performs motif enrichment analysis on gene regulatory network
adjacency tables. For each transcription factor, it identifies target
genes whose regulatory regions are enriched for the TF's binding motifs,
producing regulons (TF + enriched target gene sets).
}
\examples{
\dontrun{
data(pancreas_sub)
pancreas_sub <- standard_scop(pancreas_sub)

# First run GRN inference
grn <- RunGRNBoost2(
  pancreas_sub,
  regulators = c("Neurod1", "Arx", "Pax6"),
  backend = "cpp"
)

# Then run cisTarget (Python backend)
regulons <- RunCisTarget(
  grn,
  species = "Mus_musculus",
  backend = "python"
)
}
}
