% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PrepareEnv.R
\name{PrepareEnv}
\alias{PrepareEnv}
\title{Prepare the python environment}
\usage{
PrepareEnv(
  envname = NULL,
  conda = "auto",
  miniconda_repo = "https://repo.anaconda.com/miniconda",
  version = if (is_windows()) "3.11-1" else "3.10-1",
  force = FALSE,
  modules = NULL,
  pip_options = character(),
  components = "python",
  cores = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{envname}{The name of the conda-compatible Python environment.
If \code{NULL}, the environment name will be set to \code{"scop_env"}.
Default is \code{NULL}.}

\item{conda}{The path or command name of a conda-compatible executable
(\code{conda}, \code{mamba}, or \code{micromamba}). Use \code{"auto"} to allow automatically
finding an appropriate environment manager. If \code{"micromamba"} is requested
and micromamba is not available on \code{PATH}, a package-managed micromamba is
downloaded automatically.}

\item{miniconda_repo}{Repository URL for miniconda.
Default is \url{https://repo.anaconda.com/miniconda}.}

\item{version}{The Python version.
Default is \code{"3.10-1"} on macOS and Unix and \code{"3.11-1"} on Windows.}

\item{force}{Whether to force recreation of the environment.
If \code{TRUE}, the existing environment will be removed and recreated.
Default is \code{FALSE}.}

\item{modules}{Optional Python dependency modules to install in addition to
the default scientific stack.
If \code{NULL} or omitted in \code{\link[=PrepareEnv]{PrepareEnv()}}, the default environment is installed.
The default excludes \code{"cell2location"}, \code{"sccoda"}, and \code{"scomm"} because
cell2location is a long-running explicit workflow and the TensorFlow stacks
are not compatible with the default JAX/scVI stack in the same environment;
request them explicitly for scCODA/scOMM workflows. \code{"scenic"} is also
excluded from the default environment and is prepared in \code{"scenic_env"} by
default because SCENIC requires an older Python/numpy stack. On Windows, the
default also excludes \code{"scvi"}, \code{"glue"}, and \code{"multimap"} because those
upstream stacks are more reliable when requested explicitly for
method-specific workflows.}

\item{pip_options}{Additional command line arguments to be passed to \code{uv}/\code{pip} when installing pip packages.}

\item{components}{Components to prepare. Supported values are \code{"python"},
\code{"r"}, and \code{"all"}; \code{"all"} expands to both Python and R. The default,
\code{"python"}, preserves the existing Python-only behavior. The R component
collects packages declared through \code{check_r()} and checks/installs them,
including optional workflow dependencies not listed in \code{DESCRIPTION}.}

\item{cores}{Number of workers for R-package installation. Use \code{NULL} (the
default) to let pak select its worker count automatically.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to package installation functions.}
}
\description{
Prepare the python environment by installing the required dependencies and setting up the environment.
}
