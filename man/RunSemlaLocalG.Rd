% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSemla.R
\name{RunSemlaLocalG}
\alias{RunSemlaLocalG}
\title{Run semla local G spatial autocorrelation}
\usage{
RunSemlaLocalG(
  srt,
  features,
  alternative = NULL,
  store_in_metadata = TRUE,
  assay_name = "GiScores",
  image_type = "tissue_lowres",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object with spatial image data.}

\item{features}{Features passed to \code{semla::RunLocalG()}.}

\item{alternative}{Alternative hypothesis passed to semla. Use \code{NULL} to
keep semla's default behavior.}

\item{store_in_metadata}{Whether semla should store results in metadata.}

\item{assay_name}{Assay name used by semla when \code{store_in_metadata = FALSE}.}

\item{image_type}{Image scale used by \code{semla::UpdateSeuratForSemla()} when
the object does not already contain a Staffli object.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to semla.}
}
\value{
A \code{Seurat} object.
}
\description{
Use \code{semla::RunLocalG()} on a Staffli-enabled Seurat object. Results are
written by semla to metadata or to an assay, depending on
\code{store_in_metadata}.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- subset(
  visium_human_pancreas_sub,
  cells = colnames(visium_human_pancreas_sub)[1:120],
  features = rownames(visium_human_pancreas_sub)[1:400]
)
spatial <- Seurat::NormalizeData(spatial, assay = "Spatial", verbose = FALSE)
features <- rownames(spatial)[1:3]
spatial[[paste0(features[1], "_localG")]] <- as.numeric(scale(spatial$x))

SpatialSpotPlot(
  spatial,
  group.by = paste0(features[1], "_localG"),
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)

if (
  isTRUE(check_r("spatial-research/semla", verbose = FALSE))
) {
spatial <- RunSemlaLocalG(
  spatial,
  features = features,
  store_in_metadata = TRUE,
  verbose = FALSE
)
}
}
