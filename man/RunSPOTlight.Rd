% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSPOTlight.R
\name{RunSPOTlight}
\alias{RunSPOTlight}
\title{Run SPOTlight spatial deconvolution}
\usage{
RunSPOTlight(
  srt,
  reference,
  reference_label = "celltype",
  assay = NULL,
  reference_assay = NULL,
  layer = "counts",
  reference_layer = "counts",
  features = NULL,
  mgs = NULL,
  marker_top_n = 100,
  marker_min_logfc = 0,
  gene_id = "gene",
  group_id = "cluster",
  weight_id = "weight",
  min_prop = 0.01,
  scale = TRUE,
  prefix = "SPOTlight",
  tool_name = "SPOTlight",
  store_results = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Spatial \code{Seurat} object used as the RCTD query.}

\item{reference}{Reference \code{Seurat} object containing annotated single cells.}

\item{reference_label}{Metadata column in \code{reference} with cell type labels.}

\item{assay}{Assay used in \code{srt}. If \code{NULL}, the default assay is used.}

\item{reference_assay}{Assay used in \code{reference}.}

\item{layer, reference_layer}{Assay layers used for spatial and reference
raw counts.}

\item{features}{Features used for RCTD. If \code{NULL}, shared features are used.}

\item{mgs}{Optional marker-gene table passed to \code{SPOTlight}. It must contain
columns named by \code{gene_id}, \code{group_id}, and \code{weight_id}. If \code{NULL}, a simple
group-vs-rest marker table is generated from the reference expression matrix.}

\item{marker_top_n}{Number of automatically generated marker genes retained
per reference cell type.}

\item{marker_min_logfc}{Minimum group-vs-rest log2 fold-change used when
generating markers automatically.}

\item{gene_id, group_id, weight_id}{Column names in \code{mgs} for gene IDs, cell
type labels, and marker weights.}

\item{min_prop}{Minimum cell-type proportion passed to \code{SPOTlight}.}

\item{scale}{Whether \code{SPOTlight} scales expression internally.}

\item{prefix}{Prefix for metadata columns.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed RCTD results in \code{srt@tools}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters passed to \code{SPOTlight::SPOTlight()}.}
}
\value{
A \code{Seurat} object with \code{SPOTlight} proportion columns in metadata and
dominant cell type summaries. When \code{store_results = TRUE}, detailed results
are stored in \code{srt@tools[[tool_name]]}.
}
\description{
Estimate spot-level cell type proportions from a spatial \code{Seurat} object
using a single-cell \code{Seurat} reference and the optional \code{SPOTlight} package.
The example is a non-executing template because the backend is optional and
may require additional Bioconductor dependencies.
}
\examples{
\dontrun{
thisutils::check_r("SPOTlight", verbose = FALSE)
data(visium_human_pancreas_sub)
data(panc8_sub)
keep_spots <- unique(round(seq(
  1,
  ncol(visium_human_pancreas_sub),
  length.out = 120
)))
spatial <- visium_human_pancreas_sub[, keep_spots]
reference <- panc8_sub[, panc8_sub@meta.data[["celltype"]] \%in\%
  c("ductal", "alpha", "beta")]
reference <- Seurat::FindVariableFeatures(reference, nfeatures = 300, verbose = FALSE)
features_use <- head(intersect(
  SeuratObject::VariableFeatures(reference),
  rownames(spatial)
), 300)
spatial <- RunSPOTlight(
  srt = spatial,
  reference = reference,
  reference_label = "celltype",
  assay = "Spatial",
  reference_assay = "RNA",
  features = features_use,
  marker_top_n = 20,
  verbose = FALSE
)
SpatialDeconvolutionPlot(
  spatial,
  tool_name = "SPOTlight",
  plot_type = "dominant",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
}
}
