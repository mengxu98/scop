% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSpatialNetwork.R
\name{RunSpatialNetwork}
\alias{RunSpatialNetwork}
\title{Build a native spatial network}
\usage{
RunSpatialNetwork(
  srt = NULL,
  method = c("knn", "radius"),
  image = NULL,
  coord.cols = c("col", "row"),
  k = 6,
  radius = NULL,
  graph.name = NULL,
  overwrite = FALSE,
  verbose = TRUE,
  object = NULL
)
}
\arguments{
\item{srt}{A `Seurat` object. The same object may be supplied as
\code{object =} for consistency with spatial plotting APIs.}

\item{object}{Optional alias for \code{srt}. Supply exactly one of \code{srt} or
\code{object}.}

\item{method}{Network method, either `"knn"` or `"radius"`.}

\item{image}{Seurat image name. A single image is selected automatically;
multi-image objects require an explicit value.}

\item{coord.cols}{Metadata columns used when the object has no image.}

\item{k}{Number of neighbors for `method = "knn"`.}

\item{radius}{Positive distance threshold for `method = "radius"`, expressed
in the raw coordinate units.}

\item{graph.name}{Optional graph name. If `NULL`, a deterministic name is
generated from the image, method, and method parameter.}

\item{overwrite}{Whether an existing graph with the same name may be
replaced.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
The input `Seurat` object with a `SpatialNetwork` result in
  `srt@tools`.
}
\description{
Build a k-nearest-neighbor or radius spatial network from raw Seurat spatial
coordinates. Results are stored as named graphs in
`srt@tools$SpatialNetwork`.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
spatial <- RunSpatialNetwork(spatial, k = 6, verbose = FALSE)
SpatialNetworkPlot(spatial, group.by = "coda_label")

}
