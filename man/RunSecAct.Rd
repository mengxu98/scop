% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunSecAct.R
\name{RunSecAct}
\alias{RunSecAct}
\title{Run SecAct secreted protein activity inference}
\usage{
RunSecAct(
  srt = NULL,
  inputProfile = NULL,
  inputProfile_control = NULL,
  mode = c("auto", "matrix", "scRNAseq", "ST"),
  assay = NULL,
  layer = "counts",
  cellType_meta = NULL,
  is.singleCellLevel = FALSE,
  is.differential = FALSE,
  is.paired = FALSE,
  is.singleSampleLevel = FALSE,
  scale.factor = NULL,
  sigMatrix = "SecAct",
  is.filter.sig = FALSE,
  is.group.sig = TRUE,
  is.group.cor = 0.9,
  lambda = 5e+05,
  nrand = 1000,
  ncores = 1L,
  backend = "auto",
  rng_method = "mt19937",
  batch_size = NULL,
  output_h5 = NULL,
  activity = c("zscore", "beta", "se", "pvalue"),
  assay_out = "SecAct",
  store_assay = !is.null(srt),
  store_results = TRUE,
  tool_name = "SecAct",
  return_seurat = !is.null(srt),
  verbose = TRUE
)
}
\arguments{
\item{srt}{Optional Seurat object. When \code{mode = "scRNAseq"}, this is passed
to \code{SecAct.activity.inference.scRNAseq}. When \code{mode = "matrix"}, expression
is extracted from \code{srt} if \code{inputProfile} is not supplied.}

\item{inputProfile}{Expression matrix, Seurat object, or SpaCET object.
Matrix input must be genes x samples, cells, or spatial spots.}

\item{inputProfile_control}{Optional control expression matrix or SpaCET
object passed to SecAct.}

\item{mode}{SecAct workflow. \code{auto} dispatches from the input class.}

\item{assay}{Assay used when extracting a matrix from \code{srt}; also sets the
default assay for Seurat input passed to SecAct.}

\item{layer}{Assay layer used when \code{mode = "matrix"} and \code{inputProfile} is
not supplied.}

\item{cellType_meta}{Metadata column containing cell type or state labels for
\code{mode = "scRNAseq"} when \code{is.singleCellLevel = FALSE}.}

\item{is.singleCellLevel}{Whether SecAct should return single-cell activity
rather than cell-state activity for Seurat input.}

\item{is.differential, is.paired, is.singleSampleLevel}{Parameters passed to
\code{SecAct.activity.inference} for matrix or bulk input.}

\item{scale.factor}{Spot-level scale factor passed to
\code{SecAct.activity.inference.ST}.}

\item{sigMatrix}{SecAct signature matrix name.}

\item{is.filter.sig, is.group.sig, is.group.cor, lambda, nrand, ncores, backend, rng_method}{Parameters passed to SecAct activity inference.}

\item{batch_size, output_h5}{Optional large-matrix controls passed only to
\code{SecAct.activity.inference}.}

\item{activity}{Activity matrix to store as a Seurat assay when possible.}

\item{assay_out}{Name of the Seurat assay used to store activity values.}

\item{store_assay}{Whether to store a SecAct activity matrix as a Seurat
assay when its columns match Seurat cells.}

\item{store_results}{Whether to store raw SecAct results in \code{srt@tools}.}

\item{tool_name}{Name used in \code{srt@tools}.}

\item{return_seurat}{Whether to return a Seurat object when \code{srt} is
supplied.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}
}
\value{
A Seurat object, SpaCET object, or SecAct result list depending on
input and \code{return_seurat}.
}
\description{
Run the optional \code{SecAct} R package from \code{scop} to infer secreted protein
signaling activity from bulk, single-cell, or spatial transcriptomics
profiles. \code{SecAct} is not bundled with \code{scop}; it is checked and installed
from \code{data2intelligence/SecAct} when this function is called.
}
\examples{
\dontrun{
srt <- RunSecAct(
  srt,
  mode = "scRNAseq",
  cellType_meta = "celltype",
  is.singleCellLevel = TRUE
)

res <- RunSecAct(
  inputProfile = expr_mat,
  mode = "matrix",
  is.differential = TRUE
)
}
}
