% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpatialFrameworkConvert.R
\name{giotto_to_srt}
\alias{giotto_to_srt}
\title{Convert Giotto to Seurat}
\usage{
giotto_to_srt(giotto, ...)
}
\arguments{
\item{giotto}{A native Giotto object.}

\item{...}{Additional arguments passed to the Giotto converter.}
}
\value{
A new `Seurat` object.
}
\description{
Convert a native Giotto object into a new Seurat object.
}
\details{
The bridge selects the GiottoClass v4 or v5 converter according to the
installed Seurat major version. If GiottoClass is already installed, it is
reused instead of reinstalling \code{drieslab/Giotto}. For round trips produced by
\code{\link[=srt_to_giotto]{srt_to_giotto()}}, normalize the Seurat input first (for example with
\code{\link[Seurat:NormalizeData]{Seurat::NormalizeData()}}); the official GiottoClass converter records an
empty normalized layer otherwise, which its reverse converter cannot read
back. Conversion does not initialize Giotto's optional Python/conda
environment.
}
\examples{
\dontrun{
thisutils::check_r("drieslab/Giotto", verbose = FALSE)
data(visium_human_pancreas_sub)
spatial <- Seurat::NormalizeData(
  visium_human_pancreas_sub,
  assay = "Spatial",
  verbose = FALSE
)
giotto <- srt_to_giotto(spatial, image = "slice1")
roundtrip <- giotto_to_srt(giotto)
print(roundtrip)
}

}
