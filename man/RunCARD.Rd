% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunCARD.R
\name{RunCARD}
\alias{RunCARD}
\title{Run CARD spatial deconvolution}
\usage{
RunCARD(
  srt,
  reference,
  reference_label,
  assay = NULL,
  reference_assay = NULL,
  layer = "counts",
  reference_layer = "counts",
  features = NULL,
  image = NULL,
  coord.cols = c("col", "row"),
  sample_varname = NULL,
  minCountGene = 100,
  minCountSpot = 5,
  ct_select = NULL,
  prefix = "CARD",
  tool_name = "CARD",
  store_results = TRUE,
  round_counts = TRUE,
  create_card_params = list(),
  card_deconvolution_params = list(),
  verbose = TRUE,
  ...,
  coordinate_space = c("legacy_display", "raw")
)
}
\arguments{
\item{srt}{Spatial \code{Seurat} object used as the RCTD query.}

\item{reference}{Reference \code{Seurat} object containing annotated single cells.}

\item{reference_label}{Metadata column in \code{reference} with cell type labels.}

\item{assay}{Assay used in \code{srt}. If \code{NULL}, the default assay is used.}

\item{reference_assay}{Assay used in \code{reference}.}

\item{layer, reference_layer}{Assay layers used for spatial and reference
raw counts.}

\item{features}{Features used for RCTD. If \code{NULL}, shared features are used.}

\item{image}{Name of the Seurat spatial image used to recover coordinates
when \code{coord.cols} are not available.}

\item{coord.cols}{Metadata coordinate columns used when no image coordinate
source is requested or available.}

\item{sample_varname}{Optional metadata column in \code{reference} containing
sample labels. When \code{NULL}, all reference cells are assigned to one sample.}

\item{minCountGene, minCountSpot}{Filtering parameters passed to
\code{CARD::createCARDObject()} or \code{CARDspa::createCARDObject()} when supported.}

\item{ct_select}{Optional cell types to keep in CARD.}

\item{prefix}{Prefix for metadata columns.}

\item{tool_name}{Name used to store detailed results in \code{srt@tools}.}

\item{store_results}{Whether to store detailed RCTD results in \code{srt@tools}.}

\item{round_counts}{Whether to round non-integer counts to the nearest
integer before passing data to \code{spacexr}. RCTD requires integer count
matrices; this defaults to \code{TRUE} so bundled example data with scaled
non-integer reference counts can run directly.}

\item{create_card_params}{Additional parameters passed to
\code{createCARDObject()}.}

\item{card_deconvolution_params}{Additional parameters passed to
\code{CARD_deconvolution()}.}

\item{verbose}{Whether to print the message.
Default is \code{TRUE}.}

\item{...}{Additional parameters passed to the RCTD run step.}

\item{coordinate_space}{Coordinate space used for distance-sensitive input.
The default preserves the historical coordinate behavior.}
}
\value{
A \code{Seurat} object with CARD proportion columns in metadata and
dominant cell type summaries. When \code{store_results = TRUE}, detailed results
are stored in \code{srt@tools[[tool_name]]}.
}
\description{
Estimate spot-level cell-type proportions from a spatial \code{Seurat} object
using a single-cell \code{Seurat} reference and the optional \code{CARD}/\code{CARDspa}
backend.
}
\examples{
data(visium_human_pancreas_sub)
spatial <- visium_human_pancreas_sub
card_weights <- data.frame(
  CARD_prop_Ductal = seq(0.70, 0.20, length.out = ncol(spatial)),
  CARD_prop_Endocrine = seq(0.20, 0.70, length.out = ncol(spatial)),
  CARD_prop_Stromal = 0.10,
  row.names = colnames(spatial)
)
card_weights <- card_weights / rowSums(card_weights)
spatial <- Seurat::AddMetaData(spatial, card_weights)
spatial$CARD_dominant_type <- sub(
  "^CARD_prop_",
  "",
  colnames(card_weights)[max.col(card_weights)]
)
spatial$CARD_max_prop <- apply(card_weights, 1, max)

SpatialSpotPlot(
  spatial,
  group.by = "CARD_dominant_type",
  overlay_image = FALSE,
  coord.cols = c("x", "y")
)
  SpatialSpotPlot(
    spatial,
    group.by = "CARD_dominant_type",
    plot_type = "pie",
    overlay_image = FALSE,
    coord.cols = c("x", "y")
  )

data(pancreas_sub)
features_use <- head(intersect(rownames(spatial), rownames(pancreas_sub)), 300)
spatial <- RunCARD(
  spatial,
  reference = pancreas_sub,
  reference_label = "CellType",
  assay = "Spatial",
  reference_assay = "RNA",
  features = features_use,
  verbose = FALSE
)
}
\concept{spatial-producer}
