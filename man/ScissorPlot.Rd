% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ScissorPlot.R
\name{ScissorPlot}
\alias{ScissorPlot}
\title{Plot Scissor results}
\usage{
ScissorPlot(
  srt,
  plot_type = c("umap", "heatmap", "bar", "upset", "rose", "ring", "pie", "dot"),
  reduction = NULL,
  prefix = "Scissor",
  group.by = NULL,
  split.by = NULL,
  features = NULL,
  nfeatures = 50,
  feature_method = c("variance", "status_diff", "coef_cor", "input_order"),
  tool_name = "Scissor",
  status = c("Scissor+", "Scissor-"),
  include.background = TRUE,
  upset_top_n = NULL,
  cells = NULL,
  layer = "data",
  assay = NULL,
  max_cells = 100,
  cell_order = NULL,
  exp_method = "zscore",
  stat_type = c("percent", "count"),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Chinese",
  palcolor = NULL,
  heatmap_palette = "RdBu",
  group_palette = "Chinese",
  group_palcolor = NULL,
  cell_annotation = NULL,
  cell_annotation_palette = "Chinese",
  cell_annotation_palcolor = NULL,
  show_row_names = FALSE,
  show_column_names = FALSE,
  cluster_rows = FALSE,
  cluster_columns = FALSE,
  theme_use = "theme_scop",
  theme_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{A \code{Seurat} object after \link{RunScissor}.}

\item{plot_type}{Plot type. \code{"umap"} shows embedding panels, \code{"heatmap"}
shows a \code{FeatureHeatmap}, and statistical views such as \code{"bar"} and
\code{"upset"} are drawn with \link[thisplot:StatPlot]{thisplot::StatPlot}.}

\item{reduction}{Reduction to plot. \code{NULL} uses \link{DefaultReduction}.}

\item{prefix}{Prefix used by \link{RunScissor}.}

\item{group.by}{Optional metadata column shown together with Scissor status.
For \code{"heatmap"}, the default is the Scissor status column. For statistical
plots, it is passed to \link[thisplot:StatPlot]{thisplot::StatPlot}, except that \code{"upset"} uses it
to split Scissor status distributions by group.}

\item{split.by}{Metadata column to facet by.}

\item{features}{Features to plot.}

\item{nfeatures}{Number of features to show when \code{plot_type = "heatmap"} and
\code{features = NULL}.}

\item{feature_method}{Method used to rank heatmap features when
\code{features = NULL}. \code{"variance"} ranks genes by variance in selected cells,
\code{"status_diff"} ranks by the largest mean-expression difference between
Scissor status groups, \code{"coef_cor"} ranks by absolute correlation with
Scissor coefficients, and \code{"input_order"} keeps the \link{RunScissor} input order.}

\item{tool_name}{Name of the \code{srt@tools} entry created by \link{RunScissor}.}

\item{status}{Scissor status levels included in the heatmap.}

\item{include.background}{Whether to include background cells in heatmap and
background status in upset plots.}

\item{upset_top_n}{Maximum number of \code{group.by} levels to show in
\code{plot_type = "upset"}. The most frequent levels are kept. \code{NULL} keeps all.}

\item{cells}{Cell names to include.}

\item{layer}{Assay layer to use.}

\item{assay}{Assay to use. \code{NULL} uses the default assay.}

\item{max_cells}{An integer, maximum number of cells to sample per group.}

\item{cell_order}{A vector of cell names defining the order of cells.}

\item{exp_method}{Expression transform: \code{"zscore"}, \code{"raw"}, \code{"fc"}, \code{"log2fc"},
or \code{"log1p"}.}

\item{stat_type}{The type of statistic to compute for the plot.
Can be one of \code{"percent"}, \code{"count"}, or \code{"value"}. Continuous value mode
supports \code{plot_type = "bar"} and \code{plot_type = "dot"}.}

\item{combine}{Whether to combine UMAP panels or StatPlot panels.}

\item{nrow, ncol, byrow}{Layout parameters passed to \code{patchwork::wrap_plots()}
or \link[thisplot:StatPlot]{thisplot::StatPlot}.}

\item{pt.size, pt.alpha}{Point size and transparency. \code{pt.size = NULL} scales
with \code{sqrt(n)} (minimum \code{0.3}). Rasterized points keep at least a two-pixel
radius at \code{raster.dpi = c(512, 512)} and scale with \code{raster.dpi}.}

\item{palette, palcolor}{Palette name (\link[thisplot:show_palettes]{thisplot::show_palettes}) or custom colors.}

\item{theme_use, theme_args}{Theme used by \link[thisplot:StatPlot]{thisplot::StatPlot}.}

\item{verbose}{Print progress messages.}

\item{...}{Additional arguments passed to \link{CellDimPlot}, \link{FeatureDimPlot},
\link{FeatureHeatmap}, or \link[thisplot:StatPlot]{thisplot::StatPlot}, depending on \code{plot_type}.}
}
\value{
A ggplot/patchwork object for embedding and statistical plots, or a
list returned by \link{FeatureHeatmap} for \code{plot_type = "heatmap"}.
}
\description{
Plot Scissor results
}
\examples{
data(panc8_sub)
data(islet_bulk)
panc8_sub <- RunScissor(
  panc8_sub,
  bulk_dataset = islet_bulk,
  condition.by = "condition",
  positive = "bfa",
  family = "binomial",
  features = head(intersect(
    rownames(panc8_sub),
    rownames(SummarizedExperiment::assay(islet_bulk, "counts"))
  ), 1000),
  alpha = 0.2,
  cutoff = 0.5
)
panc8_sub <- RunStandardWorkflow(panc8_sub, verbose = FALSE)

ScissorPlot(
  panc8_sub,
  xlab = "UMAP_1",
  ylab = "UMAP_2"
)

ScissorPlot(
  panc8_sub,
  group.by = "celltype",
  xlab = "UMAP_1",
  ylab = "UMAP_2"
)

ht <- ScissorPlot(
  panc8_sub,
  plot_type = "heatmap",
  group.by = "celltype"
)
ht$plot

ScissorPlot(
  panc8_sub,
  plot_type = "bar",
  group.by = "celltype"
)

ScissorPlot(
  panc8_sub,
  plot_type = "upset"
)

ScissorPlot(
  panc8_sub,
  plot_type = "rose",
  label = TRUE
)

ScissorPlot(
  panc8_sub,
  plot_type = "ring",
  label = TRUE
)

ScissorPlot(
  panc8_sub,
  plot_type = "pie",
  label = TRUE
)

ScissorPlot(
  panc8_sub,
  plot_type = "dot",
  label = TRUE
)
}
\seealso{
\link{ScissorPlot}
}
