# Preprocess a scop Giotto object

Preprocess a scop Giotto object

## Usage

``` r
GiottoPreprocess(
  x,
  filter_params = list(),
  norm_params = list(),
  stat_params = list(),
  hvf_params = list(),
  verbose = TRUE,
  seed = 11
)
```
