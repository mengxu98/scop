# Run Giotto dimensional reduction

Run Giotto dimensional reduction

## Usage

``` r
GiottoReduce(
  x,
  reduction = c("pca", "umap"),
  dims = 1:20,
  name = NULL,
  features = NULL,
  params = list(),
  verbose = TRUE,
  seed = 11
)
```
