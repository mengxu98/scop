# Run UMAP

Run UMAP

## Usage

``` r
RunUMAP(object, ...)
```

## Arguments

- object:

  Object containing reduced-dimensional data.

- ...:

  Passed to methods.

## Value

The input object with a UMAP reduction added.
