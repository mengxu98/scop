# Add Giotto results back to Seurat

Add Giotto results back to Seurat

## Usage

``` r
AddGiottoToSeurat(
  srt,
  x,
  result = c("cluster", "hmrf"),
  name = NULL,
  tool_name = "Giotto",
  store_result = TRUE
)
```
