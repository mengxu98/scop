# Create a Giotto spatial network

Create a Giotto spatial network

## Usage

``` r
GiottoSpatialNetwork(
  x,
  network_method = c("Delaunay", "kNN"),
  network_name = NULL,
  params = list(),
  verbose = TRUE
)
```
